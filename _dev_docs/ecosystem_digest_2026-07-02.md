# Ecosystem digest — 2026-07-02

_Generated by the every-48h cloud routine. First run — no prior digest to diff against. Runtime: ~4m. Backends reached: WebSearch + Hugging-Face MCP. LAN-only backends (comfy_manager, local_llm) deferred to the operator's local cron. `tools/upgrade_research.py` and `_dev_docs/upgrade_research/` are absent from this checkout — noted in Run metadata; digest was assembled from web + HF MCP data only._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | VRAM fit | Why |
|---|---|---|---|---|---|
| 1 | ComfyUI bundle | **ComfyUI 0.23.0** (2026-06-01) | github.com/Comfy-Org | n/a | Baseline is 0.20.3 — 3 minor behind. 0.23 adds multi-threaded model loading (measurable cold-start win on the operator's rig), MediaPipe face detection, PixelDiT, Microsoft Lens support, plus 0.22's MoGe + Stable Audio 3. Cleanest single upgrade of the run. |
| 2 | Flux successor line | **FLUX.2 [klein] 4B/9B** (2026-04, black-forest-labs) + **PuLID-Flux2 v0.6.2** (iFayens) | HF + GitHub | 4B ✅ / 9B ~ / Dev 32B ❌ heavy | Klein is the "runs on consumer hardware ≥13 GB VRAM" Flux successor. 4B fits the 16 GB ceiling cleanly; 9B fits via KV/FP8 quant (see vladmandic/Flux.2-Klein-9B-KV-sdnq-hadamard-uint4, 2026-07-01). Klein 4B GGUF exists (MXKA/FLUX.2-klein-4B-GGUF). PuLID-Flux2 v0.6.2 is the first working PuLID adaptation for Flux.2 — brings the same identity-lock capability the 2026-05-08 batch shipped for Flux.1 to Klein. |
| 3 | Editing / unified gen-edit | **Qwen-Image-2.0** (2026-02-10, 7B) + **Qwen-Image-Edit-2511** | qwenimages.com / HF | ✅ (7B) | Alibaba slimmed 20B → 7B while raising every benchmark. Native 2K output, professional typography, unified generation + editing in one checkpoint. #1 on AI Arena for both text-to-image AND image editing. Edit-2511 tightens character consistency, mitigates drift, integrates LoRA. Perfect fit for 16 GB. |
| 4 | Aesthetic / style-control T2I | **Krea 2 Raw / Turbo** (weights 2026-06-22, 12.9B) | Krea AI / HF | ~ heavy — quantized only | Open-weights foundation model, custom commercial-friendly license. Turbo generates 2K in ~2 s via 8-step distillation. 12.9B is over the 13 GB threshold, so plan on Q4/Q5 GGUF or FP8 for the 16 GB rig. Flag: **heavy**. |
| 5 | Video accel (WAN pipe) | **MagCache** (Ma et al., NeurIPS 2025) | arxiv 2506.09045 / GitHub Zehong-Ma/MagCache | ✅ | 2.14× (slow) / 2.68× (fast) speedup on Wan 2.1 with single-sample calibration and no quality loss. The 2026-05-08 batch already shipped MagCache once — verify it's still wired to whichever WAN version the bundle now targets (see Ecosystem releases below re: WAN 2.6). |

## Ecosystem releases since baseline

- **ComfyUI** — baseline 0.20.3 → current **0.23.0** (2026-06-01). Intermediate: 0.21.1 (2026-05-13 Partner Nodes), 0.22.0 (2026-05-20 MoGe + Stable Audio 3), 0.23.0 (multi-threaded model loading + PixelDiT + Lens).
- **krita-ai-diffusion** — baseline v1.50 → current **v1.52.1** (2026-06-30, hotfix). v1.51.0 added Anima control layers (Scribble/Line Art/Unblur); v1.52.0 (2026-06-28) added **Krea 2** support and extended regional prompting to Anima; v1.52.1 fixes the plugin-can't-connect-to-managed-server bug.
- **LM Studio** — current **0.4.18-1** (2026-06-28). Notable stops: 0.4.16 launched "Locally" iPhone/iPad companion app (no more LM Link waitlist, default context 8k); 0.4.14 stabilised MTP Speculative Decoding + MLX engine v1.8.1; 0.4.12 added Qwen 3.6 support.
- **Models — substantively new since baseline:**
  - **Flux 2 Klein** 4B/9B + FP8 + 9B-KV variants (2026-04, black-forest-labs). Fine-tuning LoRAs under 60 min doable (HF blog).
  - **WAN 2.6** (2025-12) is the current line — WAN 2.3/2.4 don't exist; series jumped 2.2 → 2.5 → 2.6. Multi-shot, audio, source-image support. Baseline still references 2.2 → this is a real bump.
  - **LTX-2.3** (2026-03-05, Lightricks) — 22 B asymm dual-stream diffusion transformer, 4K@50fps synced A+V. **Heavy** — well over the 16 GB rig ceiling. Skip for now unless you plan a run box.
  - **Z-Image** — no direct "Turbo 2" yet. Z-Image-Base (2026-01-27) shipped as the undistilled counterpart to Turbo. Z-Image-Edit and Z-Image-Omni announced as planned; monitor.
  - **Qwen-Image-2.0** (2026-02-10, 7B) — see top-5 #3.
  - **Krea 2 Raw / Turbo** (2026-06-22 weights) — see top-5 #4.

## HF trending shift

_Skipped — first run has no prior digest to diff against. Trending-model snapshot recorded to `_dev_docs/upgrade_research/2026-07-02_hf_snapshot.md` for the next digest to diff against._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **Adopted upstream** — iFayens/ComfyUI-PuLID-Flux2 v0.6.2 works on Klein 4B/9B and Dev. Consumable now. |
| MagCache | Already in the 2026-05-08 shipped batch. Re-verify against WAN 2.6 if you bump the WAN version. |
| HyperSwap 256 | Already in the 2026-05-08 shipped batch. Upstream direction is FaceFusion Labs' hosted API (HyperSwap 1a/1b/1c) — no new open weights this cycle. ReActor issue #143 tracking the 256 support is still open. |
| Differential Diffusion inpaint | Already in the 2026-05-08 shipped batch. Mature — no upstream churn to chase. |

## Backend results

- `local_index`: 0 baseline candidates — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` has `loras: []` (empty). No local LoRAs to score against.
- `huggingface`: 8+ Klein-family repos surfaced (weights + KV + GGUF + LoRAs, all created 2026-07-01/02 window — very active).
- `civitai`: SKIPPED (`tools/upgrade_research.py` missing from checkout — no proxied backend available).
- `comfy_manager`: SKIPPED (LAN-only).
- `local_llm`: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI bundle 0.20.3 → 0.23.0.** The multi-threaded model loader in 0.23 directly helps cold-start on the 16 GB rig — likely the highest single-item ROI this cycle.
2. **Add Flux.2 Klein 4B + PuLID-Flux2 v0.6.2** to the bundle. 4B fits VRAM cleanly, and PuLID-Flux2 brings the identity-lock capability that shipped for Flux.1 to the Klein successor. Plan Klein 9B as an FP8/KV-quant secondary if 4B outputs prove insufficient.
3. **Land `tools/upgrade_research.py`** (or restore from wherever it moved). The next digest can only be authoritative once the script is back — right now the routine is running blind on `local_index` + `civitai` (both backends silent) and the "top-N by score" ranking in section 1 is my synthesis, not the script's.
4. _(Optional)_ Add Qwen-Image-Edit-2511 as a second editing path alongside Klein — 7B, 2K native, unified gen+edit, currently #1 on AI Arena.

## Run metadata

- runtime: ~5m
- WebSearch calls: 12 / ~25 budget
- WebFetch calls: 0
- HF MCP calls: 3
- Gmail MCP: **UNAVAILABLE** — server requires OAuth authorization the non-interactive session can't run. Operator: authorize the Gmail connector in claude.ai connector settings so the next digest can deliver by email.
- Google-Drive MCP: available (`mcp__Google-Drive__*` tools loaded); backup copy attempted post-commit.
- `tools/upgrade_research.py`: **MISSING** from this checkout — noted above. Digest assembled from web + HF only.
- `_dev_docs/ecosystem_digest_*.md`: this is the first — no continuity diff performed.
- `comfyui-spellcaster` build_* count: **77** (baseline 73 at 2026-05-13 → +4 since baseline; not "massively different", noted for continuity).
- next scheduled run: 2026-07-04 (48 h out).
