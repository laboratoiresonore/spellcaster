# Ecosystem digest — 2026-06-28

_Generated by the every-48h cloud routine. Runtime ~3m so far; final stamp in metadata. Backends reached: Hugging Face MCP + WebSearch. **`tools/upgrade_research.py` is absent from the repo** (no `_dev_docs/upgrade_research/<iso-week>.json` produced this run — see Run metadata). LAN-only backends (`comfy_manager`, `local_llm`) deferred to operator's local cron._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / signal | Why |
|---|---|---|---|---|---|
| 1 | Flux 2 Klein controlnet refs (pose / depth / canny) | `thedeoxen/refcontrol-FLUX.2-klein-9B-reference-{pose,depth,canny}-lora` | HF | 668 / 5.2K / 606, all created May–Jun 2026 | Closes the controlnet gap on Klein 9B — currently no first-party pose/depth/canny shipping with `black-forest-labs/FLUX.2-klein-9B`. Apache-2.0. Three siblings drop together → a real triplet, not a one-off. |
| 2 | Wan 2.2 i2v steering | `yrlyrl/wan2.2-i2v-a14b-physalign-lora` | HF | created 23 Jun 2026 | Physics-alignment LoRA on Wan2.2-I2V-A14B — addresses the "objects float/drift" failure mode operator has flagged before. Apache-2.0, low download count but very fresh. |
| 3 | PuLID identity for Flux 2 | `iFayens/ComfyUI-PuLID-Flux2` v0.6.2 | GitHub | First working PuLID port to Flux.2 | Direct successor to the PuLID-Flux2 watchlist item — Klein 4B is best-results variant per author. InsightFace + EVA-CLIP pipeline, supports Klein 4B/9B and Dev 32B. Klein 4B fits the 16 GB ceiling cleanly. |
| 4 | MagCache for Wan 2.2 | `Zehong-Ma/ComfyUI-MagCache` (post-NeurIPS 2025) | GitHub / arXiv | 1.5–2× speedup on Wan2.2, 2× on Flux-Kontext | Watchlist item now extended past Wan 2.1 — concrete plug-in speedup that compounds with existing Wan2.2 calibrations. No retrain needed; cache-only path. |
| 5 | Qwen-Image-Edit successor | `Qwen-Image-Edit-2511` / `Qwen-Image-2.0` (7B) | Qwen blog | Released Feb 2026; #1 on AI Arena for both t2i and image-edit | 7B replaces the older 20B MMDiT — fits 16 GB comfortably. Edit-2511 lifts character consistency and bakes popular LoRAs in. Strong unified gen+edit story. |

(Pulled by cross-referencing HF MCP `hub_repo_search` results — sorted by trending score and lastModified — with web-confirmed releases. Score weighting favoured: VRAM fit ≤ 13 GB, Apache/MIT license, novel capability rather than weights iteration.)

## Ecosystem releases since last digest

_(No prior digest in repo — this is the first run; using documented operator baselines instead.)_

- **ComfyUI**: bundle baseline **0.20.3** → upstream now **0.23.0** (1 Jun 2026). Intermediate 0.21.1 (13 May) added Partner Nodes + LLM nodes; 0.22.0 (20 May) added MoGe + Stable Audio 3; 0.23.0 added multi-threaded model loading, MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens. **Three minor versions behind — bump candidate.**
- **krita-ai-diffusion** (Acly): baseline **v1.50** → **v1.51.1** (Jun 2026). v1.51.0 (31 May 2026) cleans up the inpaint flow (requires inpaint control model for full-strength fill/expand), adds AMD managed-ROCm installer. v1.51.1 fixed macOS / Linux managed-install upgrade path. Low-risk bump.
- **LM Studio**: current upstream **0.4.18** (26 Jun 2026). 0.4.15 (29 May) shipped tensor parallelism for multi-GPU loading; 0.4.14 stabilised MTP speculative decoding; 0.4.16 added Locally for iPhone/iPad + LM Link. Cadence is roughly weekly. Spellcaster is decoupled from LM Studio LAN-side, so cosmetic for the bundle.
- **Models** (substantive only):
  - **Flux 2 Klein**: ecosystem matured rapidly since Jan 2026 launch. Beyond the base 4B/9B/9b-fp8/9b-kv first-party variants, controlnet (pose/depth/canny refcontrol LoRAs from thedeoxen, May–Jun 2026) and tuned merges (wikeeyang/Flux2-Klein-9B-True-V3, 25 Jun 2026) are landing. Klein 9B is heavy at edge of 16 GB ceiling — **flag as heavy**; Klein 4B is the safe pick.
  - **WAN**: 2.2 still the open-source baseline; **WAN 2.6** (Dec 2025) and **WAN 2.7** (March 2026 launch window per WaveSpeed) are upstream/closed for now, not yet open weights. Wan 2.2 LoRA churn remains very high on HF (DaSiWa, Lightning merges, motion enhancers).
  - **LTX**: **LTX-2.3** (Mar 2026, 22B, Apache-2.0, 4K @ 50 fps up to 20 s with synchronized audio). Significant jump from LTX-Video 2.0-era — but 22B parameters is well above the 16 GB ceiling for video work. **Heavy / exclusive-load.**
  - **Z-Image**: no successor announced; **Z-Image-Base** (foundation, 6B) still "coming soon" since launch. Z-Image-Turbo remains the Spellcaster-relevant variant.
  - **Qwen-Image-Edit**: **Qwen-Image-Edit-2511** (improved character consistency, group-photo consistency, popular LoRAs baked in) + parent **Qwen-Image-2.0** (7B unified gen/edit, Feb 2026) — both substantively new vs. the older 20B Edit.

## HF trending shift

_(Skipping diff — first run, no prior digest to compare against. Next run will populate this section.)_

Snapshot of Flux 2 Klein–adjacent trending (descending trendingScore, last 30d window via HF MCP):

- `black-forest-labs/FLUX.2-klein-9B` (TS 39, 170.7K dl, ↑)
- `ponpoke/flux2-klein-9b-uncensored-text-encoder` (TS 20) — **NSFW-adjacent; not surfaced as candidate per H6**
- `thedeoxen/refcontrol-FLUX.2-klein-9B-reference-pose-lora` (TS 18, fresh 22 Jun)
- `thedeoxen/refcontrol-FLUX.2-klein-9B-reference-depth-lora` (TS 15)
- `wikeeyang/Flux2-Klein-9B-True-V3` (TS 14, fresh 25 Jun)
- `unsloth/FLUX.2-klein-9B-GGUF` (TS 13, 104.1K dl)
- `nomadoor/flux-2-klein-9B-schematic-lora` (TS 11) — multi-condition (depth + normal + pose + segmentation) bundled
- `black-forest-labs/FLUX.2-klein-4B` (TS 10, 490.6K dl) — the recommended 16 GB-fit variant
- `thedeoxen/refcontrol-FLUX.2-klein-9B-reference-canny-lora` (TS 9, fresh 26 Jun)

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **News.** `iFayens/ComfyUI-PuLID-Flux2` is the first working PuLID adaptation for Flux.2 (v0.6.2). Supports Klein 4B (best results), Klein 9B, Dev 32B. Klein 4B is the 16 GB-fit pick. Already overlaps with the 2026-05-08 PuLID-Flux2 baseline upgrade — verify whether this is the same port or a newer fork. |
| MagCache | **News.** Now supports Wan 2.2 (1.5–2× speedup) and Flux-Kontext (2× speedup). Repo: `Zehong-Ma/ComfyUI-MagCache`. Already shipped in the 2026-05-08 batch for older targets — extend coverage to Wan 2.2 if not already done. |
| HyperSwap | **Steady-state.** No new model family this window. Existing `hyperswap_1a/1b/1c_256.onnx` remain canonical via ComfyUI-ReActor and FaceFusion ComfyUI ports. Already shipped 2026-05-08 — no action. |
| Differential Diffusion inpaint | **Steady-state.** Technique stable; ongoing tutorial volume (apatero, prompting-pixels). Already shipped 2026-05-08 — no action; krita-ai-diffusion 1.51 strengthens the related inpaint-control-model path on the Krita side. |

## Backend results

- `local_index`: 0 baseline candidates (`lora_calibrations_sfw.json` has empty `loras` dict — community baseline is bare)
- `huggingface` (MCP): 4 queries, 47 candidate results across Flux 2 Klein / text-to-image / Wan2.2-i2v / identity papers
- `civitai`: SKIPPED (no `tools/upgrade_research.py` to invoke; manual Civitai sweep deferred to keep within budget)
- `comfy_manager`: SKIPPED (LAN-only)
- `local_llm`: SKIPPED (LAN-only)
- **`upgrade_research.py`: NOT PRESENT in repo.** All ranking in §Headline derived from manual cross-referencing of HF MCP + WebSearch results — see Run metadata.

## Action items for operator

1. **Bump ComfyUI bundle 0.20.3 → 0.23.0** (three minor versions, ~7 weeks of releases). Watch for the Partner Nodes addition in 0.21.1 — may interact with the bundled node set; the multi-threaded model loading in 0.23.0 is a free latency win on 16 GB rigs.
2. **Bump krita-ai-diffusion v1.50 → v1.51.1.** Low-risk; the inpaint-control-model requirement at full strength may need a calibrations note on the operator's side.
3. **Adopt the Flux 2 Klein refcontrol LoRA triplet** (pose, depth, canny — all Apache-2.0 from `thedeoxen`) — closes the controlnet gap on the Klein 9B/4B path. Wire into the existing build_* functions that already target Klein.
4. **Confirm MagCache Wan 2.2 path is wired.** The May 2026 batch shipped MagCache; verify the Wan 2.2 1.5–2× speedup is on the active codepath, not just Wan 2.1.
5. **Evaluate Qwen-Image-Edit-2511** as a successor to whichever Qwen-Image-Edit version the bundle currently calibrates against — 7B fits the 16 GB ceiling comfortably and reportedly leads AI Arena.

## Run metadata

- runtime: ~4–5 min total (final stamp on delivery)
- WebSearch calls: 12 — `ComfyUI 0.21/0.22/0.23`, `krita-ai-diffusion 1.51/1.52`, `LM Studio 2026`, `Flux 2 Klein HF`, `WAN 2.3 i2v`, `LTX-Video 2.4`, `Z-Image Turbo 2`, `Qwen Image Edit 2026`, `PuLID Flux 2`, `MagCache WAN`, `HyperSwap ComfyUI`, `Differential Diffusion inpaint`
- HF MCP calls: 4 — `hub_repo_search(flux-2-klein)`, `hub_repo_search(text-to-image, trending)`, `hub_repo_search(wan-2.2-i2v, lastModified)`, `paper_search(flux klein face identity)`
- WebFetch calls: 0 (search snippets carried sufficient publish-date + version info; preserved budget)
- Total calls: 16 of ~25 budget
- baseline check: `build_*` count = **77** in cloud clone (operator baseline 2026-05-13 was 73 — 4 more functions landed since)
- **Known gap**: `tools/upgrade_research.py` not present in `laboratoiresonore/spellcaster` — the routine spec references it but the tool was never committed. Either bring it in-repo or remove the reference from the routine prompt. This run synthesized rankings manually instead.
- Calibrations file: `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` has an empty `loras` dict — `local_index` backend would have returned zero candidates anyway.
- next scheduled run: 2026-06-30 ~16:00 UTC
