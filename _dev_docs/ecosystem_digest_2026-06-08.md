# Ecosystem digest — 2026-06-08

_Generated by the every-48h cloud routine. Runtime: ~8m. Backends reached: WebSearch + Hugging-Face MCP. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` absent from cloud checkout — fell back to WebSearch + HF MCP only (see Run metadata)._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Likes | Why |
|---|---|---|---|---|---|
| 1 | `build_klein_*` (Flux.2 Klein family) | `black-forest-labs/FLUX.2-klein-4B` | hf | 404.6K / 713 | 4B params, ~13 GB VRAM — fits 16 GB ceiling cleanly (the 9B at ~29 GB is exclusive-load **heavy**). Apache 2.0. Strongest dl trend in the Klein cluster. |
| 2 | Klein depth/pose/seg control | `nomadoor/flux-2-klein-9B-schematic-lora` | hf | 0 / 35 (7-day-old) | Emerged 2026-06-01 — single LoRA covers depth + normal-map + pose + segmentation conditioning. Cross-fits any spellcaster ControlNet-style methods. |
| 3 | `build_qwen_edit_*` | `Qwen/Qwen-Image-Edit-2511` + `lightx2v/…Lightning` distill | hf | 226K + 332K / 1037 + 456 | Improved character consistency in 2511; Lightning distill cuts steps for the latency-sensitive paths. |
| 4 | Unified gen+edit | `Qwen/Qwen-Image-2.0` (7B) | web | DPG-Bench 88.32 (>FLUX.1) | 7B unified gen+edit, 2K native, 1K-token prompt context. Replaces a separate gen-then-edit pipeline if a `build_qwen_unified_*` method is added. |
| 5 | Klein character swap / occlusion | `nhathoangfoto/Flux.2-Klein-9B-SmartCharacterSwap` | hf | 1.3K / 14 (7-day-old) | Emerged 2026-06-01 — face-swap LoRA on Klein-9B with explicit occlusion handling; complements the existing ReActor / HyperSwap path. |

## Ecosystem releases since last digest

- **ComfyUI**: **v0.23.0** is current (vs **0.20.3** local baseline → 3 minor releases behind). Adds frontend 1.44.19, flash-attention fixes, Microsoft Lens support (CORE-248), Logic nodes (And/Or/Not), Rodin2.5 partner nodes, Preview3DAdvanced node, threaded-loader / memory perf. Earlier 0.18.0 (2026-03-21) shipped `--fp16-intermediates` + VRAM cuts for LTX and WAN VAE.
- **krita-ai-diffusion (Acly)**: **v1.51.0** then **v1.51.1** released (vs **v1.50** baseline). 1.51.1 is the macOS/Linux managed-install fix.
- **LM Studio**: **0.4.16** dropped **2026-06-04** (4 days ago) — adds **Locally** mobile app + **LM Link** Tailscale-mesh remote-access. 0.4.14 (May 29) shipped stable MTP Speculative Decoding; 0.4.13 (May 22) had MLX engine v1.8.1 parallel predictions; 0.4.12 (May 13) added Qwen 3.6 + Qwen 3.5 perf.
- **Models** — substantively new since baseline:
  - **Flux.2 Klein**: family launched **2026-01-14** (4B / 9B / 9B-KV / FP8 / base variants). 9B-KV (Mar 9) accelerates multi-reference editing via KV-cache.
  - **WAN**: no new official drop since **WAN 2.6** (Dec 2025). **WAN 2.7** was planned for March 2026 but does not appear shipped; treat as still upcoming. No WAN 2.3 / 2.4 — versioning skipped.
  - **LTX-Video**: **LTX-2.3** released **March 2026** — 22B DiT, 4K @ 50fps. **Heavy** (well past 13 GB) — flag as exclusive-load only.
  - **Z-Image**: **Z-Image Base** (undistilled) released **2026-01-27**. **Z-Image Edit** + **Z-Image Omni** announced, not yet GA. No Turbo 2 successor announced.
  - **Qwen-Image-2.0**: released **2026-02-10**. 7B unified gen+edit replaces the 20B split; #1 on AI Arena for both T2I + edit.

## HF trending shift

- **New / recently emerged in Klein cluster (last 30d)**:
  - `nomadoor/flux-2-klein-9B-schematic-lora` — depth/normal/pose/seg LoRA (created 2026-06-01)
  - `nhathoangfoto/Flux.2-Klein-9B-SmartCharacterSwap` — face-swap + occlusion (created 2026-06-01)
  - `ponpoke/flux2-klein-9b-uncensored-text-encoder` — **NSFW-adjacent, filtered out per H6**
- **Steady-state heavy hitters**:
  - `black-forest-labs/FLUX.2-klein-4B` (404.6K dl), `FLUX.2-klein-9B` (125.7K), `unsloth/FLUX.2-klein-9B-GGUF` (104.3K)
  - `Qwen/Qwen-Image-Edit-2511` (226K) + `lightx2v/Qwen-Image-Edit-2511-Lightning` (332K)
- **Dropped from top 15 since last digest**: _N/A — first run, no prior digest to diff against._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **Active** — `iFayens/ComfyUI-PuLID-Flux2` is the first PuLID impl supporting FLUX.2 Klein (4B + 9B) + Dev; uses InsightFace + EVA-CLIP; June 2026 commits. `Fayens/Pulid-Flux2` weights on HF. Already adopted in the 2026-05-08 upgrade pass per memory — verify your local node tracks `iFayens` (not the older `sipie800` / `PaoloC68` forks). |
| MagCache | **Active** — NeurIPS 2025 paper, 2.68–3.0× on Wan 2.1, **1.5–2× on Wan 2.2** since 2025-08-07. No 2026 refresh in webresults; treat as steady. Already adopted. |
| HyperSwap | **Active** — FaceFusion 3.6.0 + HyperSwap variants 1A (speed) / 1B (balanced) / 1C (max realism). Spellcaster already on HyperSwap 256 — evaluate 1C for the high-realism path. |
| Differential Diffusion inpaint | **Stable** — no substantive 2026 news; documentation + workflows mature. Already shipped. |

## Backend results

- local_index: 0 baseline candidates (`lora_calibrations_sfw.json` `loras` map is empty in current checkout)
- huggingface: ~30 candidate repos surfaced across Klein, WAN 2.2 i2v, Qwen-Image-Edit queries
- civitai: SKIPPED (no `tools/upgrade_research.py` in cloud checkout — could not run civitai backend)
- comfy_manager: SKIPPED (LAN-only)
- local_llm: SKIPPED (LAN-only)

## Action items for operator

1. **ComfyUI bundle bump** — 0.20.3 → 0.23.0 (or pin a known-good 0.22 if 0.23 ships breaking node API changes). Earlier 0.18.0 brought WAN/LTX VAE VRAM cuts the 16 GB ceiling will benefit from.
2. **krita-ai-diffusion bump** — Acly v1.50 → v1.51.1 (managed-install fix lands here).
3. **Evaluate FLUX.2-klein-4B as primary Klein workhorse** — current `build_klein_multi_angle` and `build_klein` likely target 9B; the 4B at ~13 GB lets Klein co-load with text encoders / LoRAs without exclusive-load swaps. Worth a `build_klein_4b_*` parallel for VRAM-constrained flows.
4. **Watch nomadoor/flux-2-klein-9B-schematic-lora** — a single LoRA covering depth + normal + pose + segmentation could simplify any ControlNet-style structured-conditioning method.
5. **Antenna / LM Studio connector** — the LM Studio 0.4.16 Tailscale-mesh remote-access feature may need a probe in `antenna/` to ensure the existing connector still resolves the local instance (HTTP API surface should be stable, but worth a smoke test on next local session).

## Run metadata

- runtime: ~8m
- WebSearch calls: 12
- WebFetch calls: 0 (HF MCP returned enough structured detail to skip page fetches)
- HF MCP calls: 6 (`hub_repo_search` ×5, `paper_search` ×1)
- build_* count this run: **75** (vs 73 at 2026-05-13 baseline — within drift)
- recent commits: `feat(antenna): LM Studio connector + service registry`, `fix(krita): multi-image outputs + Acly-style layer placement`, `feat(klein): native build_klein_multi_angle (CivitAI 2642426 port)`
- **Notable failure**: `tools/upgrade_research.py` is **not present** in the cloud checkout. The cloud routine spec assumes it exists with `local_index`, `huggingface`, and `civitai` backends. Operator should either (a) commit `tools/upgrade_research.py` to the repo so future cloud runs can score candidates programmatically, or (b) adjust the routine spec to web-only. Today's digest used WebSearch + HF MCP only.
- next scheduled run: **2026-06-10 16:09 UTC**
