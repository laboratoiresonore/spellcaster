# Ecosystem digest — 2026-06-18

_Generated by the every-48h cloud routine. Runtime: ~8 minutes. Backends reached: HF MCP + WebSearch + WebFetch + local repo introspection. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. The bundled `tools/upgrade_research.py` is not present in this clone, so backend candidate counts below come from manual HF-MCP queries and direct workflow-source diffs rather than from the scripted pipeline._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | `build_qwen_edit` (currently Qwen-Image-Edit-2509) | **Qwen-Image-Edit-2511** | hf | 994.8K dl / 1051 likes | Direct drop-in: drift mitigation, character consistency, integrated-LoRA, geometric reasoning. Released 2025-12-23. UNETLoader filename swap only. |
| 2 | `build_wan_video` / `build_wan22_t2v` / `build_wan_flf` / `build_wan_animate_video` (WAN 2.2) | **WAN 2.6** (stable since Dec 2025) | web | n/a | Four minor versions behind. WAN 2.6 reconstructs 3-D space from a single still and simulates camera, up to 15 s output. WAN 2.7 lands in March 2026 (9-grid I2V + instruction edit) — wait or jump? |
| 3 | `build_ltx_video` (LTX baseline) | **LTX-Video 2.3** | web | n/a | Lightricks shipped 2026-03-05: 22 B params, native 4 K @ 50 FPS, single-pass synchronized audio. Likely heavy (>13 GB VRAM) — flag exclusive-load. |
| 4 | (new arch, no existing builder) | **Z-Image-Base** (Tongyi-MAI) | hf+web | strong community pickup | Non-distilled raw checkpoint released 2026-01-28, sibling to the existing Z-Image-Turbo. Opens a `build_zimage_base_t2i` slot for 30–50-step max-quality runs while keeping Turbo for fast iter. |
| 5 | ComfyUI runtime (bundle baseline 0.20.3) | **ComfyUI 0.21 / 0.22 / 0.23** | github | active | v0.18+ already adds `--fp16-intermediates` (big VRAM reduction for LTX & WAN VAEs), v0.23 ships frontend 1.44.19 + flash-attn fixes. Three minor versions of headroom matter for the 16 GB ceiling. |

(`build_*` count in this clone: 77, up from baseline 73. Repo has drifted slightly — see § Backend results.)

## Ecosystem releases since last digest

_First run — no prior digest to diff. Treat this section as "current state vs the operator's stored baseline."_

- **ComfyUI**: latest v0.23.0 (frontend 1.44.19, flash-attn fix, Microsoft Lens). v0.18.0 (2026-03-21) introduced `--fp16-intermediates` and slashed VRAM for LTX/WAN VAEs. — [releases](https://github.com/comfyanonymous/ComfyUI/releases)
- **krita-ai-diffusion**: latest v1.51.1 (hotfix for managed install on macOS/Linux). v1.51.0 adds Anima v1.0 control layers (Scribble / Line Art / Unblur). — [releases](https://github.com/Acly/krita-ai-diffusion/releases)
- **LM Studio**: latest v0.4.16 (2026-06-08) — LM Link no longer requires waitlisting, default context bumped to 8 k, and the Locally iPhone/iPad app ships. v0.4.15 (2026-06-04) added CUDA tensor parallelism. v0.4.13 (2026-05-13) shipped MLX v1.8.1 with parallel predictions on Qwen 3.5/3.6 + Gemma 4. — [changelog](https://lmstudio.ai/changelog)
- **Models**:
  - **Flux 2 Klein**: 4B (~13 GB VRAM, 447.7 K downloads) and 9B (~29 GB, exclusive-load, 120.6 K) released 2026-01-14 by BFL. Already adopted in spellcaster (13 `klein_*` methods). 4B is the right fit for the 16 GB ceiling; 9B is **heavy** — exclusive-load gated.
  - **WAN 2.6**: Alibaba, released Dec 2025. WAN 2.7 promised "within March 2026"; verify before adopting.
  - **LTX-Video 2.3**: 2026-03-05, 22 B params, 4 K + audio.
  - **Z-Image-Base**: 2026-01-28 (non-distilled sibling of Z-Image-Turbo). Z-Image-Omni in testing, Z-Image-Edit unreleased.
  - **Qwen-Image-Edit-2511**: 2025-12-23 (operator's `build_qwen_edit` is still on `qwen-image-edit_2509.safetensors`). A `-2512` was rumoured in search snippets but not found in the HF hub on direct lookup — treat as unconfirmed for now.

## HF trending shift

_Skipped — no prior digest to diff against. The "text-to-image" trending query returns a lot of pre-2024 lexical matches, not real trend signal; on the next run, switch to `paper_search` / direct `flux2-klein` / `wan-2.6-i2v` queries for sharper deltas._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **Already adopted.** `build_pulid_flux` auto-detects Flux1 vs Flux2 and dispatches to `apply_pulid_flux2`. Companion repo `iFayens/ComfyUI-PuLID-Flux2` at v0.6.2 supports Klein 4B/9B + Dev. |
| MagCache | **Already adopted** (per 2026-05-08 upgrade-pass baseline). Upstream now supports Qwen-Image (×1.75) and HunyuanVideo-1.5 (×1.7) since 2025-11 — possible add-on for `build_qwen_edit` + `build_hunyuan_video`. |
| HyperSwap 256 | **Already adopted** (per baseline). No new release surfaced this run. |
| Differential Diffusion inpaint | **Already adopted** (per baseline). No new upstream activity this run. |

## Backend results

- `local_index`: 0 baseline candidates — `lora_calibrations_sfw.json` has `loras: {}` in this clone (community baseline empty).
- `huggingface` (manual MCP probe, not scripted): ~8 strong proposals concentrated in Flux 2 Klein quantizations + Qwen-Image-Edit-2511.
- `civitai`: not probed this run (script-only backend, script absent from clone).
- `comfy_manager`: SKIPPED (LAN-only).
- `local_llm`: SKIPPED (LAN-only).
- **Note**: `tools/upgrade_research.py` is **not present** in this clone (`tools/` contains spellmaker, sync_surfaces, llm_morning_briefing, etc., but no upgrade-research entry point). On the operator's local checkout this may exist; the next-run hardening item is to either commit the script or update this routine's expectations.

## Action items for operator

1. **Bump `build_qwen_edit` from `qwen-image-edit_2509.safetensors` to `qwen-image-edit_2511.safetensors`** — single filename change at `workflows.py:9877`, no node-graph changes per the 2511 release notes. Drift-mitigation + integrated LoRA path is a free win.
2. **Decide on WAN 2.6 vs wait-for-2.7.** WAN 2.2 → 2.6 is a four-minor-version jump and the workflows would need an audit (especially the canonical builder at `workflows.py:4591`). Recommend probe-then-defer: spin a feature branch wiring WAN 2.6 on `build_wan_video` only, but hold the others until WAN 2.7's 9-grid I2V drops in March-ish.
3. **Bundle bump: ComfyUI 0.20.3 → 0.23.x.** The FP16-intermediates flag and the VAE VRAM cuts in 0.18+ directly help the 16 GB ceiling. Pair with a re-run of the test suite (currently 156/156 on baseline; this clone has 77 builders, up from 73, so tests may need topping-up). Low risk because spellcaster talks to ComfyUI via the HTTP API, not Python imports.

## Run metadata

- runtime: ~8 minutes
- WebSearch calls: 6 · WebFetch calls: 2 (both 403 on huggingface.co, recovered via HF MCP) · HF MCP calls: 3 · Total external: 11 (well under the 25-call cap)
- next scheduled run: 2026-06-20 ~16:00 UTC
- caveats: `tools/upgrade_research.py` missing from this clone; civitai backend not exercised; trending HF query returned lexical noise — switch to architecture-specific HF queries on the next run.
