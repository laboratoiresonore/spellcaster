# Ecosystem digest — 2026-07-22

_Generated by the every-48h cloud routine. Backends reached: HuggingFace MCP + WebSearch/WebFetch. `tools/upgrade_research.py` was **absent** from the cloud checkout — no `local_index` / `huggingface` / `civitai` backend runs were possible from here (see Run metadata). This is the **first** ecosystem digest committed to the repo, so no prior-digest diff._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Signal | Why |
|---|---|---|---|---|---|
| 1 | Krita-AI-Diffusion (v1.50 baseline) | **krita-ai-diffusion v1.52.1** (Acly) | GitHub release | latest stable | Two minor bumps land Krea 2 support + regions + macOS/Linux managed-install fixes. Low-risk plugin bump. |
| 2 | ComfyUI (v0.20.3 baseline) | **ComfyUI v0.23.0** (Comfy-Org) | GitHub release | released 2026-06-01 | 3 releases past baseline; v0.23 ships multi-threaded model loading (measurable throughput win) + native MoGe / PixelDiT / MediaPipe nodes. Bumping the bundled Comfy is now overdue. |
| 3 | Flux family (text-to-image) | **FLUX.2-klein-4B** (black-forest-labs) | HF `black-forest-labs/FLUX.2-klein-4B` | 4B — fits ~13 GB VRAM | Only Klein variant that fits the 16 GB ceiling with headroom. A V2 checkpoint dropped 2026-04-17 fixing a longstanding color-cast bug. Direct successor to FLUX.1-dev and already has a PuLID-Flux2 adapter (see cluster). |
| 4 | Image-editing / instruction-edit | **Qwen-Image-2.0 + Qwen-Image-Edit-2511** (Alibaba/Qwen) | HF `QwenLM/Qwen-Image` | #1 AI Arena in both T2I and edit categories | Native 2K, 7 B params (down from 20 B), unified gen+edit, sharply improved identity consistency on the 2511 edit checkpoint. Reduced VRAM footprint makes it viable at 16 GB. |
| 5 | Video (image-to-video path) | **WAN 2.6 R2V** (Alibaba Tongyi) | ComfyUI blog + HF | 12 GB VRAM viable at 576–720p; 4090 comfortable at 1080p | WAN skipped from 2.2 → 2.6 → 2.7; 2.6 R2V is now natively in ComfyUI. Motion/style transfer from reference clips is the headline feature — replaces the WAN 2.2 references in the baseline. Flag as **heavy** for max-resolution use. |

Notes: rank prioritises _fits-in-16GB-VRAM_ and _integration-effort_ over raw novelty. Anything > 13 GB recommended VRAM is flagged **heavy**.

## Ecosystem releases since last digest

- **ComfyUI**: v0.21.0 (2026-05-13), v0.21.1, v0.22.0 (2026-05-20, MoGe + Stable Audio 3), **v0.23.0 (2026-06-01, multi-threaded model loading, PixelDiT, MediaPipe face detection)**. Baseline was v0.20.3 → 3 minor releases behind.
- **krita-ai-diffusion (Acly)**: v1.51.0 → v1.51.1 → v1.52.0 → **v1.52.1**. v1.52.x adds Krea 2 basic support, regions, and fixes managed-install regressions on macOS/Linux. Baseline was v1.50 → 2 minor releases behind.
- **LM Studio**: v0.4.12 (Apr 17, Qwen 3.6) → v0.4.14 (May 29, **MTP speculative decoding**) → v0.4.18 (Jul 7) → v0.4.19 (LM Studio Engine Protocol stable) → **v0.4.20 (2026-07-22, today — enterprise internal network endpoints + LM Link Bionic support)**. Also new: **LM Studio Bionic** (2026-07-16) — dedicated open-model agent app with Code/Work projects, Voxtral voice, Tailscale LM Link, Secure Cloud ZDR.
- **Models**:
  - **Flux 2 Klein**: 4B (fits ~13 GB, RTX 3090/4070+) and 9B (~29 GB, **heavy** — exclusive-load only, exceeds 16 GB ceiling). V2 (2026-04-17) fixed color cast. Uncensored / abliterated 9B text-encoder now on HF (`ponpoke/flux2-klein-9b-uncensored-text-encoder`, 27 K downloads).
  - **WAN**: 2.6 (Dec 2025, T2V+I2V+R2V) → 2.7 (April 2026, first/last-frame control + 9-grid image input). ComfyUI has native 2.6 R2V support as of June/July 2026.
  - **LTX-Video**: **2.3 (2026-03-08)** — 22 B params, native 4K@50fps, up to 20 s clips, synchronized audio, **Apache 2.0**. No 2.4 or 3.0 yet.
  - **Z-Image**: Base (2026-01-27, 24 GB VRAM — **heavy**). Z-Image-Edit in early access. Z-Image-Omni in test. Z-Image-Turbo (6 B) unchanged and still the top HF trending T2I at 1.1 M downloads.
  - **Qwen-Image-2.0** (2026-02-10, 7 B, native 2K, unified gen+edit) + **Qwen-Image-Edit-2511** (much improved character consistency, multi-person group-photo fusion).
  - **Krea 2** (Krea-2-Raw, Krea-2-Turbo — Jun 2026) — 212 K + 164 K downloads on HF, integrated into krita-ai-diffusion 1.52. Also spawning a healthy LoRA ecosystem (mgwr/M87 at 5 K downloads, DeverStyle/Krea-2-Premium-Loras, AliveAi/Krea-2-Edit-Outfit-Transfer).
  - **microsoft/Mage-Flow** (2026-07-21, brand new) — rectified-flow arch, three variants (base, standard, turbo), MIT license. **Trending #1** on HF text-to-image today; too new to grade, watch next cycle.

## HF trending shift

_First digest — no prior top-15 to diff against. Establishing today's snapshot as the reference baseline._

Top 15 trending T2I on HuggingFace (2026-07-22):
1. microsoft/Mage-Flow — trend 84 (new 2026-07-21)
2. krea/Krea-2-Turbo — trend 65, 212.5 K downloads
3. mgwr/M87 — trend 52 (Krea-2-Turbo LoRA)
4. sensenova/SenseNova-U1-8B-MoT-Infographic-V3 — trend 44 (any-to-any multimodal)
5. black-forest-labs/FLUX.1-dev — trend 43, 595.0 K downloads (steady incumbent)
6. ponpoke/flux2-klein-9b-uncensored-text-encoder — trend 33, 27.2 K downloads
7. Tongyi-MAI/Z-Image-Turbo — trend 28, 1.1 M downloads
8. krea/Krea-2-Raw — trend 24, 164.5 K downloads
9. black-forest-labs/FLUX.1-schnell — trend 23, 244.7 K downloads
10. wikeeyang/Krea2-Turbo-HD-V1 — trend 22, 23.5 K downloads
11. DeverStyle/Krea-2-Premium-Loras — trend 19
12. stabilityai/stable-diffusion-xl-base-1.0 — trend 18, 1.4 M downloads (long-tail)
13. microsoft/Mage-Flow-Base — trend 16 (new)
14. microsoft/Mage-Flow-Turbo — trend 16 (new)
15. AliveAi/Krea-2-Edit-Outfit-Transfer — trend 15

Reading the tea leaves: **Krea 2 has crowded the top of the leaderboard** (5 of top 15 are Krea-2-family). **Microsoft Mage-Flow** is the wildcard — a Microsoft-published rectified-flow model landing at #1 within 24 h is unusual; worth revisiting next digest to see if it holds.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **Active.** `iFayens/ComfyUI-PuLID-Flux2` published on GitHub with HF weights at `Fayens/Pulid-Flux2`. Supports Klein 4B (best balance), 9B (heavy), and Dev. Already shipped in the 2026-05-08 upgrade pass — cluster remains healthy. |
| MagCache | **Steady.** 2.68 × speedup on WAN 2.1 with single-sample calibration. Supports t2v 1.3 B / 14 B, i2v 480p / 720p, VACE. No newer variant this cycle. Already shipped. |
| HyperSwap | **Steady.** FaceFusion 3.6.0 stable; HyperSwap 1a/1b/1c variants documented via Segmind. ComfyUI-ReActor still has open issue #143 requesting the 256 model — no merge yet. Already shipped via the 256 endpoint. |
| Differential Diffusion inpaint | **Steady.** Mature integration; no new node/method this cycle. Continues to be the recommended soft-mask inpaint path with FLUX. Already shipped. |

## Backend results

- **local_index**: SKIPPED — `tools/upgrade_research.py` not present in cloud checkout (see Run metadata).
- **huggingface**: covered via Hugging-Face MCP `hub_repo_search` — ~40 candidates surfaced across 5 queries.
- **civitai**: SKIPPED — `tools/upgrade_research.py` absent.
- **comfy_manager**: SKIPPED (LAN-only, expected).
- **local_llm**: SKIPPED (LAN-only, expected).

## Action items for operator

1. **Bump ComfyUI to v0.23.0** — 3 minor releases past baseline; multi-threaded model loading is a straightforward wall-clock win, PixelDiT / MediaPipe / MoGe nodes unlock new workflows. Low-risk.
2. **Bump krita-ai-diffusion to v1.52.1** and pair with Krita 5.3.2.1 — unlocks Krea 2 support, which the HF trending snapshot suggests is where the LoRA ecosystem is heading in Q3 2026. Also fixes the managed-install regression that could have been silently biting your operator.
3. **Evaluate FLUX.2-klein-4B + PuLID-Flux2** as a candidate `build_*` — Klein 4B fits the 16 GB ceiling with headroom, has a working PuLID adapter (`iFayens/ComfyUI-PuLID-Flux2`), and the V2 (2026-04-17) color-cast fix removed the main blocker. Would slot next to the existing FLUX.1-dev methods. Watch out: Klein has non-permissive licensing — verify commercial-use path before promoting past prototype.
4. **First-run housekeeping**: commit `tools/upgrade_research.py` to the public repo (or note it as local-only in `.gitignore` if intentional). Without it, cloud digests are locked out of the `local_index` + `civitai` scoring paths.
5. **Baseline drift**: `build_*` count is **77** in the current checkout vs the expected **73** as of 2026-05-13. +4 builders since baseline — no action needed, just noting it for calibration on future digests.

## Run metadata

- runtime: ~4 m (single-turn cloud routine; no long backend fetches)
- WebSearch / WebFetch call count: 8 WebSearch, 0 WebFetch (results were rich enough; no per-page fetches needed). HF MCP calls: 5 `hub_repo_search`.
- `tools/upgrade_research.py`: **NOT PRESENT** in the cloud checkout. The digest is built entirely from WebSearch + Hugging-Face MCP data. Operator should verify whether the script is local-only or was expected to be committed.
- Gmail delivery: **DEFERRED** — Gmail MCP requires OAuth reauthorization, which is unavailable in a headless cloud routine.
- Google Drive delivery: **DEFERRED** — Google-Drive MCP token expired; requires reauthorization from an interactive session.
- Git push / PR: **primary delivery channel this run** — the digest lands in the repo as `_dev_docs/ecosystem_digest_2026-07-22.md` on branch `digest/2026-07-22` and, if the routine has push perms, in a PR of the same title.
- Operator action required: re-authorize the Gmail and Google-Drive MCP connectors from an interactive claude.ai session so the next 48 h digest can email + Drive-backup automatically.
- Next scheduled run: **2026-07-24 (48 h)**.
