# Ecosystem digest — 2026-07-06

_Generated by the every-48h cloud routine. Runtime: ~5m. Backends reached: HuggingFace MCP + WebSearch/WebFetch + repo-local baseline. LAN-only backends (comfy_manager, local_llm) deferred to the operator's local cron. `tools/upgrade_research.py` NOT PRESENT in this checkout — see Run metadata._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Base T2I stack (broad) | **Krea 2 (RAW + Turbo)** — 12B DiT open-weights, 8-step Turbo at 2K in ~2s | krea.ai + comfy blog + venturebeat | #1 among indie-lab T2I on Artificial Analysis; #6 globally | Genuinely new open-weights frontier model. Turbo variant runs on 16 GB. Krita-AI-Diffusion v1.52 already added support — spellcaster's `build_*` T2I path could plug it in as a new arch alongside Flux 2 / Z-Image. |
| 2 | I2V/T2V (currently WAN 2.2, LTX-Video 0.9.x) | **LTX-Video 2.3** (`Lightricks/LTX-2.3`) — 22B DiT, native audio+video, up to 4K@50fps, 20s clips | HF + Lightricks | Open weights, 4 checkpoint variants (dev/distilled/fast/pro) | Massive generation-jump from LTX-Video 0.9.x (baseline in spellcaster). **Heavy — 22B, exclusive-load on 16 GB.** Distilled variant is 8-step. |
| 3 | Qwen-Image-Edit (existing) | **Qwen-Image-Edit-2511** (Dec 23 2025) + **-2511-Lightning** distill | `Qwen/Qwen-Image-Edit-2511`, `lightx2v/Qwen-Image-Edit-2511-Lightning` | 170K + 359K downloads, 1.1K + 475 likes | In-place upgrade over the -2509 that spellcaster likely references. Adds character/multi-person identity consistency, built-in LoRA fusion, LightX2V distill for ~42× overall speedup. |
| 4 | Flux 2 Klein (default per 129be75) | **FLUX.2-klein-4b-fp8** (Jan 14 2026) + **klein-9b-kv-fp8** (Mar 9 2026) | `black-forest-labs/FLUX.2-klein-4b-fp8` / `-9b-kv-fp8` | 73K / 56K downloads | Official fp8 quants. 4b-fp8 fits 16 GB with comfortable headroom for LoRAs; kv-fp8 opens a path to running 9B on this VRAM ceiling. Direct swap-in for the current Klein 4B default. |
| 5 | PuLID face identity (Flux1) | **PuLID-Flux2** (`Fayens/Pulid-Flux2`, Mar 21 2026) | HF | 15 likes, ComfyUI-tagged | Upgrade-cluster watchlist hit. Extends the PuLID-Flux2 track already shipped in the 2026-05-08 upgrade batch — worth diffing against what's live to confirm this is a distinct release rather than a mirror. |

## Ecosystem releases since last digest

- **ComfyUI**: baseline **0.20.3** → **0.23.0** (Jun 1 2026). Notable jump: 0.21.1 (May 13, partner nodes), 0.22.0 (May 20, MoGe + Stable Audio 3), 0.23.0 (Jun 1, multi-threaded model loading, MediaPipe face-detection, NVIDIA PixelDiT, Microsoft Lens). Ref: <https://github.com/comfy-org/ComfyUI/releases>.
- **krita-ai-diffusion (Acly)**: baseline **v1.50** → **v1.52.1** (Jun 30 2026). v1.52.0 (Jun 28) added Krea 2 support; 1.52.1 fixes managed-server connect regression. v1.51.0 added Anima control layers. Ref: <https://github.com/Acly/krita-ai-diffusion/releases>.
- **LM Studio**: reached **0.4.18** (Jun 26 2026). Highlights since May 13: 0.4.13 (mlx-engine v1.8.1, Qwen 3.5/3.6 vision), 0.4.14 (MTP Speculative Decoding stable), 0.4.16 ("Locally" mobile app for iPhone/iPad, LM Link out of waitlist, 8k default ctx), 0.4.17 (mermaid to PNG), 0.4.18 (llama.cpp option guidance). Ref: <https://lmstudio.ai/changelog>.
- **Models**:
  - **Flux 2 Klein**: fp8 variants shipped Jan/Mar (see row 4 above). BFL applied a March 3 "Speed Upgrade" (2× T2I + editing, zero quality loss). Ref: <https://bfl.ai/blog>.
  - **WAN**: **no WAN 2.3 or 2.4 exists.** Open track is still WAN 2.2. The 2.5/2.6/2.7 numbers seen online are Alibaba's closed/hosted API tier only — spellcaster's WAN 2.2 references remain current for local.
  - **LTX**: LTX-2.3 (see row 2).
  - **Z-Image**: no successor to Z-Image-Turbo yet. `Comfy-Org/z_image_turbo` at 3.3M downloads is now the de-facto turbo baseline.
  - **Qwen-Image-Edit**: -2511 + -2511-Lightning shipped (see row 3).

## HF trending shift

Skipped this run — no prior digest to diff against, and the trending endpoint returned no useful signal for the raw "text-to-image" tag query (needs a more targeted approach; will retry with `trendingScore` sort + `filter=text-to-image` refinement next run).

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | New candidate found: `Fayens/Pulid-Flux2` (Mar 21, 2026) + `Shivammesh26/Pulid-Flux2` (May 16). ComfyUI-tagged. Verify vs. the version shipped in 2026-05-08 upgrade batch. |
| MagCache | Zehong-Ma/MagCache — official WAN 2.2 support since Aug 7 2025 with 1.5-2× speedup; added Qwen-Image (1.75×) and HunyuanVideo-1.5 (1.7×). Already in the 2026-05-08 batch — no new action, but confirm calibration for the WAN 2.2 path. |
| HyperSwap | No new HF or web signal this run. Already-shipped 256 variant appears to remain the current tip. |
| Differential Diffusion inpaint | No new signal this run. |

## Backend results

- **local_index (via repo)**: 0 baseline candidates. `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` has schema_version + notes but `"loras": {}` — the calibrations map is empty, so nothing to compare against. Likely an intended-but-unpopulated file; flagging for the operator.
- **huggingface (via HF MCP)**: ~30 candidate repos surfaced across FLUX.2 klein, WAN 2.2 i2v, Z-Image-Turbo, Qwen-Image-Edit, LTX-Video, PuLID-Flux2.
- **civitai**: NOT REACHED. `tools/upgrade_research.py` does not exist in this checkout, so the civitai backend could not be invoked. See Run metadata.
- **comfy_manager**: SKIPPED (LAN-only, unreachable from cloud sandbox).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **Ship `tools/upgrade_research.py`.** This routine is written to a spec that assumes the tool exists; without it, the digest is text-only synthesis. First priority — even a stub-shell so `--backends huggingface,civitai` works would let subsequent digests carry structured top-50 tables.
2. **Bump ComfyUI bundle to 0.23.0.** Three-release jump since baseline; multi-threaded model loading alone is worth it on a 16 GB rig.
3. **Add Krea 2 (Turbo) as a `build_*` arch.** Krita-AI already added support; Turbo runs in ~2s at 2K on consumer GPUs. Low integration cost, potentially large quality upside for stack.
4. **Swap default Klein 4B → `klein-4b-fp8`** in whichever calibration file gates the load path (`129be75` set 4B as default). fp8 is the intended production quant for 16 GB.
5. **Populate `lora_calibrations_sfw.json`** or delete the empty schema — an empty calibrations map defeats the local_index backend.
6. **Track LTX-2.3.** Flag as "heavy" for now (22B); consider a `distilled` slot in the arch registry so operator can opt in without pinning the full stack.

## Run metadata

- runtime: ~5 min (approx; the sandbox lacks a hard wall-clock counter but agent-elapsed at write time was ~150s + write/deliver tail)
- WebSearch calls: 7
- WebFetch calls: 0 (HF MCP returned enough context; no page-fetches needed to confirm)
- HF MCP calls: 10
- **Failures / degradations this run:**
  - `tools/upgrade_research.py` MISSING — routine spec references a tool not yet in the repo. Section 6's ranked table pulled from HF MCP + web synthesis instead of the tool's top-50 output. Recommend implementing the tool (or updating this routine) before the next 48h tick.
  - **Gmail MCP unauthenticated** — the connector is connected but not authorized in this cloud session, so email delivery is not possible from here. Operator needs to authorize the Gmail connector in claude.ai → Connectors settings; until then, delivery lands on git + Drive only.
  - Spellcaster `build_*` count is **77**, up from the 2026-05-13 baseline of 73 — 4 new build functions since. Not a bug, just noting drift.
  - `_dev_docs/upgrade_research/` directory does not exist and was not created — the tool that would populate it is missing.
- next scheduled run: **2026-07-08** UTC (per the every-48h cron)
