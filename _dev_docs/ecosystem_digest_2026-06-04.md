# Ecosystem digest — 2026-06-04

_Generated by the every-48h cloud routine. Backends reached: HF MCP + WebSearch + WebFetch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` not present in this repo checkout — `local_index` / `huggingface` / `civitai` backends could not be invoked. First run, no prior digest to diff against._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Likes / Score | Why |
|---|---|---|---|---|---|
| 1 | `build_klein_multi_angle` (just shipped — commit 13c5675) | `fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA` | hf | 60.6K / 1384 / 22 | Production-grade multi-angle / camera-control LoRA tuned on Qwen-Image-Edit-2511; gaussian-splatting-trained. Direct upgrade of the CivitAI 2642426 port — same intent, much wider adoption (1384 likes vs 1 author's recipe). Apache-2.0. ~20B base = heavy. |
| 2 | Flux 2 Klein workflows (face/character methods) | `dx8152/Flux2-Klein-9B-Consistency` | hf | 38.0K / 409 / 7 | Identity-preserving LoRA on Klein 9B; 409 likes signals broad community adoption. Slots cleanly next to PuLID-Flux2. Apache-2.0. Heavy (9B base, > 13 GB exclusive-load). |
| 3 | Klein control-injection / depth-pose workflows | `nomadoor/flux-2-klein-9B-schematic-lora` | hf | 0 / 26 / 26 | Fresh-out-the-oven (created **Jun 1, 2026**, 3 days old). Single LoRA covering depth + normal map + pose + segmentation control on Klein 9B — collapses 4 ControlNet adapters into one. Worth a sniff-test even though downloads are zero. |
| 4 | `build_klein_*` face-swap variants | `nhathoangfoto/Flux.2-Klein-9B-SmartCharacterSwap` | hf | 338 / 10 / 9 | Another **Jun 1, 2026** release. Character-swap + face-swap + occlusion-handling LoRA. Klein-native alternative to the ReActor → Klein bridge pattern. Apache-2.0. |
| 5 | Qwen-Image-Edit speed path | `lightx2v/Qwen-Image-Edit-2511-Lightning` | hf | 334.8K / 453 / 4 | Distillation LoRA — fewer steps for the 2511 edit base. 334K downloads = community has voted. Drop-in next to existing Qwen-Image-Edit calls; pure latency win. |

## Ecosystem releases since last digest

- **ComfyUI**: **v0.23.0 (Jun 1, 2026)** and v0.22.0 (May 20, 2026) shipped. Baseline is 0.20.3 → **3 versions behind**. v0.22 brought Stable Audio 3.0, MoGe geometry, HiDream-O1 area conditioning, LTXV downscaled IC-LoRA. v0.23 adds Krea2 Image partner nodes, Beeble SwitchX, 3D camera improvements. https://github.com/Comfy-Org/ComfyUI/releases
- **krita-ai-diffusion**: **v1.51.0 (May 31, 2026)** shipped. Baseline is v1.50 → 1 version behind. Adds Anima control layers (Scribble / Line Art / Unblur), GPU-crash fix, managed ROCm install. https://github.com/Acly/krita-ai-diffusion/releases/tag/v1.51.0
- **LM Studio**: 0.4.15 latest — adds tensor parallelism for multi-GPU model loading. 0.4.13 (May 2026) bumped MLX engine to v1.8.1 with parallel vision predictions for Qwen 3.5/3.6 and Gemma 4. Locally AI acquisition + LM Link (remote-instance) preview added in April 2026.
- **Models (substantively new since baseline)**:
  - **Flux 2 Klein** (BFL, Jan 14, 2026): 4B + 9B + FP8 + KV-cache variants public. Heavy (9B > 13 GB; flag as exclusive-load on RTX 5060 Ti). Klein 4B fits inside 16 GB. Already wired into krita-ai-diffusion v1.47+.
  - **Qwen-Image-2.0** (Feb 10, 2026): unified gen + edit; native 2K; 1k-token typography. Currently #1 on AI Arena.
  - **Qwen-Image-Edit-2511** (Dec 17, 2025): character consistency, two-person fusion, baked-in LoRAs. 241K downloads. Already the de-facto edit baseline.
  - **LTX-2.3** (Mar 2026): 22B DiT, 4K@50fps, 20s clips, synced audio in one pass. Heavy.
  - **WAN 2.6** (Dec 2025) public; **WAN 2.7** reportedly targeted at March 2026 (9-grid I2V, instruction edit) — not yet on HF Hub as of this run; HF still shows 2.2-I2V-A14B as the trending live build.
  - **Z-Image Base / Edit / Omni**: roadmap items, no Turbo successor — Turbo (Nov 2025, Apache-2.0) remains the fast path; Base expected Q1–Q2 2026.

## HF trending shift

First run — nothing to diff against. Recording today's notable items for next-run comparison:

- **Klein ecosystem dominates trending** (top text-to-image / image-to-image): `FLUX.2-klein-9B` (score 34), `FLUX.2-klein-4B`, `FLUX.2-klein-9b-fp8`, `unsloth/FLUX.2-klein-9B-GGUF`.
- **Two brand-new Klein LoRAs landed Jun 1, 2026** (3 days old): `nomadoor/flux-2-klein-9B-schematic-lora` and `nhathoangfoto/Flux.2-Klein-9B-SmartCharacterSwap`. Worth a re-check next run to see if they stick.
- **Multi-angle is hot**: `fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA` sitting at 1384 likes is the single highest-liked item across all queries.
- **Wan2.2-I2V-A14B-Diffusers** (150K dl, 254 likes) is still the live trending I2V build — WAN 2.5/2.6/2.7 chatter hasn't translated to a Hub-public weight drop yet.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | iFayens (canonical first impl) + `Fayens/Pulid-Flux2` model (14 likes, MIT) + `Shivammesh26/Pulid-Flux2` mirror. Already in operator's 2026-05-08 upgrade batch. **No new news.** lldacing's "PuLID Flux II" pollution-fix variant worth checking if operator hits identity-bleed. |
| MagCache | NeurIPS 2025; ComfyUI native node supports Wan2.1, Wan2.2 (1.5-2x), Wan2.2-VACE-1.3B (1.7x), HunyuanVideo, Flux. **Already adopted.** No new news. |
| HyperSwap | hyperswap_1a_256.onnx + hyperswap_1b_256.onnx still the canonical pair. Wired through ComfyUI-ReActor. **Already adopted** (2026-05-08). No new news. |
| Differential Diffusion inpaint | Flux+DD workflow last refreshed Jan 5, 2026. **Already adopted.** No new news. Auxiliary: `mofos/Flux_Differential-diffusion_Inpainting_Diffusers` adds LoRA support — only relevant if operator needs LoRA-on-DD path. |

## Backend results

- `local_index` (lora_calibrations_sfw.json): 0 entries in the cloud clone (operator's local file likely populated; cloud sees baseline schema only)
- `huggingface` (via MCP, not the script): 4 targeted queries × ~10 candidates = 40 proposals reviewed; top 5 surfaced above
- `civitai`: SKIPPED — `tools/upgrade_research.py` not in repo
- `comfy_manager`: SKIPPED (LAN-only)
- `local_llm`: SKIPPED (LAN-only)
- `tools/upgrade_research.py`: **MISSING from repo checkout** — every-48h spec references it but the script does not exist at `tools/`. Either it was never committed or it lives outside the cloud-reachable tree. Recommend adding it so future digests can use the proper ranking pipeline. Today's digest fell back to direct WebSearch + HF MCP synthesis.

## Action items for operator

1. **Bump ComfyUI 0.20.3 → 0.23.0** — three minor versions behind; ComfyUI cadence is fortnightly so the gap will widen fast. v0.22 in particular adds LTXV IC-LoRA downscaling that the LTX path could lean on. (Heavy bump — read both changelogs first.)
2. **Bump krita-ai-diffusion v1.50 → v1.51** — Anima control layers (Scribble / Line Art / Unblur) are the kind of thing a Krita-driven workflow benefits from immediately; GPU-crash fix is a free win.
3. **Adopt `fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA` in `build_klein_multi_angle`** — the freshly-shipped CivitAI 2642426 port is doing the same job from a less-validated recipe. fal's LoRA has 1384 likes and a gaussian-splatting-trained pose space, which is what a multi-angle workflow actually wants. Worth A/B testing.

## Run metadata

- runtime: ~18m
- WebSearch calls: 12 / 25 budget
- WebFetch calls: 0 / 25 budget
- HF MCP calls: 7
- generated at: 2026-06-04 UTC (datetime.now(timezone.utc))
- next scheduled run: 2026-06-06 UTC
