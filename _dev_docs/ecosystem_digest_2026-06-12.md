# Ecosystem digest — 2026-06-12

_Generated by the every-48h cloud routine. Runtime: ~5m. Backends reached: HF MCP + WebSearch + WebFetch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` not present in this checkout (see Run metadata) — digest synthesised from HF MCP + web research only._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Flux-family `build_*` (Flux.1-dev base) | **FLUX.2 Klein 4B** (`black-forest-labs/FLUX.2-klein-4B`) | hf | 431.7K dl / 723 likes / TS 15 | Apache 2.0, 4-step distilled, sub-second inference, unified text-to-image + edit + multi-reference in one arch. Cleanest path off Flux.1-dev. **HEAVY** — ~13 GB VRAM, right at the 16 GB ceiling; recommend the `fp8` (67.7K dl) or `unsloth` GGUF (214.3K dl) cuts for headroom. |
| 2 | (new arch family — no current `build_*`) | **Ideogram-4 FP8** (`ideogram-ai/ideogram-4-fp8`) | hf | 5.0K dl / 498 likes / **TS 220** | Just landed 2026-05-30; dominates current text-to-image trending (4 of top 15 slots between FP8/NF4/GGUF/INT8 variants). Strong typography + prompt adherence reputation. License is `other` (non-Apache) — check terms before adopting. Worth a `build_ideogram4_*` family if licence acceptable. |
| 3 | (new arch family — no current `build_*`) | **Z-Image-Turbo** (`Tongyi-MAI/Z-Image-Turbo`) | hf | 849.4K dl / 4794 likes / TS 31 | 6B compact, Apache 2.0, performance comparable to 20B+ flagships. Excellent low-VRAM fit alongside the 16 GB ceiling. Sister variants (Z-Image Base / Omni / Edit) expected through 2026 — early adoption sets up the cluster. |
| 4 | `build_wan22_*` family | **WAN 2.6 i2v** (no clean HF mirror found) | web | n/a on HF | Spellcaster references WAN 2.2; ecosystem is on 2.6 (Dec 2025) with 2.7 planned March 2026. WAN 2.6 supports up to 15 s clips, character consistency across cuts, synchronised audio. **Caveat**: official weights are NOT on Hugging Face — only orphan low-download mirrors (`fanfa2320s/wan2.6`, `Fhcxfh/wan2.6`, 0 dl each). Distribution appears to be wan.video / Alibaba-gated. Adopt only once trustworthy weights surface; until then keep WAN 2.2 calibrations and flag as deferred. |
| 5 | `build_qwen_image_edit_*` | **Qwen-Image-Edit-2511** (referenced by `Phr00t/Qwen-Image-Edit-Rapid-AIO`, TS 30) | hf | 0 (AIO pack) / 2137 likes | Big consistency lift on portrait edits + multi-person group fusion; integrates popular LoRAs into base. Qwen-Image-2.0 (Feb 2026) folds edit + generate into one 7B pipeline — could simplify two `build_*` paths into one once stable. |

## Ecosystem releases since last digest

(First run — comparing against the embedded 2026-05-13 baseline in the routine, not a prior digest.)

- **ComfyUI**: **v0.23.0** released 2026-06-01 (also v0.22.0 2026-05-20). Baseline 0.20.3 is now 3 minor versions behind. v0.23 highlights: Krea2 Image + Beeble SwitchX partner nodes, logic nodes (And/Or/Not), Microsoft Lens support, Tripo3D P1 nodes, new 3D file types (PLY/SPLAT/SPZ/KSPLAT) + `Preview3DAdvanced`, frontend bump 1.44.19. Release cadence ~2 weeks. ([release page](https://github.com/Comfy-Org/ComfyUI/releases))
- **krita-ai-diffusion**: **v1.51.0** (2026-05-31) + **v1.51.1** point-fix (macOS managed install + Linux env upgrade). Inpaint/outpaint now run with optional prompts; AMD managed ROCm install added; smoother document open + graceful file-permission handling. Bump from baseline v1.50 is small but worth taking the point-fix. ([1.51.0](https://github.com/Acly/krita-ai-diffusion/releases/tag/v1.51.0), [1.51.1](https://github.com/Acly/krita-ai-diffusion/releases/tag/v1.51.1))
- **LM Studio**: **0.4.16 build 2** (2026-06-08). Notable: ships "Locally" mobile companion app (iPhone/iPad) for LM Link; waitlist removed; 8k default context; multi-GPU selection hardening across CUDA 12 / ROCm / Vulkan. Recent line: 0.4.14 stable MTP speculative decoding (2026-05-29); 0.4.15 tensor parallelism + physical batch size. ([changelog](https://lmstudio.ai/changelog))
- **Models** (substantively new since baseline):
  - **FLUX.2 Klein**: 4B/9B + base + fp8 + GGUF variants, all 2026-01-14; 4B Apache 2.0, 9B non-commercial.
  - **Ideogram-4**: 2026-05-30 (FP8 + NF4 + GGUF + INT8-ConvRot quants landing through 2026-06-07).
  - **LTX-2** (2026-01-06) and **LTX-2.3** (2026-03-05, 22B, native 4K + synced audio).
  - **Z-Image-Turbo** (2025-11-27, 6B); Base/Omni/Edit variants expected through 2026.
  - **Qwen-Image-2.0** (2026-02-10) — unifies generate + edit in 7B; **Qwen-Image-Edit-2511** ships the consistency upgrade.
  - **Cosmos3-Super-Text2Image** (NVIDIA, 2026-05-28; TS 17) — worth a closer look next digest.

## HF trending shift

(First run — no prior digest to diff. Snapshot of current top 15 text-to-image trending for next-run comparison.)

Top 15 by trendingScore (2026-06-12):

1. `ideogram-ai/ideogram-4-fp8` (TS 220, 2026-05-30)
2. `ideogram-ai/ideogram-4-nf4` (TS 136)
3. `black-forest-labs/FLUX.1-dev` (TS 59, 2024-07-31)
4. `leejet/ideogram-4-GGUF` (TS 52)
5. `black-forest-labs/FLUX.1-schnell` (TS 34)
6. `Tongyi-MAI/Z-Image-Turbo` (TS 31)
7. `Phr00t/Qwen-Image-Edit-Rapid-AIO` (TS 30)
8. `bertbobson/Ideogram-4-INT8-ConvRot` (TS 29)
9. `ponpoke/flux2-klein-9b-uncensored-text-encoder` (TS 21) — **NSFW-adjacent abliteration; do not surface as candidate** (H6)
10. `nvidia/Cosmos3-Super-Text2Image` (TS 17)
11. `black-forest-labs/FLUX.2-klein-4B` (TS 15)
12. `DeverStyle/Ideogram-4.0-Loras` (TS 15)
13. `zlab-princeton/i1-3B` (TS 14)
14. `Abiray/ideogram-4-GGUF` (TS 14)
15. `stabilityai/stable-diffusion-3.5-large` (TS 13)

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | **SHIPPED** — `iFayens/ComfyUI-PuLID-Flux2` is the first PuLID adaptation for Flux.2, supports Klein 4B/9B + Dev 32B; the 4B variant is called out as the best perf/resource balance. Concrete dependency on FLUX.2 Klein adoption (candidate #1). |
| **MagCache** | Stable; WAN 2.2 supported at 1.5–2× speedup; project has extended to Qwen-Image, HunyuanVideo, Flux. NeurIPS 2025 accepted. No regressions flagged. |
| **HyperSwap** | facefusion shipped HyperSwap 256 models rivalling inswapper at 2× resolution. ComfyUI-ReActor integration tracked at issue #143 — **not yet merged**; keep watching. |
| **Differential Diffusion inpaint** | Already covered by krita-ai-diffusion 1.51 inpaint streamlining (prompt-optional mode). No new diffusion-side paper landed this cycle. |

## Backend results

- **local_index**: `lora_calibrations_sfw.json` in this cloud checkout has 0 entries in `loras` (the operator's local store doesn't sync to the public repo). Baseline candidate count from this backend: **0**. Not a regression — expected.
- **huggingface (via HF MCP)**: ~25 trending + targeted-search candidates surfaced; top 5 promoted above.
- **civitai**: not exercised this run (`upgrade_research.py` absent — see Run metadata). Will fold in once script lands in repo.
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI to v0.23.0** (skip 0.21/0.22, go straight to current). Three-version jump from 0.20.3; verify Tripo3D / 3D-file-type nodes don't conflict with the spellcaster bundle's pinned frontend.
2. **Bump krita-ai-diffusion to v1.51.1** — small lift, fixes macOS managed install + Linux env-upgrade edge cases; useful inpaint UX improvements for the Tavern/Krita surface.
3. **Plan FLUX.2 Klein 4B adoption** — pair it with the already-shipped `ComfyUI-PuLID-Flux2` to land "PuLID-Flux2" from the upgrade-cluster watchlist on real weights. Use the fp8 / unsloth GGUF cut to stay clear of the 16 GB ceiling. Apache 2.0 means no licence friction for the 4B path.
4. **Stop tracking "WAN 2.3 / 2.4"** in the routine prompt — that bracket is two generations stale. Refocus on WAN 2.6 (Dec 2025) and 2.7 (March 2026), and decide whether to retire `build_wan22_*` once trustworthy weights surface on HF.
5. _(stretch)_ Evaluate **Ideogram-4** licence — if compatible, it's the strongest new text-to-image entrant and worth a `build_ideogram4_*` family. If not, the FLUX.2 Klein 4B + Z-Image-Turbo pair covers the same niche under Apache 2.0.

## Run metadata

- runtime: ~5m (cloud sandbox; well under the 60-minute ceiling)
- WebSearch calls: 10 / WebFetch calls: 0 / HF MCP calls: 6 (total 16, under the ~25 cap)
- `tools/upgrade_research.py`: **NOT PRESENT in this checkout** at `/home/user/spellcaster/tools/`. Section 2 of the routine could not run. Digest synthesised from HF MCP + WebSearch only. Operator action: add the script (or update the routine to drop the dependency).
- `_dev_docs/upgrade_research/` directory: created empty this run; no JSON/MD artefacts to commit until the script lands.
- `build_*` count check: **77** (baseline 73 as of 2026-05-13). +4 builders since baseline — within expected drift.
- next scheduled run: **2026-06-14 16:00 UTC**
