# Ecosystem digest — 2026-05-16

_Generated by the every-48h cloud routine. Runtime: ~14m. Backends reached: HF MCP + WebSearch + WebFetch. Local backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` is **absent from this checkout** — top-candidate ranking below was synthesised from HF MCP trending data + web releases instead._

> First run — no prior digest to diff against. The "trending shift" section is a snapshot only this time; the next run (2026-05-18) will start producing real deltas.

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Signal | Why |
|---|---|---|---|---|---|
| 1 | WAN i2v (baseline: WAN 2.2 A14B) | **WAN 2.6** (Dec 2025) / **WAN 2.7** (planned Mar 2026) | wavespeed + mindstudio | shipping | Operator's lora_calibrations references WAN 2.2 — two full major versions behind. 2.6 adds Pro/Flash/Spicy variants + ref-to-video; 2.7 brings 9-grid i2v and instruction-based editing. Heavy (14B MoE). |
| 2 | LTX i2v / t2v | **LTX-2.3** (5 Mar 2026, 22B) + community `Kijai/LTX2.3_comfy` ComfyUI port | github + wavespeed | trending HF LoRA (`WarmBloodAban/Singularity_LTX-2.3_OmniCine_Preview0.1`, 25 trend) | Native 4K @ 50fps with synchronised audio in one pass — only open-weight model doing this. **Heavy** (22B, > 13 GB exclusive-load). |
| 3 | Qwen-Image-Edit | **Qwen-Image-Edit-2511** (latest) + **Qwen-Image-2.0** (10 Feb 2026, 7B) | qwenimages.com + gigazine | Phr00t AIO at 2015 likes | Native 2K, unified generation+editing in 7B (leaner than older 20B Edit). 2511 specifically targets multi-person consistency in group shots — relevant to character-portrait flows. |
| 4 | Z-Image-Turbo (image base) | **Tongyi-MAI/Z-Image (full base)** (Jan 27 2026) + **Z-Image-Edit / Z-Image-Omni** (in testing) | comfyui-wiki + Tongyi-MAI HF | base 1.3M dl, trend 32 | Foundation model that Z-Image-Turbo distilled from — higher quality, more flexibility. Same 6B arch so VRAM ceiling unchanged. Edit/Omni variants extend it to inpaint + unified gen-edit. |
| 5 | New entrant (general T2I) | **baidu/ERNIE-Image** (7 Apr 2026, 8B, Apache-2.0) | HF trending #11 (score 17) | 13.9k dl / 626 likes in ~5 weeks | Baidu's first fully-open 8B T2I — krita-ai-diffusion v1.50 already added it as a preview model. Worth a `build_*` adapter once it stabilises. |

## Ecosystem releases since last digest

- **ComfyUI** — baseline 0.20.3. GitHub releases page (`comfyanonymous/ComfyUI` + `Comfy-Org/ComfyUI`) shows nothing **dated 2026** higher than 0.20.x in the fetched output (most-recent labelled tags are 0.21.x but stamped 2025 — likely upstream metadata oddity). **No confirmed bump available right now.** Re-check next run.
- **krita-ai-diffusion** — baseline v1.50 (Acly). Latest release page still tops at v1.50.0 (May 3). v1.47 already added Flux 2 Klein + Z-Image Turbo, v1.50 added ERNIE-Image + Anima preview. **No new release.**
- **LM Studio** — Build 1 with mlx-engine v1.8.1 (13 May 2026): parallel predictions for Qwen 3.5/3.6 and Gemma 4 vision models; chat-input newline-paste fix. LAN-only impact, but flags that Qwen 3.6 is now first-class on the local LLM side.
- **Models** (only substantive new things):
  - **Flux 2 Klein**: no new BFL checkpoint beyond the Jan/Apr 2026 4B/9B family; `Lakonik/AsymFLUX.2-klein-9B` (12 May, score 42) is a pixel-diffusion finetune worth tracking, and `fal/FLUX.2-dev-Turbo` (score 3) is a turbo distillation LoRA on top of Flux.2-dev.
  - **WAN**: 2.6 already shipping, 2.7 imminent (see top-5).
  - **LTX**: 2.3 is current shipped open-weight (see top-5).
  - **Z-Image**: full Z-Image base + community Juggernaut-Z-Image (29 Apr, score 45) and SeeSee21/Z-Anime (25 Apr, score 124 — **#1 trending T2I overall**) — clear momentum.
  - **Qwen-Image-Edit**: 2511 is current; Qwen-Image-2.0 (Feb 10 2026, 7B) is the new gen-side base.

## HF trending shift

_First run — no prior top-15 to diff against._ Snapshot of current HF text-to-image top-15 by trendingScore (SFW-filtered per H6; two NSFW-tagged entries dropped):

1. SeeSee21/Z-Anime (124)
2. sensenova/SenseNova-U1-8B-MoT (59) — any-to-any unified model
3. RunDiffusion/Juggernaut-Z-Image (45)
4. Lakonik/AsymFLUX.2-klein-9B (42)
5. Tongyi-MAI/Z-Image-Turbo (32)
6. black-forest-labs/FLUX.1-dev (30)
7. Phr00t/Qwen-Image-Edit-Rapid-AIO (26)
8. WarmBloodAban/Singularity_LTX-2.3_OmniCine_Preview0.1 (25)
9. black-forest-labs/FLUX.1-schnell (21)
10. baidu/ERNIE-Image (17)
11. Qwen/Qwen-Image-2512 (14)
12. SearchingMan/Z-Image-Turbo-student-adapter (13)
13. stabilityai/stable-diffusion-xl-base-1.0 (13)
14. fal/FLUX.2-dev-Turbo (3)
15. black-forest-labs/FLUX.2-klein-base-4B (2)

Headline: **Z-Image ecosystem is the dominant trend cluster** — three of the top-5 trending entries are Z-Image derivatives.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | `iFayens/ComfyUI-PuLID-Flux2` v0.6.2 confirmed working on Klein 4B/9B + Dev; this is the upstream the operator's 2026-05-08 adoption batch was tracking. No newer fork found this sweep. |
| MagCache | NeurIPS 2025 paper, ~2.68–3.0× speedup on WAN 2.1 t2v/i2v. Already shipped in the 2026-05-08 pass. **No 2026 follow-up paper or WAN 2.2/2.6 port surfaced this sweep** — worth a poke at `Zehong-Ma/ComfyUI-MagCache` issues next run to see if a 2.2-compatible branch exists. |
| HyperSwap | FaceFusion 3.3.0 ships 1A/1B/1C variants with new encoder (breaking change vs older runs). Operator's HyperSwap 256 was from this line. No 2026 named upgrade yet — encoder swap is the only watch-item. |
| Differential Diffusion inpaint | Already adopted (2026-05-08 pass). Fluid Diffusion integrated it in their 2026 update — affirms it's stable upstream. No new variant to evaluate. |

## Backend results

- **local_index**: 0 baseline LoRA candidates in `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` (`loras: {}` — file has schema + notes but no entries yet).
- **huggingface**: ~25 candidates surfaced via 3 trending queries (text-to-image, flux2-klein lastModified, i2v) — SFW-filtered to 22.
- **civitai**: SKIPPED (no `tools/upgrade_research.py` driver in this checkout).
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **WAN 2.2 → 2.6/2.7 bump is the highest-leverage item this cycle.** Operator's `lora_calibrations_sfw.json` and any `build_wan_*` methods still target 2.2; 2.6 is a year-old shipping release. Suggest spinning a `build_wan_2_6_i2v` method alongside the existing 2.2 builders rather than swapping in-place — too many calibrations to invalidate. Heavy: 14B MoE, > 13 GB.
2. **Ship `tools/upgrade_research.py`.** The cloud routine is operating blind without it; the doc references a `local_index,huggingface,civitai` backend driver that doesn't exist in the repo. Even a minimal HF-trending + civitai-search wrapper would 2-3× the digest's specificity.
3. **Add a `build_*` for Z-Image base + an evaluator slot for Z-Image-Edit when it ships.** Z-Image-Turbo is presumably already wired (it's been out since Nov 2025); the full base now has enough community momentum (top trending cluster) to be worth a parallel slot.

## Run metadata

- runtime: ~14 minutes
- WebSearch + WebFetch call count: 11 (under the ~25 cap)
- HF MCP calls: 5
- next scheduled run: 2026-05-18 16:00 UTC
- divergence note: `build_*` count is **74** in this checkout vs 73 baseline (one new builder landed since 2026-05-13); not material.
- known gap: `tools/upgrade_research.py` absent → top-5 was synthesised by hand from HF trending + web releases rather than ranked by the driver's scoring fn.
