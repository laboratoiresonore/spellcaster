# Ecosystem digest — 2026-06-20

_Generated by the every-48h cloud routine. Backends reached: HF MCP + WebSearch + WebFetch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. The `tools/upgrade_research.py` script referenced by the routine is **not present** in the current repo HEAD — digest was assembled from HF MCP trending data + targeted web search instead._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Likes | Why |
|---|---|---|---|---|---|
| 1 | `build_klein_*` (FLUX.2 [klein]) | `black-forest-labs/FLUX.2-klein-4B` | HF (trendingScore 14) | 502K / 746 | The 4B Klein fits ~13 GB VRAM — squarely inside the 16 GB ceiling. The 9B variant (~29 GB) is **heavy**; only the 4B is safely loadable without exclusive-load. Spellcaster currently treats Klein as one tier; splitting 4B-default / 9B-on-demand reflects the actual VRAM math. |
| 2 | `build_qwen_edit` | `Qwen/Qwen-Image-Edit-2511` | HF (trendingScore 6, +derivatives 25/11/9) | 193K / 1057 | Drop-in successor to Qwen-Image-Edit (2509). Operator's `build_qwen_edit` calibration likely targets 2509 — bumping to 2511 gains image-drift mitigation, character consistency, integrated LoRA capability. Same arch, no workflow rewrite. |
| 3 | _(new arch — no current builder)_ | `Tongyi-MAI/Z-Image-Turbo` | HF (trendingScore **34**, top of trending) | 935K / **4839** | The breakout T2I model of the last quarter. 6B parameter, 8-step distilled, bilingual, photorealistic — sits cleanly below the VRAM ceiling. No `build_z_image*` exists yet. Recommend a new `build_z_image_turbo` workflow as a fast-path alternative to Lumina2/Flux. |
| 4 | `build_pulid_flux` | `iFayens/ComfyUI-PuLID-Flux2` (custom node) + `Fayens/Pulid-Flux2` weights | GitHub + HF | — / 14 | The watchlist item finally has a working impl. Supports Klein + Dev variants, dual-mode (Face Combine + Zero-Shot), InsightFace + EVA-CLIP. Direct upgrade path from current PuLID-Flux. Pair with item #1 (Klein 4B) for face-locked Klein generation. |
| 5 | `build_wan22_t2v`, `build_wan_video`, `build_wan_flf` | WAN 2.6 / WAN 2.7 | Web (mindstudio.ai, wavespeed) | — | WAN jumped 2.2 → 2.6 (Dec 2025) → 2.7 (April 2026), **skipping 2.3/2.4 entirely**. 2.7 adds first/last-frame control (which spellcaster already calls `build_wan_flf` for), 9-grid input, 5000-char prompts. Calibration JSON should rename WAN-2.2 references — 2.2 is now two majors behind. |

## Ecosystem releases since last digest

_(No prior digest — this is run #1 of the routine. "Since last digest" is interpreted as "since the 2026-05-13 spellcaster baseline".)_

- **ComfyUI**: 0.20.3 → **0.21.1 (May 13, 2026)** → **0.22.0 (May 20, 2026)** → **0.23.0 (June 1, 2026)**. Three minor versions in three weeks. 0.21 adds Partner Nodes (image/edit/LLM) + dtype fixes; 0.22 adds MoGe + Stable Audio 3; 0.23 brings multi-threaded model loading, MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens. The multi-threaded loader in 0.23 is the operator-visible win — bundle bump recommended.
- **krita-ai-diffusion**: v1.50 → **v1.51.0 → v1.51.1** (Acly). Adds Anima control layers + supported control modes. Minor bump, low-urgency.
- **LM Studio**: no actionable 2026 release surfaced this window (search came back thin — local backend is LAN-side and operator-tracked anyway).
- **Models** (substantively new since 2026-05-13):
  - **Flux 2 Klein**: 4B + 9B + multiple quantized variants (fp8, nvfp4, base) all live as of Jan 14, 2026. Active LoRA ecosystem: depth-controlnet (May 27), consistency LoRA (Mar 5), SmartCharacterSwap (Jun 1).
  - **WAN**: 2.6 (Dec 2025) and 2.7 (April 2026) — see item #5.
  - **LTX-Video**: **LTX 2.3 released March 5, 2026** — 22B-param DiT, native 4K @ 50 FPS, single-pass audio sync. Spellcaster's `build_ltx_video` likely targets an older version; calibration check warranted.
  - **Z-Image**: Turbo (Nov 2025) + full Base expected later 2026 + Edit/Omni in testing. See item #3.
  - **Qwen-Image-Edit**: 2511 (Dec 23, 2025) — see item #2. Also: **Qwen-Image 2.0** (Feb 10, 2026) — 7B vs the original 20B with better benchmarks, worth tracking as a possible `build_qwen_image_v2` if operator wants leaner Qwen-arch T2I.

## HF trending shift

_Skipped — no prior digest to diff against. First-run snapshot of "what's hot now":_

- **Top trending in spellcaster-relevant arches (last 30d, by HF trendingScore):**
  - Z-Image-Turbo (34) — see item #3
  - FLUX.2-klein-9B (32)
  - Phr00t/Qwen-Image-Edit-Rapid-AIO (25) — all-in-one Qwen edit checkpoint, 2.1k likes
  - WanVideo_comfy (Kijai) (18) — the WAN 2.1-VACE bundle, still dominant
  - flux2-klein-9b-uncensored-text-encoder (16) — ponpoke; **flagged as out-of-scope (H6 SFW canon)** — not surfaced as a candidate
  - FLUX.2-klein-4B (14) — see item #1
  - refcontrol-FLUX.2-klein-9B-reference-depth-lora (13)
  - fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA (11) — pairs with `build_klein_multi_angle`-style workflow

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **Live.** `iFayens/ComfyUI-PuLID-Flux2` custom node + `Fayens/Pulid-Flux2` weights on HF. See item #4. |
| MagCache | **Shipped + broadened.** Supports WAN 2.1/2.2 (1.5–2× speedup), Qwen-Image (1.75×), HunyuanVideo-1.5 (1.7×), FLUX-dev. If spellcaster's MagCache integration is WAN-only, broadening to Qwen + Hunyuan is cheap win. |
| HyperSwap | **Stalled at 256 + 512 pixel-boost.** No native 512 model release — FaceFusion Labs models still hyperswap_1{a,b,c}_256.onnx. Re-check next run. |
| Differential Diffusion inpaint | No 2026 movement surfaced. Keep on watchlist; consider de-prioritising if quiet next 2 runs. |

## Backend results

- local_index: **0** baseline LoRA candidates — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` is the shipped empty stub (`loras: {}`). No local-LoRA upgrade signal possible from cloud.
- huggingface (via MCP, not the routine's missing script): **~40** repos enumerated across Klein / WAN / Qwen-Edit / Z-Image / PuLID-Flux2 queries. Filtered to 5 headline candidates above.
- civitai: SKIPPED this run (no `tools/upgrade_research.py` — would need direct API calls, deferred).
- comfy_manager: SKIPPED (LAN-only).
- local_llm: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI bundle 0.20.3 → 0.23.0**, primarily for the multi-threaded model loader. Three minors of accumulated fixes too. (high impact)
2. **Add a `build_z_image_turbo` workflow.** Z-Image-Turbo is the strongest new T2I architecture of the period (935K downloads / 4839 likes on HF in 7 months) and fits comfortably under the 16 GB VRAM ceiling. Spellcaster has nothing for it yet. (medium impact, net-new feature)
3. **Calibration JSON sweep**: WAN 2.2 references in `lora_calibrations_sfw.json` are now stale (WAN is at 2.7). Also re-target `build_qwen_edit` calibration from Qwen-Image-Edit-2509 → 2511. (low effort, accuracy)
4. **Implement `tools/upgrade_research.py`** referenced by this routine — without it, future digests rely on hand-curated MCP queries. The routine prompt's structure (`--backends`, `_dev_docs/upgrade_research/<iso-week>.{json,md}`) is the spec. (meta — improves all future runs)

## Delivery channels

- **Git/PR**: ✅ committed + pushed (this digest is the canonical artefact).
- **Gmail**: ❌ MCP tools never registered in this session — no `mcp__Gmail__*` schemas surfaced via ToolSearch despite the connection notice. Operator should check their Gmail MCP server config.
- **Google Drive**: ❌ `MCP server "Google-Drive" requires re-authorization (token expired)`. Operator needs to re-auth the Google-Drive MCP connection.

## Run metadata

- runtime: ~7m
- WebSearch calls: 8 / WebFetch calls: 0 (HF MCP covered the model-detail needs)
- HF MCP queries: 6
- GitHub MCP releases queries: 2 (both denied — scope is `laboratoiresonore/spellcaster` only; release info pulled from web instead)
- Notable gaps:
  - `tools/upgrade_research.py` missing → no machine-ranked candidate list (digest assembled from HF MCP + targeted web search).
  - civitai backend skipped (would need direct API calls absent the script).
  - LM Studio search returned no 2026 release signal.
  - Gmail MCP + Google-Drive MCP both unavailable this run (see "Delivery channels" above).
- next scheduled run: 2026-06-22 (T+48h)
