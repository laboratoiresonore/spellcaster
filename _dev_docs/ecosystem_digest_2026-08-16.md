# Ecosystem digest — 2026-08-16

_Generated by the every-48h cloud routine. Runtime: ~4m. Backends reached: HF MCP + WebSearch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. **`tools/upgrade_research.py` is not present in the checked-out repo** — digest built entirely from web + HF MCP research (see Run metadata)._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Flux2 Klein base — image gen | **`wikeeyang/Flux2-Klein-9B-True-V3`** finetune | hf | 30.2K DL / 129 likes / trending 4 | Community-driven Klein 9B rework that resolves color-cast, saturation, and "dirty image" artifacts reported against BFL's baseline release; native diffusers + gguf. Heavy at 9B (fp8 needed on 16 GB). |
| 2 | Text-to-image foundation | **Krea 2 Turbo (12.9B)** | hf `krea/Krea-2-Turbo` | Open weights, custom license | New from-scratch DiT released 22 Jun 2026; Turbo distill hits 2 s @ 2K on consumer HW, ranks #1 among independent-lab T2I models. Free commercial use for individuals; enterprise-license gated at >50 seats. Very heavy — ~13 GB is the fp8/quant path. |
| 3 | Video (I2V) | **LTX-Video 2.5** | Lightricks / hf | Open weights day-one ComfyUI | Released 11 Aug 2026, Apache-2.0. 10-second 720p I2V clip in 6.8 s on 2× GB200 (well beyond 5060 Ti VRAM, but the model is smaller/faster than WAN 2.2 at parity — worth measuring). Replaces or supplements WAN 2.2 I2V. |
| 4 | Qwen-Image-Edit — edit path | **Qwen-Image-2.0 (7B)** | hf `Qwen/Qwen-Image-2.0` | ~7B, unified gen+edit | Released 10 Feb 2026 — 7B compact model brings native 2K, unified generation+editing, professional typography. Smaller than the 20B Qwen-Image-Edit; likely closer to the 16 GB budget. Note: Qwen-Image-Edit-2511 (Nov update) also adds strong multi-person consistency for group photos. |
| 5 | Face identity (PuLID) | **PuLID-Flux2 v0.6.2** (Fayens fork) | github + hf `Fayens/Pulid-Flux2` | Native safetensors weights | Now supports Klein 4B / 9B / Dev variants; identity injection via double-block token, InsightFace + EVA-CLIP encoders. Direct drop-in for the existing PuLID-Flux path once we lift Flux2 wiring. Training scripts temporarily removed (instability) — inference-only for now. |

## Ecosystem releases since last digest

_Baselines: ComfyUI 0.20.3, krita-ai-diffusion v1.50, spellcaster @ 73 build\_\*._

- **ComfyUI**: three minor bumps landed — **v0.21.0** (13 May 2026, partner-nodes + dtype fixes), **v0.22.0** (20 May 2026, MoGe + Stable Audio 3 support), **v0.23.0** (1 Jun 2026, multi-threaded model loading + PixelDiT + MediaPipe face detection + OpenAPI cleanup). We're 3 minor versions behind the bundle.
- **krita-ai-diffusion (Acly)**: **v1.51.0** (Anima control layers — Scribble/Line Art/Unblur), **v1.51.1** (macOS/Linux managed install fixes), **v1.52.0** (Krea 2 basic support, requires Krita 5.3.2.1, auto-update broken → manual install needed), **v1.52.1** (managed-server connect fix).
- **LM Studio**: **0.4.14** (29 May, MTP speculative decoding stable), **0.4.16** (8 Jun, "Locally" iOS/iPad app + LM Link — largest LM Studio models on the go), **0.4.18** (27 Jun, UI/reasoning fixes), **0.4.20** (22 Jul, enterprise network endpoints + Bionic LM Link), plus **LM Studio Bionic 1.0.0** (17 Jul 2026).
- **Models** (only genuinely new since 2026-05-13):
  - **Krea 2** — 12.9B open-weights DiT, `Raw` + `Turbo` checkpoints (22 Jun 2026). New foundation model.
  - **LTX-Video 2.5** — open-weights I2V, day-one ComfyUI (11 Aug 2026).
  - **Wan 2.6** (Dec 2025) and **Wan 2.5** (Q1 2026 preview) remain **API/commercial-only, no weights published** — not adoptable in Spellcaster today. **Wan 2.7** is being teased on Wavespeed as "coming soon" — watch, do not integrate.
  - **Flux 2 Klein finetunes** on HF now include `wikeeyang/Flux2-Klein-9B-True-V3` (25 Jun), `nhathoangfoto/Flux.2-Klein-9B-MatchingPose` (19 Apr, pose transfer / character consistency LoRA), `fal/flux-2-klein-4b-spritesheet-lora` and `fal/flux-2-klein-4B-outpaint-lora` (19 Jan).
  - **Qwen-Image-Edit-2511** — improved character/multi-person consistency (still 20B though).
  - **Z-Image Base** — Alibaba (not Tencent), released 27 Jan 2026 as the undistilled foundation the Turbo was cut from. Correction to prior operator notes: Z-Image is Alibaba Tongyi, not Tencent.

## HF trending shift

_No prior digest to diff against — this is the first run. Skipping trend-shift table; from next run onward, this section will list "new in top 15" and "dropped" vs the previous digest._

Current HF `flux2-klein` trending leaders (Aug 2026), for the next diff's baseline:
1. `black-forest-labs/FLUX.2-klein-base-9b-fp8` (score 17)
2. `black-forest-labs/FLUX.2-klein-9B` (16)
3. `ponpoke/flux2-klein-9b-uncensored-text-encoder` (16) — NSFW-adjacent by intent, **skip per H6**
4. `black-forest-labs/FLUX.2-klein-4B` (10)
5. `black-forest-labs/FLUX.2-klein-9b-fp8` (10)
6. `unsloth/FLUX.2-klein-4B-GGUF` (4)
7. `unsloth/FLUX.2-klein-9B-GGUF` (4)
8. `wikeeyang/Flux2-Klein-9B-True-V3` (4)

Current HF `wan-2.2-i2v` trending leaders:
1. `Wan-AI/Wan2.2-I2V-A14B` (score 7)
2. `Wan-AI/Wan2.2-I2V-A14B-Diffusers`, `QuantStack/Wan2.2-I2V-A14B-GGUF`, `bullerwins/Wan2.2-I2V-A14B-GGUF` (all 1)
3. New INT8/FP8 quants: `Dewprisim/WAN2.2-14B-I2V-INT8` (15 Jul), `unsloth/Wan2.2-I2V-A14B-FP8` (18 Jul) — closer fits to the 16 GB budget than the full A14B checkpoint.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | Active — v0.6.2 with Klein 4B/9B & Dev; `Fayens/Pulid-Flux2` native safetensors. Ready to wire once Spellcaster has a Flux2 Klein path. Training scripts temporarily removed (instability). |
| **MagCache** | Active — official Wan 2.2 support at 1.5–2× speedup; Flux-Kontext 2×; HunyuanVideo-1.5 1.7×. NeurIPS 2025 paper landed. `Zehong-Ma/ComfyUI-MagCache` is the canonical node. |
| **HyperSwap** | Regressing — the `hyperswap_1a/1b/1c_256.onnx` models still ship via ComfyUI-ReActor but a black-circle-face regression opened Apr 2026 (issue #226) is unfixed. Operator baseline (HyperSwap 256, part of the 2026-05-08 upgrade batch) may hit the same bug — **verify locally**. Fallbacks (InSwapper, ReSwapper) still work. |
| **Differential Diffusion inpaint** | Quiet at the ComfyUI-integration level — no new node/model of note. Adjacent: `DiffSynth-Studio` released Qwen-Image-Layered-Control with brush-controlled layer separation (Aug 2026) — potentially interesting for edit pipeline. `IS-Diff` (arXiv Sep 2025) refined initial-seed picking for diffusion inpaint but no production integration yet. |

## Backend results

- **local_index**: 0 candidates. `lora_calibrations_sfw.json` currently has an empty `loras` dict (schema present, no entries populated) — nothing for local_index to rank against. Worth investigating on operator's local repo — this may be a cloud-clone divergence, or the calibration table may genuinely have been reset.
- **huggingface**: ~8 substantive Flux2-Klein candidates + LTX-Video 2.5 + Krea 2 (see Headline).
- **civitai**: SKIPPED (backend absent from repo).
- **comfy_manager**: SKIPPED (LAN-only, and backend absent).
- **local_llm**: SKIPPED (LAN-only, and backend absent).

## Action items for operator

1. **Bump ComfyUI 0.20.3 → 0.23.0** in the Spellcaster bundle. Three minor releases have landed; the multi-threaded model loading in 0.23 is a clean win for cold-start latency, and the PixelDiT + MediaPipe face detection additions may open new build\_\* opportunities.
2. **Bump krita-ai-diffusion v1.50 → v1.52.1** on operator's Krita side. Note the v1.52.0 auto-update-broken caveat — manual install required for this hop. Krea 2 basic support (v1.52.0) is the interesting change.
3. **Restore `tools/upgrade_research.py`** — this cloud routine's prompt assumes it exists; it does not on the current branch. Without it, `local_index` scoring is unreachable and the digest degrades to web-only research. If it lives on an unmerged branch, cherry-pick; if it was never written, prioritize scaffolding it.
4. **Populate `lora_calibrations_sfw.json`** — the file has schema-only content (0 LoRAs). Confirm this reflects operator intent, not accidental truncation.
5. **Investigate the HyperSwap 256 regression** — the Apr 2026 ReActor issue may or may not manifest on operator's setup. Non-blocking: InSwapper is a working fallback.
6. **Flux2 Klein 4B > 9B for our VRAM ceiling** — with 16 GB VRAM, target the 4B variants (fp8 or GGUF) for any Flux2-Klein path work. The 9B fp8 is possible but under exclusive-load pressure; the base 9B (~29 GB) is out of scope.

## Run metadata

- runtime: ~4 minutes
- WebSearch calls: 11 / ~25 budget
- WebFetch calls: 0 (search snippets were substantive enough — did not need per-page fetches)
- HF MCP calls: 3 (`hub_repo_search` × 3)
- **Missing capabilities this run:**
  - `tools/upgrade_research.py` — absent from the repo; degraded to web+HF only (see action item #3).
  - **Gmail MCP** — server requires authorization; not delivered via email. Operator can authorize in their claude.ai connector settings for future runs.
  - **Google-Drive MCP** — attempted, see delivery section of PR body.
- baseline check: `build_*` count = **77** (baseline: 73 at 2026-05-13). Modest +4 growth on the checked-out `main`.
- next scheduled run: 2026-08-18 ~16:00 UTC (48h cadence)
