# Ecosystem digest — 2026-06-24

_Generated by the every-48h cloud routine. First run — no prior digest to diff against. Backends reached: HuggingFace MCP + WebSearch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` is absent from the cloud checkout — see Run metadata._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Signal | Why |
|---|---|---|---|---|---|
| 1 | image generation base | **Flux.2 Klein 4B** (`black-forest-labs/FLUX.2-klein-4B`) | hf | Apache-2.0, official BFL, 4B params, 13 GB VRAM | Newer base than Flux/Flux-Kontext; fits 16 GB ceiling; unified gen+edit in one checkpoint; sub-1s e2e inference. Drop-in successor architecture. |
| 2 | image editing (Qwen-Image-Edit) | **Qwen-Image-Edit-2511 + Lightning LoRA** (`lightx2v/Qwen-Image-Edit-2511-Lightning`) | hf | 294K dl (Lightning), 188K dl (base), 1067 likes | Strong refresh of Qwen-Image-Edit baseline: multi-person fusion, identity preservation; Lightning LoRA distills to 8-step inference. #1 on AI Arena editing. |
| 3 | speedup (Flux.2 quantization) | **Nunchaku SVDQuant for FLUX.2-Klein-9B** (`tonera/FLUX.2-klein-9B-Nunchaku`) | hf | 2.3K dl, 15 likes | 3.05× throughput, 7.52 GB peak VRAM (down from 20 GB). Unlocks the 9B Klein on a 16 GB card with KV-cache variant for multi-ref. Apache-flavoured fork. |
| 4 | WAN i2v speedup | **MagCache v2 — official WAN 2.2 support** (`Zehong-Ma/MagCache`) | github+hf | NeurIPS 2025 paper, 194 GH stars | Official 1.5–2× speedup for WAN 2.2 (training-free). Wan2.2 build_* methods should adopt; complements existing MagCache integration shipped 2026-05-08. |
| 5 | face identity (PuLID-Flux2) | **ComfyUI-PuLID-Flux2 v0.6.2** (`iFayens/ComfyUI-PuLID-Flux2`) | github | active May 2026 | First-class PuLID for FLUX.2 (Klein 4B/9B + Dev 32B). Direct successor to PuLID-Flux2 already in 2026-05-08 upgrade batch — version bump worth a look. |

## Ecosystem releases since baseline

- **ComfyUI**: 0.20.3 baseline → **0.23.0** (2026-06-01). Multi-threaded model loading, MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens nodes. 0.22.0 (2026-05-20) added MoGe + Stable Audio 3. **Three-minor jump worth a bundle bump.**
- **krita-ai-diffusion**: v1.50 baseline → **v1.51.1**. Fill/Expand at full strength now requires the inpaint control model (much better results). ROCm managed install added for AMD. macOS install fixes in 1.51.1.
- **LM Studio**: **0.4.16** (2026-06-08). Mobile app ("Locally") on iPhone/iPad with LM Link to your desktop models. 0.4.14 made MTP Speculative Decoding stable. 0.4.12 added Qwen 3.6.
- **Flux.2 Klein**: family launched **April 2026** under Apache 2.0 — 4B (13 GB VRAM, consumer-fit), 9B, 9B-KV (KV-cache for multi-ref editing), base/FP8 variants. Multiple community quants on HF (Nunchaku SVDQ, GGUF, SDNQ 4-bit).
- **WAN**: 2.6 live since Dec 2025; **WAN 2.7 launching ~March 2026** (per WaveSpeed roadmap) with 9-grid i2v, audio, instruction-edit. No "WAN 2.3/2.4" — version line jumped 2.2 → 2.5/2.6.
- **LTX-Video**: **LTX-2.3** current. No 2.4 or 3.0 announced. New gated-attention text connector, sharper VAE, native portrait.
- **Z-Image**: **Z-Image-Turbo** (Nov 2025), **Z-Image base** (2026-01-27), **Z-Image-Omni-Base** (gen+edit). No v2 successor — line expanded sideways with base + omni variants.
- **Qwen-Image-Edit**: **Qwen-Image-2.0** (2026-02-10) unifies gen+edit in 7B. **Edit-2511** sub-variant improves character consistency for multi-person group composites.

## HF trending shift

_No prior digest — skipping diff. Trending search for "text-to-image" returned mostly stale templates (sort:trendingScore is dominated by low-quality 2022/2023 repos under this query). Recommend the local cron's HF backend use a richer query mix._

Notable model-level signal from name-targeted searches:
- **Flux.2-Klein-9B-Nunchaku** (tonera, 2.3K dl) — strongest community quant for the 9B.
- **Wan2.2-I2V-A14B-mlx-q8** (Anes1032, 2026-06-23) — fresh Apple Silicon port; not relevant to RTX 5060 Ti but signals continued WAN 2.2 momentum.
- **lightx2v Qwen-Image-Edit-2511-Lightning** (294K dl, 469 likes) — overwhelming community pickup of the distilled variant.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **v0.6.2** (May 2026) shipped. Supports Klein 4B/9B + Dev 32B. Bump candidate vs. 2026-05-08 batch. |
| MagCache | **Official WAN 2.2 support** (1.5–2×). Existing MagCache integration may already cover this — verify in local lookup pass. |
| HyperSwap | Three model variants (`hyperswap_1a/1b/1c_256.onnx`) shipped in ComfyUI-ReActor. Matches 2026-05-08 "HyperSwap 256" line — no new sizes announced. |
| Differential Diffusion inpaint | Stable; **LanPaint** added Anima inpaint/outpaint (May 2026), fixed perf bug (March 2026). DD itself unchanged. |

## Backend results

- **local_index** (`lora_calibrations_sfw.json`): **0** baseline candidates — `loras` dict is empty in the shipped public repo (community baseline not yet seeded).
- **huggingface** (via MCP): **5** ranked proposals (above). 4 hub_repo_search + 1 paper_search calls.
- **civitai**: **SKIPPED** — `tools/upgrade_research.py` is absent from the cloud checkout, so the SFW-double-filter backend wasn't reachable. Local cron must continue to cover Civitai.
- **comfy_manager**: SKIPPED (LAN-only)
- **local_llm**: SKIPPED (LAN-only)

## Action items for operator

1. **Bump ComfyUI 0.20.3 → 0.23.0** in the bundled install (three-minor jump; multi-threaded model loading alone is significant). Test the Spellcaster `build_*` workflows against new partner-node OpenAPI standardisation.
2. **Bump krita-ai-diffusion v1.50 → v1.51.1** (Fill/Expand quality improvement). Inpaint control model is now required at full strength — surface the dependency in the installer.
3. **Evaluate Flux.2 Klein 4B as a base architecture** — 13 GB VRAM fits the 16 GB ceiling, Apache 2.0, unified gen+edit. Would join (not replace) Flux/Kontext lineup. Klein-9B-Nunchaku is the heavier path if SVDQuant integration is in scope.
4. **Refresh Qwen-Image-Edit calibration** with Edit-2511 + Lightning LoRA — large community adoption signal (≥600K combined downloads), better multi-person consistency than baseline.
5. **Confirm baseline drift**: `build_*` count is 77 in this clone vs. 73 documented baseline — 4 new builders since 2026-05-13. Worth a sanity check against the local Spellcaster repo's HEAD.

## Run metadata

- Repo HEAD: `5ab033f` (`chore: complete burial -- remove live antenna/ on main`)
- `build_*` count: **77** (baseline: 73 — drifted +4)
- WebSearch calls: **14** (cap: 25)
- WebFetch calls: **0** (skipped — search snippets were sufficient)
- HF MCP calls: **5** (4 hub_repo_search + 1 paper_search)
- `tools/upgrade_research.py`: **ABSENT** from cloud checkout — produced digest from WebSearch + HF MCP only, per section 10 fallback. Operator should investigate whether the tool was removed or never landed in `main`.
- Next scheduled run: **2026-06-26** (~48 h)
