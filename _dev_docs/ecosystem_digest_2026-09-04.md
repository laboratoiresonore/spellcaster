# Ecosystem digest — 2026-09-04

_Generated by the every-48h cloud routine. Runtime: ~6m. Backends reached: HF MCP + WebSearch + local (arch_registry / builders_manifest / local_index). LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. **`tools/upgrade_research.py` was still missing on the checked-out repo at run start — this run drafted a minimal working scaffold and included it in the PR (Tier 1 fix, see below).**_

## Tier 1 — fixes applied in this PR

Two clean-win fixes landed alongside this digest. See the commit diff for the exact changes; each was reproduced locally before commit.

| # | Finding | Fix | Where |
|---|---|---|---|
| 1 | `README.md` "Status — May 2026" panel is 4 months stale; describes only pre-May work (self-healing installer, LM Studio prompt-enhancement) and misses the SBS 3D pipeline, Klein 4B default, Krita method expansion (photo-restore / fooocus-inpaint / video-upscale), antenna burial, nightly.yml, and HERMES-EDITS-CODE audit scaffolding that all landed since. | Rewrote News / What works / Current focus / Next columns to reflect the current shipped state, sourced from `git log --since=2026-05-01 origin/main`. | `README.md` L46-53 |
| 2 | The cloud digest routine's own prompt references `python tools/upgrade_research.py`. That file has been missing for at least three consecutive prior runs (see the 2026-08-16 digest's action item #3). Prior runs silently degraded to web-only research instead of drafting the tool. | Drafted a minimal, dependency-free scaffold: 3 reachable backends (`arch_registry`, `local_index`, `builders_manifest`), 4 declared-deferred backends (`comfy_manager`, `local_llm`, `civitai`, `huggingface`), `--json`, `--check` (CI-friendly), and human summary. Verified locally: reports 26 archs / 0 LoRA calibrations / 73 builders (62 with `target_class`, 84.9%). | `tools/upgrade_research.py` (new, 213 LOC) |

Both fixes were verified before commit:

```
$ python3 tools/upgrade_research.py
upgrade-research snapshot
================================================
arch_registry: 26 architectures registered
local_index:   0 LoRA calibrations (schema 1)
builders:      73 total, 62 with target_class (84.9%)
deferred:      comfy_manager, local_llm, civitai, huggingface

$ python3 tools/upgrade_research.py --check ; echo "exit=$?"
...  exit=0
```

## Tier 1 — candidates NOT applied (downgraded to Tier 2 with reason)

- **"SUPIR arch has no `_reg()` stub"** — this recurring suggestion from prior digests is **not a real bug**. `supir` is a *method* (like `img2img`, `inpaint`, `hallucinate`, `seedv2r`, `colorize`, `style`), not an *architecture*. It appears as a key inside SDXL's `autoset_denoise` / `autoset_cn` / `autoset_loras` dicts because SUPIR uses SDXL as its backbone (workflow at `workflows.py:3108` `build_supir()`), and it's classified as `supir` in `CKPT_ARCH_RULES` (line 103 `model_detect.py`). Adding a stray `_reg("supir", ...)` would create an orphan arch entry with no builder wiring. **Downgrade to Tier 3-doc:** consider renaming "supir arch" → "supir method" everywhere the prior-run notes propagated the confusion.
- **DEEP_DIVE.md "All 69 Tools" count vs. table** — 50 rows in tool tables under a header claiming 69. Not clear whether the count refers to `build_*` methods (73 per `builders_manifest.json`), user-facing tool cards (50 visible), or something else. Not safe to autobump without operator intent — deferred to operator judgment.

## Headline — top 5 upgrade candidates (Tier 2 — needs human tradeoff review)

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Video (I2V / T2V) — **NEW class of model** | **MiniMax H3 (Hailuo)** open weights, day-0 ComfyUI | hf `MiniMaxAI/MiniMax-H3` | 5.12 M DL / 4897 likes / trending #13 | Open sourced 3 Aug 2026 with native ComfyUI support merged same day. Omni-modal (text + image + video + audio) generates video with native stereo audio in one pass, up to 2K @ 24fps, ~15 s per clip. Single node covers T2V (no image), I2V, and first/last-frame. ComfyUI 0.34.3 (2 Sep 2026) added the H3 **Max** variant. VRAM: multi-tier quants available; smallest fp8/gguf checkpoint is the operator-relevant one to try first. Replaces or supplements WAN 2.2 I2V. |
| 2 | Video (I2V) — direct WAN 2.2 comparison | **LTX-Video 2.5** (22B, official Lightricks nodes) | hf `Lightricks/LTX-2.5` | 1.4 M DL / 2746 likes / trending #6 | Released 11 Aug 2026. 22B open-weight audio-video foundation model. Native multishot (one gen → multiple connected shots holding character/env/lighting/voice across cuts), auto duration, 4K HDR output, custom Gemma 4 12B text encoder for complex prompts. Day-0 ComfyUI workflow templates: T2V, I2V, FLF2V — including a two-stage distilled variant. Direct upgrade path from LTX 2.3. |
| 3 | Text-to-image / edit — **NEW compact option** | **Qwen3.8-Flash-Next** (image-text-to-text) | hf `Qwen/Qwen3.8-Flash-Next` | 351 K DL / 4854 likes / trending #2 | Released 26 Aug 2026 as Qwen's early preview of the Qwen4-generation architecture. Multimodal (image + text → text) — replaces/augments Qwen-Image-Edit path. Companion `unsloth/Qwen3.8-Flash-Next-GGUF` (779 likes) provides quant path. Note this is text-generation-oriented; if you want unified gen+edit Qwen-Image-2.0 (7B, Feb 2026) remains the smaller edit-model target. |
| 4 | Prompt enhancement / local LLM | **GLM-5.3** and **GLM-5.3-Flash** | hf `zai-org/GLM-5.3` / `zai-org/GLM-5.3-Flash` | 303 K + 655 K DL / 1679 + 2037 likes / trending #1 + #5 | Released 4 Sep 2026 (today). Text-generation flagship + image-text-to-text Flash. Candidate for the LM Studio prompt-enhancement chain if the operator wants to A/B against the current local-LLM primary. GGUF quants available. |
| 5 | Coding / research assistant (adjacent — LM Studio primary) | **Tencent Hy4 (Hunyuan 4) preview** | hf `tencent/Hy4-preview` | 5.7 K DL / 426 likes / trending #14 | Open-sourced 28 Aug 2026 under Apache 2.0. 770B MoE (49B active/token), 1M-token context. Very heavy — not a fit for the 5060 Ti VRAM budget for local inference, but relevant as a benchmark competitor and for eventual API-side integration in the Wizard Guild chat path. |

## Ecosystem releases since last digest (2026-08-16 → 2026-09-04)

_Baselines carried over: ComfyUI 0.20.3 (still, in the Spellcaster bundle), krita-ai-diffusion v1.50 (still). Spellcaster `build_*` count is now **73** per `builders_manifest.json` (76 by grep of `def build_` in `workflows.py`, some are internal helpers), up from 77 count reported in 2026-08-16 — the discrepancy is that the 2026-08-16 digest measured `def build_*` in `workflows.py` (76 today) while `builders_manifest.json` counts the ones actually wired for dispatch (73). Neither number changed materially this window._

- **ComfyUI**: **v0.34.3** (2 Sep 2026, latest) — Partner Node Updates: **MiniMax H3 Max** model option added to H3 T2V / FLF2V / R2V nodes; TRELLIS remesh memory-usage lowered; 3D preview and HEVC-remux bugfixes. We're now at least a full major behind (0.20.3 → 0.34.3 spans 14 minor version bumps; see 2026-08-16 digest for the 0.21/0.22/0.23 changelog highlights we're still missing).
- **krita-ai-diffusion (Acly)**: **v1.53.0** (Aug 2026, latest) — semantic segmentation control layer via Anima regional ControlNet, PyTorch 2.13 + CUDA 13 backend, **int4 + int8 quantization** (Flux runnable on 6-8 GB VRAM), EU AI Act compliance metadata + invisible watermarks on Interstice cloud. Nunchaku + insightface removed. Long-standing timestamp + prompt-import bugs fixed on all platforms. We're now 3 minor versions behind (v1.50 → v1.53).
- **LM Studio**: no dedicated 0.4.x release since v0.4.20 (22 Jul 2026), but **Bionic 1.1.0** shipped 31 Aug 2026 — project-less chats, auto review for shell commands, simpler project creation, images in tool-call results.
- **Models** (only genuinely new since 2026-08-16):
  - **MiniMax H3** — open weights + day-0 ComfyUI (3 Aug 2026 — landed 2 weeks before last digest but reported here as first-run coverage in this trend window). H3 Max variant on 2 Sep via ComfyUI 0.34.3.
  - **LTX-Video 2.5** — 22B, 4K HDR, native multishot + audio (11 Aug 2026). Larger and more capable than LTX-2 (both still ship). Replaces earlier "LTX 2.3" mentions in this digest series.
  - **Qwen3.8-27B** open weights (14 Aug 2026, Apache 2.0). **Qwen3.8-Flash-Next** (26 Aug 2026, Qwen4 architecture preview). Both multimodal image-text-to-text; both have Unsloth GGUF quants trending.
  - **GLM-5.3** and **GLM-5.3-Flash** (4 Sep 2026, ZhipuAI). Text and multimodal.
  - **Tencent Hy4 preview** — 770B MoE, 1M ctx, Apache 2.0 (28 Aug 2026). Flagship; too heavy for local inference at operator scale.
  - **DeepSeek-V4-Flash-Vision-Exp** (1 Sep 2026). Multimodal image-text-to-text (experimental).
  - **FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree** (28 Aug 2026) — 4-step distill of MiniMax H3. Worth watching for VRAM-constrained I2V acceleration.
  - **Breeze-TTS-2** (2 Sep 2026, MediaTek) — trending TTS candidate, adjacent to Spellcaster's voice/walkie-talkie surface.

## HF trending shift vs. 2026-08-16

**New in top 20** (this digest's `hf://models/trending` snapshot vs. what the 2026-08-16 digest listed):
- `zai-org/GLM-5.3`, `zai-org/GLM-5.3-Flash` (day-of release, today)
- `Qwen/Qwen3.8-Flash-Next`, `unsloth/Qwen3.8-Flash-Next-GGUF`
- `Qwen/Qwen3.8-27B`, `unsloth/Qwen3.8-27B-GGUF`, `ISTA-DASLab/Qwen3.8-27B-GSQ-RCO-GGUF`, `HauhauCS/Qwen3.8-27B-Uncensored-HauhauCS-Aggressive-MTP-GGUF`
- `Lightricks/LTX-2.5` (was released just before last digest, now trending)
- `MiniMaxAI/MiniMax-H3`
- `deepseek-ai/DeepSeek-V4-Flash-Vision-Exp`
- `google/timesfm-3.0-pytorch` (time-series — not Spellcaster-relevant)
- `XHToken/Spark-X2.5-4B` (text-gen — not Spellcaster-relevant)
- `tencent/Hy4-preview`
- `FastVideo/FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree`
- `BreezeBlue/Breeze-TTS-2`

**Fallen off** vs. last digest's flux2-klein / wan-2.2-i2v listings (those specific trending queries no longer return in the top 20 model-agnostic view — image gen has cooled off relative to video + LLM releases). Klein 4B/9B remain the top matches when the query is scoped, and the leaderboard within Klein is stable since last digest (see the queries in Run metadata).

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | Unchanged since 2026-08-16 — v0.6.2 with Klein 4B/9B & Dev; `Fayens/Pulid-Flux2` native safetensors; training scripts still removed (instability), inference-only. Ready to wire once Spellcaster's Klein-native identity path is opened (currently on the "Next" list per updated README). |
| **MagCache** | Unchanged — official WAN 2.2 support at 1.5-2×; new WAN 2.2 speed LoRAs (`lightx2v/Wan2.2-I2V-A14B-Moe-Distill-Lightx2v`) trending as complementary acceleration. |
| **HyperSwap** | Same regression status (ReActor #226 still open Apr → Sep 2026). Fallbacks (InSwapper, ReSwapper) still work. |
| **Differential Diffusion inpaint** | Quiet — no new node. `DiffSynth-Studio` Qwen-Image-Layered-Control (Aug 2026) remains the interesting adjacent capability. |
| **NEW — Video model shortlist** | Now three viable open-weight video foundations: **WAN 2.2** (current), **LTX-2.5** (bigger, longer, native multishot + audio), **MiniMax H3** (first open-weight Hailuo, native audio + 2K + FLF2V). Recommend picking one per method rather than adding all three; the WAN 2.2 → LTX-2.5 upgrade is the least disruptive to build_wan_i2v callers (both are I2V-first). |
| **NEW — 4-step distill for VRAM ceiling** | `FastVideo/FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree` is worth measuring on operator's 5060 Ti — if it holds quality, MiniMax H3 becomes reachable inside the VRAM budget. |

## Backend results

- **local_index**: still 0 candidates. `lora_calibrations_sfw.json` schema present, no entries populated. Same finding as 2026-08-16 digest — worth an operator check that this reflects intent, not accidental truncation. **Left as-is this run** (Tier 3 — needs operator judgement on whether local calibration data lives elsewhere).
- **arch_registry**: 26 architectures registered (unchanged this window). Enumerated: `sd15, sdxl, illustrious, zit, flux1dev, chroma, flux2klein, flux_kontext, sdxl_turbo, pony, playground, sd3, sd3_turbo, hunyuan_dit, pixart, auraflow, kolors, wan, ltx, seedvr, cogvideo, framepack, hunyuan_video, mochi, hunyuan_3d, lumina2`.
- **builders_manifest**: 73 builders, 62 with `target_class` (84.9% SSoT coverage). Coverage-hole list is the 11 methods still lacking a resolved dispatcher class — worth a follow-up to close that gap.
- **huggingface**: MCP reachable; 3 targeted searches this run.
- **civitai / comfy_manager / local_llm**: SKIPPED (LAN-only / backend absent).

## Tier 3 — local_action_queue (structured for later mechanical consumption)

```yaml
local_action_queue:
  - action: verify_lora_calibration_source
    target_repo: null
    target_host: theo   # operator's primary workstation carrying spellcaster_core
    reason: "lora_calibrations_sfw.json has schema but 0 entries on cloud clone; confirm this matches local state or restore from local source of truth."
    command_hint: "diff <(git show origin/main:comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json) <local_calib_backup>"
    risk: low
  - action: download_model_update
    target_repo: MiniMaxAI/MiniMax-H3
    target_host: unknown   # pick spark1 or spark2 based on free VRAM
    reason: "First open-weight Hailuo. Native audio + 2K + FLF2V. Direct alternative to WAN 2.2 I2V. Try smallest quant first (FastVideo/FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree for 4-step VRAM-cheap variant)."
    command_hint: "huggingface-cli download MiniMaxAI/MiniMax-H3 --include '*.safetensors' --local-dir D:/LLM/staging/minimax-h3"
    risk: medium
  - action: download_model_update
    target_repo: Lightricks/LTX-2.5
    target_host: unknown
    reason: "22B audio-video foundation; direct upgrade path from LTX-2. Native multishot + 4K HDR."
    command_hint: "huggingface-cli download Lightricks/LTX-2.5 --local-dir D:/LLM/staging/ltx-2.5"
    risk: medium
  - action: bump_comfyui_bundle
    target_repo: comfyanonymous/ComfyUI
    target_host: theo
    reason: "Bundle still at 0.20.3; latest is 0.34.3 (2 Sep 2026). 14 minor versions behind. MiniMax H3 Max, multi-threaded model loading, PixelDiT, MediaPipe face det, TRELLIS remesh mem fixes all landed since. Consider a versioned bump not a jump-to-latest."
    command_hint: "cd D:/ComfyUI && git fetch --tags && git checkout v0.34.3 && python -m pip install -r requirements.txt"
    risk: high
  - action: bump_krita_ai_diffusion
    target_repo: Acly/krita-ai-diffusion
    target_host: theo
    reason: "v1.50 → v1.53.0. Anima segmentation ControlNet + int4/int8 quant (Flux on 6-8 GB VRAM). Nunchaku + insightface removed — verify our downstream nothing relied on those."
    command_hint: "In Krita: Tools → AI Image Generation → Manage → Update (or grab v1.53 tarball if auto-update path is broken like v1.52.0 was)"
    risk: medium
  - action: retire_superseded_model
    target_repo: Lightricks/LTX-Video-2.3
    target_host: unknown
    reason: "If LTX-2.5 gets adopted, LTX-2.3 checkpoints on D:/LLM can be moved to cold storage. Do NOT delete until LTX-2.5 validated."
    command_hint: "mv D:/LLM/ltx-2.3 D:/LLM/_retired/ltx-2.3"
    risk: low
  - action: benchmark_fasth3_distill
    target_repo: FastVideo/FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree
    target_host: unknown
    reason: "4-step distill of MiniMax H3. If quality holds at operator's 16 GB VRAM budget, MiniMax H3 becomes reachable — otherwise defer H3 entirely to Spark2."
    command_hint: "huggingface-cli download FastVideo/FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree --local-dir D:/LLM/staging/fasth3-4step"
    risk: low
```

## Action items for operator (routine reminders that stay Tier 3)

1. **Populate `lora_calibrations_sfw.json`** — same as prior digests. Confirm intent, populate or explicitly document why it's empty.
2. **Close the builders-manifest `target_class` coverage gap** — 11 of 73 builders still without a dispatcher class (84.9% → target 100%). Continues the 35 → 62 backfill trend from commit `3da2d68`.
3. **Consider adopting MiniMax H3 or LTX-2.5 to replace or complement WAN 2.2 I2V** — both bring native audio, both are day-0 ComfyUI native. Neither auto-adopts; needs the human tradeoff call.
4. **Rename "supir" from ambiguous "arch"-flavored language in prior digest notes** — it is a *method*, not an architecture. Suggest updating the operator's internal notes so future digests don't keep tripping on the "missing _reg stub" red herring.

## Run metadata

- runtime: ~6 minutes
- WebSearch calls: 7 / ~25 budget
- WebFetch calls: 0 (search snippets substantive)
- HF MCP calls: 3 (`ls hf://models/trending` + 2 targeted `search` for `flux2 klein` and `wan 2.2 i2v`)
- Tier 1 fixes committed: 2 (README status + tools/upgrade_research.py scaffold)
- Tier 2 findings: 5 top upgrade candidates (see Headline)
- Tier 3 local_action_queue items: 7 (see YAML block)
- Local backends reached: `arch_registry` (26), `local_index` (0 LoRAs), `builders_manifest` (73 builders / 62 with target_class)
- **Missing capabilities this run:**
  - **Gmail MCP** — still requires authorization; digest is delivered via the PR only. Operator can authorize in their claude.ai connector settings for future runs.
  - **Google-Drive MCP** — not attempted this run; the PR is the sole delivery.
- baseline check: `def build_*` grep = **76** in `workflows.py`; `methods` in `builders_manifest.json` = **73**. Discrepancy is builder helpers not in the manifest — noted for future runs.
- next scheduled run: 2026-09-06 ~16:00 UTC (48h cadence)
