# Ecosystem digest — 2026-07-12

_Generated by the every-48h cloud routine. Runtime: ~4m (research phase). Backends reached: HF MCP + WebSearch (WebFetch skipped — WebSearch summaries were sufficient). LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` NOT PRESENT in this checkout — see Run metadata below._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Flux family base | **FLUX.2 Klein 4B** (black-forest-labs) | hf | 366K dl / 786 likes | Fits 13 GB VRAM (inside 16 GB ceiling); unified generation + editing in one checkpoint; 4-step distilled + 50-step base variants; drop-in replacement path for the current Flux-family `build_*` methods. |
| 2 | Compact fast base | **Z-Image-Turbo** (Tongyi-MAI) | hf/web | leaderboard #1 open-source (Dec 2025) | 16 GB VRAM native, 8-NFE sub-second inference; strong candidate for a lightweight-fast `build_*` alongside heavier Flux2. Companion `Z-Image-Edit` covers editing. |
| 3 | Unified gen + edit | **Qwen-Image-2.0** (7B) | web | #1 AI Arena in both T2I and edit | Feb-2026 launch, drops from 20B→7B while gaining native 2K + professional typography; single-model gen+edit path. Fits well within 16 GB. |
| 4 | Edit / identity refresh | **Qwen-Image-Edit-2511** | web | rolling update | Improved character consistency, multi-person group-photo identity, built-in community LoRA support — a natural upgrade to existing edit surfaces. |
| 5 | Face identity on Flux2 | **PuLID-Flux2 v0.6.2** (iFayens) | web/hf | v0.6.2 (first working PuLID adapter for FLUX.2) | Follow-up to the shipped PuLID-Flux2 batch; now supports Klein 4B / 9B and Dev. Direct pairing with candidate #1. |

FLUX.2 Klein 9B (~29 GB VRAM) is **heavy** — flag as exclusive-load only; not recommended for the 16 GB ceiling. Klein 4B is the operator-fit variant.

## Ecosystem releases since last digest

- **ComfyUI**: baseline **0.20.3** → head **0.23.0** (Jun 2026). Intermediate landings: 0.21.0 (May, auto-regressive video + Gemma4 text; **removed IPEX support** — breaking), 0.21.1 (May 13), 0.22.0 (May 20, MoGe + Stable Audio 3), 0.23.0 (Jun 1, multi-threaded model loading + MediaPipe face detection + NVIDIA PixelDiT + Microsoft Lens). **Three-version drift.**
- **krita-ai-diffusion**: baseline **v1.50** → head **v1.52.1** (Jun 30, 2026). v1.51 added Anima control layers + ROCm managed-install for AMD; v1.52.0 added Krea 2 support (auto-update was broken, manual install required); v1.52.1 fixed managed-server connectivity.
- **LM Studio**: 0.4.13 (May 13, MLX-engine v1.8.1 + parallel vision predictions) → 0.4.14 (May 29, MTP Speculative Decoding stable) → 0.4.16 (Jun 8, LM Link out of waitlist + iOS "Locally" app) → 0.4.17/18/19 (Jun 25 – Jul 7, mermaid full-screen + Engine Protocol on by default + `/reasoning` command).
- **Models — substantively new since May baseline:**
  - **FLUX.2 Klein** (Jan 14, 2026): 4B and 9B, distilled + base, FP8 quants. See top-5.
  - **WAN**: skipped 2.3/2.4 entirely — line went 2.2 → 2.5 (Sep 2025, native audio-visual, 1080p/24fps) → **2.6 (Dec 2025)** which builds on the open-source 2.2 stack and adds ref-to-video + image edit + video extend → **2.7 planned Mar 2026** (9-grid I2V + instruction editing).
  - **LTX**: LTX-2 (Jan 6, 2026, 4K + native audio) → **LTX-2.3 (Mar 5, 2026)**, 22B params, rebuilt VAE for sharper output.
  - **Z-Image family** (Jan 27, 2026): Z-Image, Z-Image-Turbo, Z-Image-Omni-Base, Z-Image-Edit. See top-5.
  - **Qwen-Image-2.0** (Feb 10, 2026): 7B unified; **Qwen-Image-Edit-2511**: identity + multi-person consistency, community LoRA support.

## HF trending shift

_Skipped — first digest, no prior digest to diff against._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **HIT** — `iFayens/ComfyUI-PuLID-Flux2` v0.6.2 adds Klein 4B/9B/Dev support. Verify shipped-May-2026 version and consider version-bump if stale. |
| MagCache | Stable — NeurIPS 2025 baseline (`Zehong-Ma/MagCache`). No new speedup line since baseline; ~2.68–3.0× on WAN 2.1 remains SOTA. Confirm compatibility if migrating to WAN 2.6/2.7. |
| HyperSwap | Stable — no major 2026-dated line beyond baseline (`facefusion.io` HyperSwap 1a/1c). Version pin still valid. |
| Differential Diffusion inpaint | Stable — mature technique; no 2026 revision surfaced. Continues to be the recommended soft-inpaint path in ComfyUI. |

## Backend results

- local_index: **0** baseline candidates (`lora_calibrations_sfw.json` is empty stub — `"loras": {}`).
- huggingface: **10** proposals surfaced via HF MCP `hub_repo_search` on `flux2-klein` (real trending scores 5–58).
- civitai: **SKIPPED** — `tools/upgrade_research.py` not present in this checkout, so civitai backend could not be invoked.
- comfy_manager: SKIPPED (LAN-only).
- local_llm: SKIPPED (LAN-only).

## Action items for operator

1. **ComfyUI is three minor versions behind** (0.20.3 → 0.23.0). Bump the bundled ComfyUI; expect the IPEX removal in 0.21 to be the only breaking change worth pre-checking (no Intel Extension for PyTorch in the current bundle → no-op). Multi-threaded model loading in 0.23 is a real cold-start win.
2. **krita-ai-diffusion v1.50 → v1.52.1** — Krea 2 support is downstream-visible; ROCm managed install unblocks any AMD scaffolding you were deferring. Grab v1.52.1 directly (v1.52.0 has a broken auto-update path).
3. **Land FLUX.2 Klein 4B as a candidate `build_*`** — fits the 16 GB VRAM ceiling comfortably (~13 GB), unifies gen + edit, and unlocks the PuLID-Flux2 v0.6.2 identity path. Track FLUX.2 Klein 9B as heavy-only (~29 GB, exclusive-load; not recommended for 16 GB).
4. **`tools/upgrade_research.py` is missing** from this checkout — restore it (or land the initial version) so future digests can pull local_index + civitai signal, and so the digest can cross-reference `lora_calibrations_sfw.json` recipes against ranked candidates. Empty stub in that JSON also means the local_index backend has nothing to score against once restored.

## Run metadata

- runtime: ~4m for the research phase; total including write + deliver targeted <10m
- WebSearch/Fetch call count: 12 WebSearch, 0 WebFetch (summaries were sufficient), 2 HF MCP `hub_repo_search`
- next scheduled run: 2026-07-14 ~16:07 UTC (48h cadence)
- routine caveats this run:
  - `tools/upgrade_research.py` NOT PRESENT (see action item #4) — no JSON/MD artefact under `_dev_docs/upgrade_research/` for this cycle
  - Gmail MCP is present but not authenticated in this cloud session → email delivery skipped; digest reaches operator via git + Google Drive
  - build_* count observed: **77** (baseline 73; +4 drift, non-alarming — expected minor divergence between operator's local repo and the cloud clone)
