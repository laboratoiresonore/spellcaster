# Ecosystem digest — 2026-07-26

_Generated by the every-48h cloud routine. Runtime: ~30 min. Backends reached: HF MCP + WebSearch + WebFetch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` is absent from the repo — its role was replaced by direct HF MCP `hub_repo_search` queries (see backend results below)._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Likes | Why |
|---|---|---|---|---|---|
| 1 | text-to-image (new arch) | **microsoft/Mage-Flow-Turbo** | hf | 1.1K / 62 | Brand-new 4B rectified-flow model (released 21 Jul 2026, MIT license). Native-resolution 512→2048px at arbitrary aspect ratios in a single checkpoint. 4-step turbo variant. Reported competitive with Qwen-Image (20B) and FLUX.2 (32B) on GenEval / CVTG-2K despite ~1/5 the params. Fits comfortably in 16GB VRAM. |
| 2 | text-to-image + edit unified | **microsoft/Mage-Flow** (RL-aligned base) | hf | 1.4K / 317 | Same 4B backbone, RL-tuned for prompt-following + text rendering. Highest trending score (308) in text-to-image this week. Unifies gen + edit in one pipeline (relevant if we want to collapse `build_flux_edit_*` + `build_flux_txt2img_*` calibration workload). |
| 3 | flux-family t2i (existing) | **black-forest-labs/FLUX.2-klein-4B** | hf | (BFL flagship) | Official BFL 4B checkpoint — ~13GB VRAM (fits 16GB), sub-second inference, unified gen + multi-ref edit. First-party alternative to community Flux2Klein forks. PuLID-Flux2 v0.6.2 now supports Klein 4B/9B natively (May 2026), unlocking identity-lock on this arch. |
| 4 | qwen-image-edit (existing) | **Qwen/Qwen-Image-Edit-2511** + `lightx2v/Qwen-Image-Edit-2511-Lightning` | hf | 226K + 306K / 1186 + 495 | Dec-2025 refresh of the Qwen-Image-Edit line already in Spellcaster. Lightning distill (LoRA-on-top) trims steps ~4x with acceptable quality. Drop-in for existing `build_qwen_edit_*` methods. Also worth noting: `fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA` (1482 likes) for camera-control edits. |
| 5 | z-image-turbo (existing) | **Tongyi-MAI/Z-Image** (foundation base, Jan 2026) | hf | (see [note](#z-image-base-note)) | Full-quality successor to the Z-Image-Turbo baseline (which is 6B params, distilled). Base model targets fine-tuning + downstream. Consider a "quality" tier next to the current "turbo" tier — same arch, minimal calibration migration. `alibaba-pai/Z-Image-Turbo-Fun-Controlnet-Union-2.1` (76K dl / 432 likes) is a further ecosystem hint. |

**Heavy-model flag (>13GB VRAM, exclusive-load on RTX 5060 Ti 16GB):**
- `FLUX.2-klein-9B` (~29GB): exclusive-load only, skip on this rig.
- WAN 2.6 / 2.7 family: 14B-class MoE, typically ≥16GB in fp8 — treat as heavy.
- LTX-2.3 22B distilled: heavy; see the MXFP8 quant (`Swarzox/LTX2.3-22b-distilled-video-only-MXFP8`) for a possible fit.

## Ecosystem releases since last digest

- **ComfyUI**: `v0.20.3` (baseline) → `v0.21.1` (13 May 2026, Partner Nodes + dtype fixes) → `v0.22.0` (20 May 2026, MoGe + Stable Audio 3 support) → `v0.23.0` (1 Jun 2026, multi-threaded model loading, MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens). **Three point releases behind — recommend bumping the bundled ComfyUI to 0.23.x.**
- **krita-ai-diffusion**: `v1.50` (baseline) → `v1.51.0` (31 May 2026, prompt-optional inpaint/outpaint, ROCm managed install) → `v1.51.1` (macOS/Linux managed-install fixes) → `v1.52.0` (28 Jun 2026, **Krea 2 support**, regional prompting in Anima, auto-updater regression fix) → `v1.52.1` (30 Jun 2026, server connect fix). **Recommend bumping baseline to v1.52.1.**
- **LM Studio**: `v0.4.20` released 22 Jul 2026 — introduces "Bionic" agent (Code + Work projects, Voxtral voice, LM Link over Tailscale, Secure Cloud w/ ZDR). Enterprise internal model endpoints + LM Link device-model support. Closed source; HN reports rough UX edges. Not directly relevant to Spellcaster's Comfy path, but noted for the LLM-delegate tooling.
- **Models (substantively new since prior touchpoint):**
  - **Flux 2 Klein**: Official BFL family stabilized — 4B / 9B / fp8 / kv variants all live. Third-party ecosystem exploded (Krita+ComfyUI configs, GGUF quants, PuLID-Flux2 v0.6.2 support). No successor "Klein v2" — v1 still current.
  - **WAN**: Now at 2.6 (Dec 2025) with 2.7 shipping Apr 2026 (2.7-Image Apr 1, 2.7-Video Apr 7). WAN 2.2 (Spellcaster baseline) is effectively 2 releases behind. 2.5 does native 4K T2V+I2V; 2.6 covers T2V/I2V/R2V + 2 image gens; 2.7 adds 9-grid I2V + instruction-based editing.
  - **LTX**: LTX-2 (6 Jan 2026) → LTX-2.3 (Mar 2026, 22B). Synchronized 4K video+audio in one pass, 50fps, native. Distilled/MXFP8 variants exist for consumer GPUs.
  - **Z-Image**: Z-Image (base) shipped 27 Jan 2026 alongside the existing Turbo. Controlnet Union, DPO, and realism LoRAs now mature.
  - **Qwen-Image-Edit**: 2511 refresh (17 Dec 2025) is now the current line, with Lightning distill + Multiple-Angles LoRA the two most-adopted add-ons.
  - **Krea 2** (Raw + Turbo, 18 Jun 2026): new closed-weights-but-open-checkpoint family; strong trending (200K+ dl on Turbo). Ecosystem LoRAs landing (`mgwr/M87`, `AliveAi/Krea-2-Edit-Outfit-Transfer`).
  - **NVIDIA Qwen-Image-Flash** (1 Jul 2026): DMD2 few-step distill of Qwen-Image with ModelOpt quantization — potentially the fastest path to Qwen-Image at Spellcaster's step budget.

## HF trending shift

_No prior digest exists — first run of this routine. Baseline trending snapshot recorded below; next run will diff against it._

Top 15 text-to-image on HF this week (trendingScore desc):
1. microsoft/Mage-Flow (308) — 21 Jul 2026
2. krea/Krea-2-Turbo (64)
3. microsoft/Mage-Flow-Turbo (62)
4. nvidia/Qwen-Image-Flash (43)
5. black-forest-labs/FLUX.1-dev (42) — perennial anchor
6. mgwr/M87 (37) — Krea-2-Turbo LoRA
7. AliveAi/Krea-2-Edit-Outfit-Transfer (32)
8. microsoft/Mage-Flow-Base (32)
9. ponpoke/flux2-klein-9b-uncensored-text-encoder (28) — flagged NSFW-adjacent, skip
10. krea/Krea-2-Raw (28)
11. black-forest-labs/FLUX.1-schnell (24)
12. sensenova/SenseNova-U1-8B-MoT-Infographic-V3 (19)
13. Tongyi-MAI/Z-Image-Turbo (16) — Spellcaster baseline
14. ideogram-ai/ideogram-4-fp8 (16)
15. wikeeyang/Flux2-Klein-9B-True-V3 (15)

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | v0.6.2 released, native Klein 4B/9B + Dev support, "improved face recognition, less artifacts, added native Klein weights (v1 & v2)." Active repo: `iFayens/ComfyUI-PuLID-Flux2`. Ready to graft onto Flux2Klein arch when we add it. |
| **MagCache** | Official WAN 2.2 support: 1.5–2× speedup. WAN 2.1: 2–3×. `Zehong-Ma/ComfyUI-MagCache` is upstream (NeurIPS 2025 paper). No 2.6/2.7 support yet — track. |
| **HyperSwap** | No specific 2026 upgrade beyond the `hyperswap_1c_256` model still recommended for quality/speed balance. Reactor-Node improvements (`ReActorFaceBoost`) continue. No breaking changes to react to. |
| **Differential Diffusion inpaint** | Widely adopted, stable node graph. Krita 1.51 shipped prompt-optional inpaint/outpaint that overlaps functionally — worth cross-referencing if we surface a Krita bridge. No new 2026 research paper. |

## Backend results

- `local_index`: **0** baseline candidates from `lora_calibrations_sfw.json` (the shipped file has an empty `"loras": {}` map — nothing to seed from until operator populates confirmed calibrations).
- `huggingface`: **6 targeted queries** via HF MCP `hub_repo_search` (text-to-image trending; flux 2 klein; wan 2.2 i2v; ltx video; z-image turbo; qwen image edit). Returned 75 unique repos combined; top-15 trending shown above.
- `civitai`: **SKIPPED** — no `tools/upgrade_research.py` present in the repo checkout; the civitai backend never fired. See "Action items" below.
- `comfy_manager`: SKIPPED (LAN-only)
- `local_llm`: SKIPPED (LAN-only)

## Action items for operator

1. **Bump ComfyUI 0.20.3 → 0.23.x** — three point releases available, includes multi-threaded model loading which is a straight win.
2. **Bump krita-ai-diffusion v1.50 → v1.52.1** — Krea 2 support + auto-updater regression fix (the fix release itself; safe to skip the .0).
3. **Evaluate Microsoft Mage-Flow (Turbo)** as a new lightweight text-to-image method. 4B / MIT / 16GB-friendly / July 22 checkpoints on HF. Highest single-week trending score in the category (308). If benchmarks hold up, it's a candidate to replace or complement Z-Image-Turbo for the "fast tier."
4. **Restore `tools/upgrade_research.py`** — the routine's spec assumes it exists, but it's not in this checkout. Either land the script (with `local_index`, `huggingface`, `civitai` backends) or update the routine prompt to remove the reference. Same story for the local `lora_calibrations_sfw.json` — it ships empty, so `local_index` will always be 0 candidates until first write.
5. **WAN 2.2 references in the codebase are 2 releases behind** — flag for future upgrade pass. Migration to 2.6/2.7 is a larger effort (14B-class, ~heavy on 16GB) but the capability jump (native 4K, instruction editing) is real.

## Run metadata

- runtime: ~30 min (soft, no precise clock available in the sandbox — `Date.now` policies aside, no wall stopwatch was recorded)
- WebSearch calls: 11
- WebFetch calls: 4 (2× 403 on hackernoon + microsoft.github.io, retrieved 2× successfully)
- HF MCP calls: 6× `hub_repo_search`, 1× `hub_repo_details`
- Google-Drive MCP: **UNAVAILABLE (token expired — re-auth needed via claude.ai connector settings)**
- Gmail MCP: **UNAVAILABLE (not authorized — same fix path)**
- Delivery channels used: git branch + PR only. Drive backup + email deferred until the operator re-auths those connectors.
- Baseline `build_*` count: **77** (spec expected ~73; +4 since 2026-05-13). Not massively different — noted, no action.
- next scheduled run: 2026-07-28 (48h)
