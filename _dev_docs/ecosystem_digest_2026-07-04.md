# Ecosystem digest — 2026-07-04

_Generated by the every-48h cloud routine. Runtime: ~5m. Backends reached: HF MCP + WebSearch. Note: `tools/upgrade_research.py` was not found in the cloned repo — see Run metadata. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Flux-family t2i / img-edit | `black-forest-labs/FLUX.2-klein-4B` | hf | 448.6K / 776★ | 4B unified gen+edit rectified-flow — fits ~13 GB VRAM (inside 16 GB ceiling), sub-second inference, direct replacement path for FLUX-family flows |
| 2 | WAN i2v (baseline WAN 2.2) | WAN 2.7 open-weight | HF (Alibaba) | released 2026-05-20 | Direct upgrade of baseline WAN 2.2 references; official open weights on HF under permissive license, big jump in visual quality + motion dynamics |
| 3 | Qwen-Image-Edit (baseline) | Qwen-Image-Edit-2511 | hf (Alibaba/Tongyi) | flagship img-edit | Improved consistency, character preservation, integrated LoRA, mitigated image drift — drop-in upgrade of the existing edit method |
| 4 | New arch — aesthetic-first t2i | Krea 2 Turbo (12.9B) | hf (krea) | open 2026-06-22 | Novel foundation model, 2K in ~2s via 8-step distilled Turbo, moodboard/style-ref conditioning; commercial-friendly community license — heavy (12.9B); check VRAM |
| 5 | LTX-family t2v | LTX-2.3 (22B) | hf (Lightricks, Apache-2.0) | released 2026-03-05 | Native 4K + synchronized audio in one pass, new VAE noticeably sharper — largest arch here; **heavy**, likely exclusive-load on the 16 GB ceiling |

## Ecosystem releases since last digest

- **ComfyUI**: v0.23.0 (2026-06-01), v0.22.0 (2026-05-20), v0.21.1 (2026-05-13). Baseline 0.20.3 → 0.23.0 is a three-feature-release gap. 0.23 adds multi-threaded model loading + MediaPipe face detection + NVIDIA PixelDiT support. ([changelog](https://docs.comfy.org/changelog))
- **krita-ai-diffusion**: v1.52.1 (2026-06-30). Baseline v1.50 → v1.52.1 covers v1.51 (inpaint control model requirement at full strength, ROCm managed install), v1.52 (Krea 2 support + Anima regional prompting + auto-update fix). ([release notes](https://github.com/Acly/krita-ai-diffusion/releases))
- **LM Studio**: 0.4.18 (2026-06-26). Recent jumps: 0.4.13 shipped mlx-engine v1.8.1 + parallel vision predictions (Qwen 3.5/3.6, Gemma 4); 0.4.14 added MTP speculative decoding; 0.4.16 introduced companion mobile app "Locally" + removed LM Link waitlist. ([changelog](https://lmstudio.ai/changelog))
- **Models — substantively new since 2026-05-13**:
  - **FLUX.2 Klein**: `black-forest-labs/FLUX.2-klein-{4B,9B,base-4B,base-9B,4b-fp8,9b-fp8,9b-kv-fp8}` (all 2026-01-14 initial, kv-fp8 variant 2026-03-09). 4B 448K / 9B 208K downloads. Unified gen+edit rectified flow.
  - **WAN 2.7**: open weights on HF, 2026-05-20. Big upgrade over 2.6 (mid-2026-Q1 rev).
  - **LTX-2.3**: Lightricks, 2026-03-05, Apache-2.0, 22B, 4K + audio.
  - **Z-Image (base)**: Tongyi-MAI, 2026-01-27. 24 GB VRAM base variant + Turbo already-existing 8-step distilled. Also **Z-Image-Omni-Base** (gen+edit) and **Z-Image-Edit** variants.
  - **Qwen-Image-2.0**: 2026-02-10, 7B (down from 20B), native 2K, unified gen-edit, currently #1 on AI Arena.
  - **Qwen-Image-Edit-2511**: consistency-focused refresh.
  - **Krea 2 Raw + Turbo**: 12.9B, open weights 2026-06-22, custom commercial-friendly license.

## HF trending shift

Skipped — first digest run, no prior baseline to diff against. Will populate from run 2 onward.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | Active. `iFayens/ComfyUI-PuLID-Flux2` at v0.6.2 with support for FLUX.2 Klein 4B/9B + Dev — pairs directly with the top-1 candidate above; May 2026 commits. |
| MagCache | NeurIPS 2025 accepted paper. Native Wan 2.2 support (1.5–2× speedup) and Qwen-Image support (1.75× accel, 2025-11-23) shipped since baseline. Track any Wan 2.7 / FLUX.2 Klein plug-ins. |
| HyperSwap | Stable — ReActor + FaceFusion nodes cover HyperSwap 1a/1b/1c 256. 1c recommended quality/speed pick. No new tier surfaced this run. |
| Differential Diffusion inpaint | Stable — well-integrated in ComfyUI (Gaussian Blur Mask + Differential Diffusion + Inpaint Model Conditioning trio). No new drops surfaced this run. |

## Backend results

- local_index: 0 baseline candidates — `lora_calibrations_sfw.json` ships with an empty `loras: {}` dict (schema_version 1). Baseline for future digests: file exists, currently empty.
- huggingface: 5 direct arch-family candidates surfaced (top-5 above) + 10 FLUX.2 Klein variants enumerated via `hub_repo_search`.
- civitai: SKIPPED — `tools/upgrade_research.py` absent in cloud clone, could not invoke backend.
- comfy_manager: SKIPPED (LAN-only, per routine spec).
- local_llm: SKIPPED (LAN-only, per routine spec).

## Action items for operator

1. **ComfyUI bump 0.20.3 → 0.23.0** (three releases behind, includes multi-threaded loader + PixelDiT). Priority: high — baseline drift is now large enough that new nodes/APIs may need calibration checks.
2. **FLUX.2 Klein 4B evaluation** — 13 GB VRAM footprint sits inside the 16 GB ceiling, unified gen+edit; if adopted, PuLID-Flux2 v0.6.2 is the identity-lock companion.
3. **WAN 2.2 → WAN 2.7 open-weight swap** — direct upgrade path for existing WAN references in the codebase.
4. **Restore `tools/upgrade_research.py`** to the cloud-visible tree (or teach the routine to skip it cleanly) — the local_index / civitai backends were unreachable this run and future digests will keep losing that signal until it's back.

## Run metadata

- runtime: ~5m
- WebSearch calls: 11
- WebFetch calls: 0 (search snippets were sufficient)
- HF MCP calls: 2 (`hub_repo_search` × 2)
- GitHub MCP calls: 2 (both denied — session is scoped to `laboratoiresonore/spellcaster` only, so ComfyUI + krita-ai-diffusion release lookups fell through to WebSearch)
- Notable variance from baseline: `spellcaster_core.workflows` reports 77 `build_*` functions vs. baseline 73 (+4 since 2026-05-13). Local repo has grown; not a bug.
- Notable failure: `tools/upgrade_research.py` not present in the cloud clone (routine spec assumes it exists). Digest produced from WebSearch + HF MCP only, per routine's fallback rule (§10).
- next scheduled run: 2026-07-06 16:08 UTC
