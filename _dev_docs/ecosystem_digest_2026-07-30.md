# Ecosystem digest — 2026-07-30

_Generated by the every-48h cloud routine. First run — no prior digest to diff against. Backends reached: WebSearch + Hugging-Face MCP. `tools/upgrade_research.py` is not present in this checkout; civitai + local_index backends were unreachable. LAN-only backends (comfy_manager, local_llm) deferred to the operator's local cron._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Notes | Why |
|---|---|---|---|---|---|
| 1 | Flux-family text-to-image | **FLUX.2-klein-4B** (`black-forest-labs/FLUX.2-klein-4B`) + GGUF/fp8 forks | HF | GGUF 199 dl, HDR-LoRA + Unity-Sentis + ternary-quant forks landing this week | The 4B variant is the practical successor to FLUX.1 for a 16 GB card — the 9B (~29 GB VRAM) is out of range. Ecosystem is now dense (LoRAs, quants, 1-step RDM). |
| 2 | Flux 2 few-step generation | **FLUX.2-klein-4B-1step-RDM-GGUF** (`novelaide/…` / base `epfl-vita/flux2-klein-1step-rdm`) | HF | Just dropped 2026-07-30 | One-step Recursive Distillation for Flux 2 Klein — if it holds up qualitatively, it collapses generation cost dramatically. Worth a probe workflow. |
| 3 | Qwen-Image-Edit builder | **Qwen-Image-Edit-2511** (prerelease uploads: `GuaiSax/…`, `neverstopcn/…`) | HF | Uploads 29-30 Jul; 20B, `QwenImageEditPlusPipeline` | Successor tag to the 2509 build; Alibaba doc pages last updated 2026-07-14/15 confirm active push. Heavy (20B) — flag for staged rollout, likely fp8/GGUF variants will follow. |
| 4 | Z-Image family (image gen) | **Tongyi-MAI/Z-Image** (undistilled foundation, Jan 27 2026) + **Z-Image-Omni-Base** (gen+edit) | HF / web | 6B — fits the 16 GB ceiling comfortably | Z-Image-Turbo (Nov 2025) benchmarks above Flux at 6B; the undistilled Z-Image and the omni variant unlock finer control and shared gen/edit surface. Prime candidate for a new `build_*` slot. |
| 5 | LTX video generation | **Lightricks/LTX-2.3** (22B, native 4K + synced audio, March 5 2026) | HF / web | HF port `PocketAiHub/PocketAI-Video-MLX-LTX-2.3` (26 Jul) shows quantization work in flight | Only open-weight model producing native 4K with audio in a single pass. **Heavy** (22B > 13 GB) — exclusive-load or wait for a distilled/quantised community fork. |

## Ecosystem releases since last digest

- **ComfyUI**: baseline **0.20.3** → three point releases shipped in the last 6 weeks. **v0.21.1** (May 13), **v0.22.0** (May 20, MoGe + Stable Audio 3), **v0.23.0** (Jun 1, multi-threaded model loading, MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens). Bundle bump strongly recommended.
- **krita-ai-diffusion (Acly)**: baseline **v1.50** → **v1.51.0** (Fill/Expand require inpaint control model → much better output, ROCm managed install for AMD), **v1.51.1** (macOS + Linux managed-install fixes), **v1.52.0** (basic Krea 2 support + Regions require plugin ≥1.51), **v1.52.1** (managed-server reconnect fix). Auto-update path was broken in the 1.51 → 1.52 jump — manual reinstall required to escape v1.51.
- **LM Studio**: **0.4.12** (Qwen 3.6 + MCP OAuth on Windows), **0.4.13** (mlx-engine 1.8.1, parallel vision predictions), **0.4.14** (stable MTP Speculative Decoding), **0.4.16** (**Locally** — companion iOS/iPad app). All 2026 releases.
- **Models — genuinely new:**
  - **Flux 2 Klein 4B / 9B / base variants** — full family shipped by black-forest-labs since baseline; 4B is the 16 GB-friendly target. 1-step RDM variant just dropped today (2026-07-30).
  - **WAN 2.6** (Dec 2025) — T2V/I2V/R2V + two image models, 15 s clips at 1080p / 24 fps. **WAN 2.7 planned March 2026** with 9-grid I2V and instruction-based editing (not yet shipped as of this run).
  - **LTX-2.3** (Mar 5, 2026) — 22B, native 4K + synchronized audio, portrait video up to 1080×1920, 20 s clips.
  - **Z-Image** (Jan 27, 2026) + **Z-Image-Omni-Base** — 6B, undistilled foundation and dual gen/edit variant to Turbo (Nov 2025).
  - **Qwen-Image-3.0** (Jul 21, 2026) — 4,500-token prompts, dense layout / infographic generation. Weights not published at announcement — track availability.
  - **Qwen-Image-Edit-2511** — successor to 2509 appearing on HF this week (community prerelease uploads).

## HF trending shift

_Skipped — first run, no prior digest to diff against._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | Actively maintained (`iFayens/ComfyUI-PuLID-Flux2` v0.6.2, `Fayens/Pulid-Flux2` weights on HF). No 2026 Flux 2-native breakthrough beyond the initial port. |
| **MagCache** | WAN 2.2 support landed August 2025 (1.5–2× speedup); **March 2026 update** added SenCache for Wan2.2 T2V + I2V — ~1.4× additional. Repo `Zehong-Ma/ComfyUI-MagCache`. |
| **HyperSwap** | Variants **1a / 1b / 1c** (speed / balanced / max realism) available via FaceFusion Labs; 256-resolution native. No new successor announced. |
| **Differential Diffusion inpaint** | Stable in ComfyUI core; 2026 incremental — **LanPaint KSampler** and Krea 2 inpaint hookup are the relevant new additions. |

## Backend results

- **local_index**: 0 candidates. `lora_calibrations_sfw.json` in the repo contains only `schema_version`/`notes`/`loras: {}` — no shipped calibrations yet, so nothing to seed comparisons from.
- **huggingface**: ~19 fresh assets surfaced this run via MCP (8 Flux 2 Klein items in last 7d, 6 Qwen-Image-Edit-2511/2509 items, 5 LTX-2.3 items).
- **civitai**: **SKIPPED** — `tools/upgrade_research.py` not in checkout, no MCP alternative.
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI bundle 0.20.3 → 0.23.0** — three point releases in six weeks, multi-threaded loader alone is a meaningful UX win. Check `MoGe` + `PixelDiT` node availability against Spellcaster's build_* set (currently 77, up from the 73 baseline — that already-widened surface may partly cover this).
2. **Bump krita-ai-diffusion v1.50 → v1.52.1**, but do it manually — the auto-update path was broken between 1.51 and 1.52.0 and a fresh managed install is the cleanest route. Krea 2 support in 1.52 is the marquee feature.
3. **Add a Flux 2 Klein 4B build recipe** (`build_flux2_klein_4b` or similar) using `black-forest-labs/FLUX.2-klein-4B` — the fp8 and GGUF variants keep it within the 16 GB ceiling. Watch the just-dropped 1-step RDM variant for a follow-up cheap-inference method.

## Run metadata

- runtime: ~10 min elapsed at write; delivery adds ~2–3 min
- WebSearch calls: **9** (well under the 25 combined cap)
- WebFetch calls: **0** — MCP hub_repo_search returned enough structured data that no page fetches were needed
- HF MCP calls: **6**
- `tools/upgrade_research.py`: **MISSING from repo** — noted per section 10 fallback; full digest still produced from WebSearch + HF MCP data
- Next scheduled run: **2026-08-01 ~16:08 UTC** (every 48 h)

---

_Generated by the [Ecosystem Digest cloud routine](https://claude.ai/code) on 2026-07-30._
