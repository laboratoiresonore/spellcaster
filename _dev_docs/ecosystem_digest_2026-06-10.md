# Ecosystem digest — 2026-06-10

_Generated by the every-48h cloud routine. Runtime: ~8m. Backends reached: HuggingFace MCP + WebSearch + WebFetch. `tools/upgrade_research.py` not found in repo — backend deferred (see Run metadata). LAN-only backends (`comfy_manager`, `local_llm`) deferred to operator's local cron._

_First run — no prior digest to diff against. The "HF trending shift" section is skipped this cycle and will populate next run._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Likes | Why |
|---|---|---|---|---|---|
| 1 | `build_klein_*` (FLUX.2-klein) | **FLUX.2-klein-9B-KV** (KV-cache optimized) | hf:`black-forest-labs/FLUX.2-klein-9b-kv` | (official BFL release) | Multi-reference editing accelerates: caches K/V from reference images during first denoise step, skips redundant compute on subsequent steps. Direct upgrade for klein multi-angle workflow shipped 2026-05-31 (b5107c2 / 13c5675). Klein already 13 GB VRAM-friendly — fits 16 GB ceiling. |
| 2 | New arch — text-to-image | **Ideogram-4** (fp8 / nf4 / GGUF) | hf:`ideogram-ai/ideogram-4-fp8` | 7.2K dl / 463 likes (trending #1) | Released 30 May 2026 with fp8 / nf4 / GGUF quantizations same week. Diffusers + flow-matching DiT. Highest trending score on the Hub right now (440); first open-weight release from Ideogram. Worth a `build_ideogram_*` arch scaffold. |
| 3 | `build_qwen_image_edit_*` (Qwen-Image-Edit) | **Qwen-Image-Edit-2511** | hf:`Qwen/Qwen-Image-Edit-2511` | (official Qwen release) | Successor to -2509. Fixes image drift, lifts multi-person group-photo consistency, integrates popular LoRAs into base, adds geometric-reasoning for design lines. Nunchaku quantization (`QuantFunc/Nunchaku-Qwen-Image-EDIT-2511`, 4.2K dl) shrinks for 16 GB. |
| 4 | New arch — text-to-image (high-quality non-distilled) | **Z-Image-Base** (sibling to Z-Image-Turbo) | repo:`Tongyi-MAI/Z-Image` | Z-Image-Turbo 857K dl / 4.8K likes | Z-Image-Base released 2026-01-28 as the non-distilled sibling to Z-Image-Turbo (already in HF top 15 trending). 6B params; 30-50 steps for "artist's choice" quality. Pairs with existing Turbo for fast-draft → finish-pass flow. Roadmap mentions Z-Image-Omni-Base + Z-Image-Edit. |
| 5 | `build_wan22_*` (WAN 2.2 i2v) | **Wan2.2-I2V-A14B-Moe-Distill-Lightx2v-NVFP4** | hf:`InsecureErasure/Wan2.2-I2V-A14B-Moe-Distill-Lightx2v-NVFP4` | 136 dl / 1 like | NVFP4-quantized MoE distillation of WAN 2.2 I2V. Distill + NVFP4 = meaningful VRAM/throughput win on the 5060 Ti's 16 GB. Diffusers-compatible. Worth a swap-in trial against current `wan22_i2v_a14b` reference. |

## Ecosystem releases since last digest

- **ComfyUI**: **v0.22.0 released 2026-05-13** (one minor + workflow-template + bug-fix wave above baseline 0.20.3). v0.21.1 also published earlier. No 0.23 yet. → https://github.com/Comfy-Org/ComfyUI/releases
- **krita-ai-diffusion** (Acly): **v1.51.0 released 2026-05-31**, v1.51.1 patch shortly after. Adds Anima control layers (Scribble / Line Art / Unblur), managed ROCm install for AMD, streamlined inpaint/outpaint UI. **Direct hit on baseline v1.50.** → https://github.com/Acly/krita-ai-diffusion/releases/tag/v1.51.0
- **LM Studio**: **v0.4.16 released 2026-06-08** (introduces "Locally" iOS app + LM Link for remote model access). v0.4.15 (2026-05-29) added CUDA tensor parallelism, fixed prompt-cache-on-every-message bug **specifically against Claude Code** — operator should bump. v0.4.14 made MTP Speculative Decoding stable. → https://lmstudio.ai/changelog
- **Models** (only substantively new):
  - **FLUX.2-klein**: KV-cached variant (`9b-kv`) is the news — accelerates multi-reference editing (the klein multi-angle path the operator just shipped). Unsloth GGUFs and fp8 variants of both 4B and 9B all available.
  - **WAN**: No 2.3 / 2.4 — Alibaba's roadmap puts WAN 3.0 (60B, native 4K, 30s continuous) at mid-2026, so it could land in the next 1–2 cycles. WAN 2.2 i2v remains state-of-the-art on the i2v branch; ecosystem now optimizing it (distills, NVFP4, MagCache).
  - **LTX**: LTX-2.3 released 2026-03-05 (22B params, native 4K at 50 fps with synced audio, open weights). LTX-2 base was 2026-01-06. **Substantive — currently the highest-perf open-weight video model.** No 2.4 / 3.0. Heavy (>13 GB) — flag as **exclusive-load**. → https://huggingface.co/Lightricks/LTX-2.3
  - **Z-Image**: Z-Image-Base (non-distilled, 6B, released 2026-01-28). See headline #4.
  - **Qwen-Image-Edit**: -2511 update. See headline #3.
  - **Cosmos3 / Microsoft Lens**: New text-to-image entrants in HF top 15 trending — `nvidia/Cosmos3-Super-Text2Image` (16.5K dl, created 2026-05-28) and `microsoft/Lens` (3.9K dl, 2026-05-15). Watch only — too new to recommend integration.

## HF trending shift

_Skipped — first run, no prior digest to diff against._

For reference, current HF top-15 trending text-to-image (snapshot 2026-06-10):
- Ideogram-4 variants dominate the top 4 (fp8 / nf4 / GGUF / nf4-diffusers).
- Stalwarts holding rank: FLUX.1-dev (#3), Z-Image-Turbo (#5), FLUX.1-schnell (#7), SDXL-base (#15), SD-3.5-large (#13).
- New trending entrants this cycle: Cosmos3-Super-Text2Image (#6), microsoft/Lens (#14), Phr00t/Qwen-Image-Edit-Rapid-AIO (#8).

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | **Live**. `iFayens/ComfyUI-PuLID-Flux2` is the first working PuLID for FLUX.2; supports klein 4B/9B and dev. Weights at `Fayens/Pulid-Flux2`. Plug-and-play in ComfyUI; auto-detects model. Klein gives best results per author. **Recommend trial against operator's current face-identity path** (already shipped in 2026-05-08 upgrade pass — verify which PuLID variant is bundled). |
| **MagCache** | **Stable**. Supports WAN 2.2 with 1.5–2× speedup, training-free. NeurIPS 2025 accepted. Operator's 2026-05-08 batch already shipped this — no new action. |
| **HyperSwap** | **Active**. ReActor (Gourieff) added HyperSwap CPU Float normalization fix + Face Similarity node. Three models supported: hyperswap_1a / 1b / 1c (all 256). Known bug: "black circle instead of face" on some hyperswap configs — InSwapper / Reswapper remain working fallbacks. 2026-05-08 batch already shipped HyperSwap 256 — monitor issue #226 for the black-circle fix before next push. |
| **Differential Diffusion inpaint** | **Stable / mature**. Native in modern ComfyUI; no flagship update — Krita-AI v1.51's "Anima inpaint/outpaint support" is the closest movement. Already shipped in 2026-05-08 batch — no new action. |

## Backend results

- `local_index`: 0 candidates (`lora_calibrations_sfw.json` is empty — store hasn't been written to yet; expected on a fresh install)
- `huggingface`: ~40 proposals surfaced across 4 queries (trending t2i, flux2-klein, wan2.2-i2v, qwen-image-edit)
- `civitai`: SKIPPED (script `tools/upgrade_research.py` not present in repo)
- `comfy_manager`: SKIPPED (LAN-only)
- `local_llm`: SKIPPED (LAN-only)

## Action items for operator

1. **Bump krita-ai-diffusion baseline v1.50 → v1.51.1** — direct minor with Anima control layers and managed ROCm install. Low risk.
2. **Bump LM Studio** (whatever local build the operator runs) **to v0.4.15+** — the Claude-Code prompt-cache-on-every-message fix is a direct quality-of-life win for the antenna/LM-Studio connector shipped in commit `1c97f6f`.
3. **Trial FLUX.2-klein-9b-kv against `build_klein_multi_angle`** — KV-cache accelerates exactly the multi-reference path the operator just shipped (commits b5107c2 / 13c5675). If quality matches, swap is mechanical.
4. **(Soft, next cycle) Scaffold `build_ideogram4_*` arch** — Ideogram-4 is the strongest emerging entrant on the Hub (trending score 440, fp8 + nf4 + GGUF all out within one week). Probably worth adding before Wan 3.0 lands and crowds the queue.
5. **(Soft)** Investigate why `tools/upgrade_research.py` is missing — operator may have it locally but un-pushed, or it may be planned. Without it the digest is HF + Web only and loses civitai signal.

## Run metadata

- runtime: ~8m
- WebSearch+WebFetch combined call count: ~7 (well under 25 budget)
- HF MCP call count: 5
- Google-Drive MCP: **FAILED — token expired** (re-auth needed)
- Gmail MCP: **NOT ATTEMPTED — requires interactive OAuth, unreachable from autonomous routine**
- `tools/upgrade_research.py`: **NOT FOUND in repo** at HEAD `1c97f6f`. Backend deferred. (`local_index`, `huggingface`, `civitai` partially substituted by HF MCP + WebSearch.)
- baseline drift: `build_*` count is **75** (baseline 73 at 2026-05-13) — accounted for by the two new `build_klein_multi_angle*` functions added in commits b5107c2 / 13c5675.
- next scheduled run: **2026-06-12 16:09 UTC** (every 48h)
