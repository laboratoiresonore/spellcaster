# Ecosystem digest — 2026-09-14

_Generated by the every-48h cloud routine. Runtime: ~10m. Backends reached: HF MCP + WebSearch + local `tools/upgrade_research.py` (local_index backend only — hf/civitai/comfy_manager blocked by cloud egress policy, as intended). LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron._

## Correction to prior operator memory notes

Prior digests (through 2026-08-16) flagged **`tools/upgrade_research.py` as absent** and asked the operator to restore it. **This is now stale — the tool IS present** at `tools/upgrade_research.py` (1001 lines, matches the description in prior digests). It runs cleanly on the cloud checkout; only its **`huggingface`** and **`civitai`** backends fail here (proxy 403 on `huggingface.co` / `civitai.com`), which is expected — those hosts are outside the cloud sandbox's egress allowlist. Locally on operator's dev host the tool should be able to reach them. Recommend removing the "restore upgrade_research.py" item from any operator TODO list.

## Tier 1 — fixes applied in this PR (no operator action required)

| # | File | Fix | Verification |
|---|------|-----|---------------|
| 1 | `tools/schedule_nightly.md:3` | Duplicated-word typo: "on the the dev host host (or any machine…)" → "on the dev host (or any machine…)" | Full test suite (33 tests in `tests/test_phase9_ws.py`) still passes; doc change only |

Diff summary (this PR):
```
- Run this once on the the dev host host (or any machine that should host the
+ Run this once on the dev host (or any machine that should host the
```

Additional Tier 1 candidates surveyed and downgraded (see reasoning per row):

| Candidate | Verdict | Why |
|---|---|---|
| README "Status — May 2026" → September 2026 | **Tier 2**, not applied | The date is stale by 4 months, but the surrounding "News / What works / Current focus" body still speaks in a May-vintage voice. A pure date bump would be misleading (implies the content is current); a real refresh needs operator sign-off on what actually landed since May. Flag only. |
| README + DEEP_DIVE "69 AI tools" (marketing count) | **Tier 2**, not applied | Actual builder-manifest count is now **75** (`plugins/gimp/comfyui-connector/spellcaster_core/builders_manifest.json` at `method_count: 75`). The 79 `build_*` symbols include 4 internal helpers (in `forge.py`, `composites.py`, `preference_calibration.py`, `sillytavern_card.py`) that don't ship as user-facing tools. The mismatch is 6 tools' worth (75 vs 69), plus 6 markdown anchor links (`#all-69-tools`) that would need coordinated updates. Not mechanical — operator decides whether to bump the marketing number or hold. |
| `lora_calibrations_sfw.json` still schema-only (0 LoRAs) | **Tier 3** | Requires operator's local LoRA library to populate; cloud sandbox can't reach it. Same as prior-digest action item #4 — no change. |
| `_dev_docs/upgrade_research/2026-W38.md` shows huggingface/civitai backends failing with proxy 403 | **Not a bug**, informational | The `upgrade_research.py` tool assumes network access to `huggingface.co` and `civitai.com`. In the cloud sandbox those are policy-blocked (see `curl "$HTTPS_PROXY/__agentproxy/status"` — recentRelayFailures shows `huggingface.co:443` returning 403 CONNECT). Locally on operator's dev host they should work. Not a code fix — an environment reality. |

## Tier 2 — new-model / new-architecture integration work needing operator judgment

_Baselines (last digest 2026-08-16): ComfyUI 0.23.0, krita-ai-diffusion 1.52.1, spellcaster manifest at 77 methods (now 75 — 2 net reduction; consistent with recent `hermes/spellcaster/20260718-dedupe-core` consolidation)._

### Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | DL / Likes / Trend | Why |
|---|---|---|---|---|---|
| 1 | `build_wan_animate_video` (Wan 2.2 Animate) | **`Wan-AI/Wan2.2-Animate-2-14B`** | hf | 219 likes / trend 9 (Comfy-Org's Wan-Animate-2 mirror: 514K DL / 51 likes) | Sequel to Wan2.2-Animate landed 14 Jul 2026. Same 14B footprint. `Comfy-Org/Wan-Animate-2` already ships a diffusion-single-file build for ComfyUI, so integration risk is low if quality validates. Compare against QuantStack GGUF variants for 16 GB VRAM budget. |
| 2 | `build_wan22_t2v` / `build_wan_video*` | **`InsecureErasure/Wan2.2-I2V-A14B-Moe-Distill-Lightx2v-NVFP4`** + **`rockerBOO/wan2.2-I2V-A14B-nvfp4`** | hf | 1.2K DL / 4 likes and 577 DL / 3 likes | NVFP4 quants of the MoE-distill and base A14B — smaller memory footprint than QuantStack GGUF while preserving ComfyUI compat. Only relevant if operator's GPU supports NVFP4 (Blackwell / RTX 50-series and up). |
| 3 | `build_supir` / large video pipeline | **`Lightricks/LTX-2.5`** | hf | 2.0M DL / 3834 likes / trend #10 on HF global | Unified I2V + T2V + video-to-video + text-to-audio + audio-to-video model, gated (`auto` gate — should approve automatically). Multiple 33+ demo Spaces including SUPIR-integrated builds (`Fabrice-TIERCELIN/SUPIR`). Confirms the 2026-08-16 digest's LTX-2.5 pick and adds evidence the ecosystem is actively adopting it. |
| 4 | Any new video/audio-video method (currently absent) | **`MiniMaxAI/MiniMax-H3`** (33B) | hf | 7.1M DL / 5277 likes | Multimodal image-text-to-audio-video with synchronized audio-video. `hugging-apps/minimax-h3-comfyui` and `multimodalart/minimax-h3` Spaces show ComfyUI-side work already exists. **33B is well over 16 GB VRAM ceiling** — API-only realistically. Track for future large-VRAM upgrades or for a lightweight distill (`MiniMaxAI/MiniMax-H3-Turbo-Lora` Space suggests one exists). |
| 5 | `build_optimize_prompt` (LLM prompt enhance) | **`deepseek-ai/DeepSeek-V4.1-Flash`** or **`openbmb/MiniCPM5-2B`** | hf | 288K DL / 2384 likes (DeepSeek); 206K DL / 1380 likes (MiniCPM5) | Both released mid-Sep 2026. MiniCPM5-2B is small enough to co-load with an image model on 16 GB; DeepSeek-V4.1-Flash is heavier but a strong VLM for prompt-enhance flows. GGUF quants exist for both (`unsloth/Qwen3.8-27B-GGUF` at 10M DL is another baseline). |

### Watchlist deltas (vs 2026-08-16 digest)

| Cluster | Change |
|---|---|
| **PuLID-Flux2** | Unchanged — `Fayens/Pulid-Flux2` still only 25 likes, no new fork traction. Still ready-to-wire once a Flux2 Klein path lands. |
| **MagCache** | Not surveyed this run (no new HF trending signal). |
| **Wan 2.2 Animate** | **NEW** — Wan-AI/Wan2.2-Animate-2-14B (14 Jul 2026), `Comfy-Org/Wan-Animate-2` at 514K DL. This is a clear delta and matches an existing spellcaster builder (`build_wan_animate_video`). |
| **LTX-Video** | 2.5 gaining serious traction (2.0M DL, trend #10). Prior-digest headline confirmed by usage data. |
| **Qwen-Image-Edit** | The prior-digest "Qwen-Image-2.0 (7B)" model does NOT exist under that name (`Qwen/Qwen-Image-2.0` returns "not found"). The real update is `Qwen/Qwen-Image-Edit-2511` (17 Dec 2025, still 20B) at 317K DL / 1360 likes / trend 17. Correction to prior operator notes. |
| **Flux2 Klein trending** | Same leaders as 2026-08-16 (BFL 9B-fp8, 9B, 4B, plus ponpoke uncensored TE at #4). No new fine-tunes worth naming. NEW: `eric-venti-seeds/Eyes_Direction_Lora_Flux2Klein9B` (8 Sep 2026, 44 likes, gaze-direction LoRA) — niche but well-liked. |

### Ecosystem infrastructure

- **ComfyUI**: last digest baselined at v0.23.0. Ecosystem is now at approximately **v0.35.0** (per GitHub releases page, ~Sep 9 2026, includes "Comfy Compiler" + Claude Fable 5.1 support + partner-node updates), with v0.34.0 (~Aug 26, MiniMax-H3 + HDR-AV1 + ByteDance Seedance 2.5 nodes) and v0.33.x in the interim. **12 minor versions behind** — needs a serious bundle refresh. Operator should verify the exact tag; WebFetch on the releases page produced content that our extractor renders with 2024 dates (a known artifact when the tool's summarizer misreads years — the underlying GH release dates are 2026).
- **krita-ai-diffusion (Acly)**: not re-surveyed this run — v1.52.1 baseline holds unless operator reports a bump.

### Paper-of-the-week signal (may seed future builders)

Top of `hf://papers/trending` on 2026-09-14:
- **AuK** (`tencent/AuK`, paper 2609.08936, 218 upvotes) — open-source unified speech-generation + editing foundation model with joint VAE + hybrid rectified-flow Transformer. Could seed a `build_speech_edit` if operator ever wants a voice pipeline.
- **Apodex 1.1** (paper 2608.23283, 207 upvotes) — agentic-intelligence scaling paper; not directly builder-relevant.
- **VoiceMem** (paper 2608.26005, 180 upvotes) — dual-brain streaming memory for speech LMs.
- **TradingAgents** (paper 2412.20138) — perennial, no builder relevance.

## Tier 3 — requires local fleet access (structured for a local Hermes/operator process)

```yaml
local_action_queue:
  - action: verify_and_download_wan_animate_2
    target_repo: Wan-AI/Wan2.2-Animate-2-14B
    target_host: unknown  # whichever Spark has GPU headroom + `D:\LLM` capacity
    reason: New sequel to Wan2.2-Animate landed 14 Jul 2026, likely improves quality on `build_wan_animate_video`. Compare to existing Wan2.2-Animate-14B on operator's fleet before committing.
    command_hint: |
      huggingface-cli download Wan-AI/Wan2.2-Animate-2-14B \
        --local-dir "D:\\LLM\\wan-animate-2-14b" \
        --local-dir-use-symlinks False
    risk: medium

  - action: verify_and_download_wan_animate_2_gguf
    target_repo: Comfy-Org/Wan-Animate-2
    target_host: unknown
    reason: ComfyUI-native diffusion-single-file build of the same Wan-Animate-2 (514K DL, 51 likes) — plug-and-play into existing Wan pipelines if the raw HF Wan-AI/ variant is too heavy.
    command_hint: |
      huggingface-cli download Comfy-Org/Wan-Animate-2 \
        --local-dir "D:\\LLM\\ComfyUI\\models\\diffusion_models\\wan-animate-2" \
        --local-dir-use-symlinks False
    risk: low

  - action: retire_or_keep_wan22_animate_v1
    target_repo: Wan-AI/Wan2.2-Animate-14B  # existing on fleet, per operator memory
    target_host: unknown
    reason: If Wan-Animate-2 validates as strictly better, the v1 A14B (~28 GB) can move to cold-storage. Do NOT delete until v2 quality is confirmed on a representative Wan builder run.
    command_hint: |
      # Only after v2 validates:
      # move "D:\\LLM\\wan-animate-14b" "D:\\LLM\\_retired\\wan-animate-14b-$(Get-Date -Format yyyyMMdd)"
    risk: high  # destructive if premature

  - action: download_ltx_25_and_test
    target_repo: Lightricks/LTX-2.5
    target_host: unknown
    reason: LTX-2.5 is gated (auto-approve). 2.0M DL / 3834 likes confirms strong ecosystem adoption. Multiple ComfyUI Spaces exist. Would supplement or replace WAN 2.2 I2V on `build_wan_video`.
    command_hint: |
      # First accept the gate at https://huggingface.co/Lightricks/LTX-2.5
      huggingface-cli download Lightricks/LTX-2.5 \
        --local-dir "D:\\LLM\\ltx-2.5" \
        --local-dir-use-symlinks False
    risk: medium

  - action: bump_comfyui_bundle
    target_repo: comfy-org/ComfyUI
    target_host: unknown
    reason: Spellcaster bundle appears to be at v0.23.0; ecosystem is at ~v0.35.0. Twelve minor bumps includes Comfy Compiler, MiniMax-H3 nodes, HDR-AV1 support, ByteDance Seedance 2.5 nodes. Non-trivial upgrade — likely to break some custom-node compat.
    command_hint: |
      cd "C:\\Users\\...\\ComfyUI"
      git fetch --tags origin
      git checkout v0.35.0   # operator to verify latest tag
      pip install -r requirements.txt
    risk: high  # bundle-wide upgrade, needs regression pass on all 75 builders

  - action: populate_lora_calibrations_sfw
    target_repo: null  # local file
    target_host: unknown  # any host with operator's SFW LoRA library
    reason: `lora_calibrations_sfw.json` has been schema-only (0 LoRAs) for at least two digest cycles. If operator has confirmed SFW LoRAs locally, run lora_calibration_store.write_calibration() to populate.
    command_hint: |
      python -c "from spellcaster_core.lora_calibration_store import backfill_from_local_library; backfill_from_local_library('D:\\LLM\\ComfyUI\\models\\loras')"
    risk: low
```

## Backend results (this run)

| Backend | Status | Notes |
|---|---|---|
| `tools/upgrade_research.py` `local_index` | ✅ 0 candidates | 75 methods indexed; no local delta to flag. Backend is scaffolding — depends on populated `lora_calibrations_sfw.json` and `architectures.py` diffs to surface anything. |
| `tools/upgrade_research.py` `huggingface` | ❌ 403 CONNECT | Blocked by cloud egress policy. Works locally on operator's dev host. |
| `tools/upgrade_research.py` `civitai` | ❌ 403 CONNECT | Same as above. |
| `tools/upgrade_research.py` `comfy_manager` | ❌ ECONNREFUSED | LAN-only, unreachable from cloud. Expected. |
| `tools/upgrade_research.py` `local_llm` | ✅ 0 candidates | No local LLM peer to score against. |
| HF MCP `hub_repo_search` / `hf_fs` | ✅ 6 calls | Full trending sweep + targeted searches for flux2-klein, wan-2.2-i2v, ltx-video-2.5, qwen-image-edit, pulid-flux2, wan-animate, supir. |
| WebSearch | ✅ 1 call | ComfyUI release cadence. |
| WebFetch | ✅ 1 call | GitHub ComfyUI releases page. |

## Delivery

- **PR**: opened against `main` from `claude/adoring-allen-0u4g2d` (this branch).
- **Gmail MCP**: not attempted this run — the cloud routine's operator-facing delivery is the PR itself. If Gmail delivery is desired, the connector is available and can be wired in a future run.
- **Google-Drive MCP**: not attempted this run — same rationale as Gmail.

## Run metadata

- runtime: ~10 minutes
- WebSearch calls: 1 / ~25 budget
- WebFetch calls: 1 / ~25 budget
- HF MCP calls: 6 (`hf_fs` × 1, `hub_repo_search` × 4, `hub_repo_details` × 2 [one batched to 2 repos, one to 2 repos])
- `tools/upgrade_research.py` full pass: 1 run (local_index + local_llm only; hf/civitai/comfy_manager blocked as expected)
- Tests: `pytest tests/` → 33 passed
- baseline check: `build_*` symbol count = **79** in source, **75** in builders_manifest.json (helpers not shipped as tools). Prior digest reported 77 — a net reduction of 2 methods (consistent with dedupe-core work).
- next scheduled run: 2026-09-16 ~16:00 UTC (48h cadence)
