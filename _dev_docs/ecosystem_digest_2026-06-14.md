# Ecosystem digest — 2026-06-14

_Generated by the every-48h cloud routine. Runtime: ~9m. Backends reached: HF MCP + WebSearch (Civitai backend deferred — see Backend results). LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. Note: `tools/upgrade_research.py` is not present in the current cloud checkout — sections that would normally pull from it were sourced from HF MCP + WebSearch instead._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | ComfyUI runtime (baseline 0.20.3) | **ComfyUI 0.24.0** (2026-06-03) | comfy docs | n/a — Comfy-Org | Four minor versions ahead of bundle baseline; 0.21.1 added the `Flux2ImageNode` that the Flux.2-klein candidates below depend on. Mandatory dep upgrade. |
| 2 | `build_*` Qwen image-edit pipelines | **Qwen-Image-Edit-2511-Lightning** (lightx2v, 2025-12-22) | hf | 242.1K dl / 462 likes / trending 7 | Distilled / few-step Qwen-Image-Edit-2511 specifically packaged for ComfyUI. Fits the 16 GB ceiling; large multiplier on edit-pipeline latency. |
| 3 | `build_*` Flux text-to-image | **FLUX.2-klein-4B** (BFL, 2026-01-14) | hf | 319.0K dl / 730 likes / trending 14 | Apache-2.0 4B variant sits right at the 13 GB VRAM ceiling; the 9B is heavy (~29 GB, exclusive-load). 4B is the realistic Flux.2 entry point for the operator's 5060 Ti. |
| 4 | `build_*` WAN image-to-video (baseline WAN 2.2) | **WAN 2.6 I2V** (Alibaba, 2025-12) | web | n/a — Alibaba | T2V/I2V/R2V unified family up to 15 s @ 1080p 24 fps; 2.2 references in `lora_calibrations_sfw.json` are now two majors stale. WAN 2.7 reportedly lands ~March 2026 — track for next digest. |
| 5 | `build_*` LTX-Video | **LTX-Video 2.3** (Lightricks, 2026-03-05) | web | 22B params, Community License | Single-pass synced video + audio at 4K@50 fps for up to 20 s clips; replaces the LTX baseline. Heavy — 22B class, flag exclusive-load. |

> Cross-checked against the 2026-05-08 25-upgrade batch: PuLID-Flux2, MagCache, HyperSwap 256, and Differential Diffusion inpaint are already shipped — deliberately excluded from candidates above and tracked in the watchlist instead.

## Ecosystem releases since last digest

_First run — comparing against operator's recorded 2026-05-13 baseline rather than a prior digest._

- **ComfyUI**: baseline 0.20.3 → **0.18.0** (2026-03-21, fp16-intermediates + LTX/WAN VAE VRAM reductions) → **0.20.1** (2026-04-27) → **0.21.0** (2026-05-11, audio+video loading in video loader node) → **0.21.1** (2026-05-13, `Flux2ImageNode`, `GrokImageEditNodeV2`) → **0.24.0** (2026-06-03, day-0 Ideogram 4 support). Net: 4 minor-rev gap.
- **krita-ai-diffusion**: baseline v1.50 → **v1.51.0** (control layers for Anima: Scribble/Line Art/Unblur control modes for official v1.0) → **v1.51.1** (managed-install fixes on macOS + Linux upgrades). No v1.52 yet.
- **LM Studio**: cadence active — **0.4.10** (Apr 9, MCP OAuth, Gemma 4 tool-calls), **0.4.12** (Apr 17, Qwen 3.6), **0.4.13** (May 13, mlx-engine v1.8.1 + Qwen 3.5/3.6 + Gemma 4 vision), **0.4.14** (May 29, MTP Speculative Decoding stable), **0.4.15** (May 29, tensor parallelism for multi-GPU + Physical Batch Size), **0.4.16** (Jun 8, `Locally` mobile app + LM Link no waitlist + 8k default ctx).
- **Models** (substantively new since baseline):
  - **Flux 2 [klein]**: BFL family released 2026-01-14 across 4B/9B + FP8 + KV-cached + GGUF (unsloth) variants. **Heavy**: 9B = ~29 GB VRAM (exclusive-load); 4B = ~13 GB (at ceiling). Ecosystem now spawning LoRAs (SmartCharacterSwap, manga-colorization, refcontrol-depth, dx8152 Migration).
  - **WAN**: 2.6 (Dec 2025) live, 2.7 telegraphed for ~March 2026 with 9-grid I2V + instruction-based editing. No 2.3 / 2.4 releases — Alibaba skipped straight to 2.5/2.6/2.7.
  - **LTX**: 2.19B (Jan 6, 2026) + 2.3 (Mar 5, 2026, 22B, 4K@50fps, native audio).
  - **Z-Image**: Turbo (Nov 2025) → **Z-Image Base** undistilled foundation (Jan 27, 2026); Z-Image Omni unified gen/edit in testing.
  - **Qwen-Image-Edit-2511** (Dec 17, 2025): 165.3K dl / 1048 likes. Mitigates image drift + integrated LoRA + character consistency. **Qwen-Image-2.0** (Feb 10, 2026): unified gen+edit, 7B params (down from 20B), 2K native resolution.

## HF trending shift

_Skipped — no prior digest to diff against. Next digest can compare against today's trending fingerprint:_

- FLUX.2-klein-9B (BFL) — trending 25, 89.4K dl
- ponpoke/flux2-klein-9b-uncensored-text-encoder — trending 22 _(flagged per H6: NOT a candidate. Surfaced as trend signal only.)_
- nhathoangfoto/Flux.2-Klein-9B-SmartCharacterSwap (LoRA) — trending 22
- dx8152/Flux2-Klein-9B-Migration — trending 19 (created 2026-06-10, brand new)
- thedeoxen/refcontrol-FLUX.2-klein-9B-reference-depth-lora — trending 16
- fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA — trending 14
- unsloth/Qwen-Image-Edit-2511-GGUF — trending 12

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | `iFayens/ComfyUI-PuLID-Flux2` at v0.6.2; supports Klein + Dev. Already in the 2026-05-08 batch — confirmed quiet upstream this cycle. |
| MagCache | Now ~2.8–3.0× on WAN 2.1 (incl. VACE-14B since 2025-07); Flux-Kontext 2× supported. Already shipped — no action; revisit if WAN 2.6 support lands. |
| HyperSwap | `hyperswap_1c_256` is current; ReActor side reports MaskHelper 1.5–2× speed-ups and a Face Similarity / ReActorSetWeight intensity dial. No drop-in upgrade; minor side-grade. |
| Differential Diffusion inpaint | Stable; soft-gradient mask pattern unchanged. Continues to be the recommended inpaint path for Flux + Qwen-edit chains. No action. |

## Backend results

- **local_index**: 0 candidates — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` `loras` dict is empty (schema v1, written by `lora_calibration_store.write_calibration()` on confirmed-SFW LoRAs; nothing committed yet).
- **huggingface (MCP)**: 5 candidate proposals surfaced — Flux.2-klein-4B/9B family + GGUF, Qwen-Image-Edit-2511 + Lightning distill, plus LoRA derivatives.
- **civitai**: SKIPPED (no `tools/upgrade_research.py` in cloud checkout to drive the civitai backend; raw civitai API call deferred to operator's local cron).
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).
- **WebSearch + WebFetch supplements**: 14 web queries + 1 WebFetch (403 on docs.comfy.org — version data triangulated from search results instead).

## Action items for operator

1. **Bump ComfyUI bundle 0.20.3 → 0.24.0**. The 0.21.1 cut adds `Flux2ImageNode`, which any Flux.2-klein adoption depends on; without this bump the headline #2/#3 candidates can't land.
2. **Replace WAN 2.2 references in `lora_calibrations_sfw.json` notes + any `build_wan_*` workflow** with WAN 2.6 I2V. Operator's `loras` dict is empty so this is a notes/workflow change only.
3. **Trial Qwen-Image-Edit-2511-Lightning** as a drop-in distill for whichever `build_*` Qwen edit pipelines currently exist — distilled few-step variants are the cheapest latency win on the table.

## Run metadata

- runtime: ~9 m
- WebSearch/WebFetch call count: 14 + 1 (within ~25-call budget)
- HF MCP calls: 5 (`hub_repo_search` ×4, `paper_search` ×1)
- backends reached: HF MCP, WebSearch
- backends skipped: civitai (no driver script in cloud), comfy_manager + local_llm (LAN-only by design)
- next scheduled run: **2026-06-16 16:00 UTC**
