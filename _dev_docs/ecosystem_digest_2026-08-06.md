# Ecosystem digest — 2026-08-06

_Generated by the every-48h cloud routine. Runtime: ~10 min. Backends reached: HuggingFace MCP + WebSearch. `tools/upgrade_research.py` **not present in cloud clone** — local_index/comfy_manager/local_llm/civitai backends all deferred to the operator's local cron. Gmail MCP is unauthenticated in this session — no email delivery this run; digest reaches the operator via git PR + Google Drive._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / trending | Why |
|---|---|---|---|---|---|
| 1 | Flux-family text-to-image builders | **FLUX.2 Klein 4B** (`black-forest-labs/FLUX.2-klein-4B`) | hf | 380k dl / trending 11, Apache-2.0 | Fits ~13 GB VRAM → **within** the 16 GB ceiling; the 9B fp8 needs ~29 GB and is exclusive-load "heavy". Klein 4B is the drop-in Flux successor for the operator's rig. |
| 2 | Text-to-image (unified gen+edit) | **Qwen-Image-2.0** (Feb 10, 2026 release) | web | 7B params, native 2048×2048 | Replaces the 20B Qwen-Image-Edit at ~⅓ the params while beating it on benchmarks; single model covers both `build_qwen_image_*` and `build_qwen_image_edit_*` paths. |
| 3 | Image-to-video (WAN family) | **WAN 2.6** (Alibaba, Dec 2025) | web | Up to 15 s @ 1080p 24 fps | Two full generations past the baseline naming. No "WAN 2.3" or "2.4" ever shipped — the watchlist entry is stale; WAN 2.2 → **2.6** is the real jump. |
| 4 | Text-to-image (Z-Image family) | **Z-Image Base + Z-Image Edit + Z-Image Omni** (Jan–Jun 2026) | web + civ | 6B distilled Turbo already shipped; Base open-sourced Jan 2026 | Turbo June-2026 update dropped 1024² latency from ~6 s → ~4 s. Omni unifies generation + editing under one arch — potential consolidation win for the Z-Image `build_*` cluster. |
| 5 | Face-identity conditioning on Flux.2 | **PuLID-Flux2 v0.6.2** (`iFayens/ComfyUI-PuLID-Flux2`, `Fayens/Pulid-Flux2`) | web + hf | Supports Klein 4B / 9B & Dev 32B | Already tracked in the operator's upgrade-pass baseline (25 shipped upgrades incl. PuLID-Flux2), but it's now on **v0.6.2** with dual-mode identity generation (Face Combine + Zero-Shot InfiniteYou). Worth confirming the local install is current. |

## Ecosystem releases since last digest

_(No prior cloud digest — this is the first cloud-side run; "since last digest" is elided.)_

- **ComfyUI**: baseline 0.20.3 is **3 minor versions behind**. Timeline:
  - **v0.21.1** — May 13, 2026 (Partner Nodes for image gen/edit + LLM; save/dtype fixes)
  - **v0.22.0** — May 20, 2026 (MoGe, Stable Audio 3, wide bug-fix pass)
  - **v0.23.0** — June 1, 2026 (multi-threaded model loading, MediaPipe face-detect, NVIDIA PixelDiT, Microsoft Lens)
- **krita-ai-diffusion**: baseline v1.50 is **2 minor versions behind**.
  - **v1.51.x** — shipped an auto-updater bug
  - **v1.52.0** — June 28, 2026 (Krea 2 support, regional prompting extended to Anima, auto-update fix)
  - **v1.52.1** — patch after 1.52.0
- **LM Studio**: **major rebrand** — the app formerly known as LM Studio 0.4.x is now **LM Studio Bionic 1.0.0** (released July 17, 2026), pitched as an agent platform rather than a chat runner. 0.4.16 (June 8) was the last of the 0.4.x line and shipped 8k default context + the "Locally" iPhone/iPad companion. Cloud can't verify the operator's local peer version — flagged for local review.
- **Models** (only if substantively new):
  - **Flux 2 Klein**: released Jan 14, 2026 (4B and 9B; fp8 quants; GGUF via Unsloth). Sits on HF trending at score 24 for the 9B variant.
  - **WAN**: 2.6 (Dec 2025) up to 15 s @ 1080p; no 2.3/2.4 exists.
  - **LTX**: **LTX-2** (Jan 2026, 4K + synchronized audio, fully open weights) and **LTX-2.3** (Mar 2026, 22B, 8-step distillation, spatial+temporal upscalers, desktop editor for local execution). No 2.4/3.0 yet.
  - **Z-Image**: Base + Edit + Omni family expansion (Jan–Jun 2026); Turbo June-2026 speed update.
  - **Qwen-Image**: **2.0** (Feb 10, 2026) — 7B params (vs 20B), native 2K, 1000-token prompts, unified gen/edit. Edit-2511 (Dec 2025) also added multi-image editing consistency & multi-angle view from a single reference.

## HF trending shift

_(Skipped — no prior digest to diff against.)_

Snapshot for future diffs: FLUX.2-klein-9B is the standout trending item (score 24) among Flux-family repos as of this run; Unsloth's GGUF quant of the same model trails at score 13.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | Live at v0.6.2 (`iFayens/ComfyUI-PuLID-Flux2`); supports Klein 4B/9B and Dev 32B. Confirm local install ≥ 0.6.2. |
| **MagCache** | Stable at ~2.68× speedup on Wan 2.1, 2.82× on HunyuanVideo (NeurIPS 2025 / arXiv 2506.09045). No breaking updates found; already adopted in the operator's upgrade-pass. |
| **HyperSwap** | FaceFusion Labs' HyperSwap 256 is stable (variants 1a=speed / 1c=quality). No 2026 breaking release; still the current SOTA face-swap head, no need to re-scan. |
| **Differential Diffusion inpaint** | No 2026 update to the core method (still the inference-only soft-inpainting framework from Levin et al.). **JoyAI-Image** (Apr 14, 2026) is a new adjacent open-source instruction-guided edit model with low-VRAM inference — worth a look as a peer, not a replacement. |

## Backend results

- **local_index**: `lora_calibrations_sfw.json` in this clone contains 0 LoRAs (schema-only). Operator's local cron should have populated results; treat cloud value as "not authoritative".
- **huggingface**: 10 flux2-klein hits, 15 (mostly-stale) generic text-to-image hits, WAN i2v query returned no direct matches under that exact string. Effective actionable proposals from HF today: **~5** (all Flux.2 Klein variants).
- **civitai**: **SKIPPED** — `tools/upgrade_research.py` not in the cloud clone, so the civitai backend wasn't invoked. No CivitAI candidates this run.
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **Upgrade ComfyUI 0.20.3 → 0.23.0** on the bundled install. Three minor versions of fixes + multi-threaded model loading is a real speedup on the 5060 Ti; PixelDiT support unlocks a new arch cluster.
2. **Bump krita-ai-diffusion v1.50 → v1.52.1** (skip 1.51 — it shipped a broken auto-updater). Krea 2 support lands with the upgrade.
3. **Add Flux.2 Klein 4B to `lora_calibrations_sfw.json`** and wire a `build_flux2_klein_*` workflow in `spellcaster_core.workflows` — this is the highest-leverage new architecture that stays under the 16 GB VRAM ceiling. Skip 9B (exclusive-load "heavy").
4. **Retire the "WAN 2.3 / WAN 2.4" watchlist entry** in the routine prompt — those versions never shipped; replace with "WAN 2.6" tracking.
5. **Restore `tools/upgrade_research.py` to the cloud clone** (or add a bootstrapping step) so future cloud digests can score candidates rather than narrate them. If it's intentionally local-only, drop it from the cloud routine's step-2 instructions to remove the misleading failure.
6. **Confirm LM Studio local peer is either on Bionic 1.0.0 or held at 0.4.16** — the Bionic rename is a full agent-platform pivot, not a routine upgrade; may need config changes.

## Run metadata

- **runtime**: ~10 min
- **WebSearch/Fetch call count**: 10 WebSearch, 0 WebFetch, 3 HF MCP `hub_repo_search` — 13 total (well under the 25 cap)
- **build_* count in this clone**: 77 (baseline was 73 at 2026-05-13; +4 net since baseline)
- **backends actually reached**: HF MCP + WebSearch. Everything else skipped/deferred.
- **delivery**: git PR + Google Drive only this run. Gmail MCP requires OAuth authorization that this non-interactive cloud session cannot perform.
- **next scheduled run**: 2026-08-08 (every-48h cadence)
