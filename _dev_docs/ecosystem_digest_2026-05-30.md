# Ecosystem digest — 2026-05-30

_Generated by the every-48h cloud routine. Runtime: ~6m. Backends reached: HF (MCP) + WebSearch/WebFetch. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` not present in this checkout — see Run metadata. First run: no prior digest to diff against._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Likes | Why |
|---|---|---|---|---|---|
| 1 | text-to-image base (general purpose) | **microsoft/Lens** | hf | 1.5K / 144 | Brand-new (May 15, 2026) text-to-image base from Microsoft, MIT license, diffusers-native. Top of HF trending (score 95) — first credible MS-authored T2I in months. Worth a probe. |
| 2 | fast/preview T2I (parallel to Z-Image-Turbo slot) | **microsoft/Lens-Turbo** | hf | 1.8K / 129 | Same-day companion to Lens; distilled "Turbo" variant for fast preview generation. Direct competitor in the Z-Image-Turbo / Flux-schnell preview niche. |
| 3 | VRAM-budget T2I (16 GB ceiling) | **prism-ml/bonsai-image-ternary-4B-gemlite-2bit** | hf | 0 / 65 | 1.58-bit ternary quantization of a FLUX.2-Klein-4B-derived backbone via gemlite. Targets sub-13 GB residency — directly relevant to the RTX 5060 Ti ceiling. Brand new (May 21). |
| 4 | identity-preserving editor (PuLID-Flux2 cluster) | **dx8152/Flux2-Klein-9B-Consistency** + **fwwrsd** LoRA fork | hf | ~13 / — | Consistency LoRA targeting the FLUX.2-Klein-9B backbone; complements the already-shipped PuLID-Flux2 with a lighter identity-lock path. Pairs cleanly with the operator's existing PuLID-Flux2 wiring. |
| 5 | image-edit (Qwen-Image-Edit slot) | **Phr00t/Qwen-Image-Edit-Rapid-AIO** | hf | 0 / 2076 | Highly-liked (2K+) AIO bundle around `Qwen-Image-Edit-2511`. Trending score 29; surfaces because Qwen-Image-Edit-2511 added the identity-preserving improvements documented in the May Qwen blog. |

**Heavy flags (VRAM > 13 GB → exclusive-load on 16 GB ceiling):**
- FLUX.2-Klein-9B family (~29 GB) — heavy, but the 4B variants (and the gemlite/MLX 2–4-bit packs) stay under budget.
- LTX-Video 2.3 (22B) — heavy, video-only path.

## Ecosystem releases since last digest

- **ComfyUI**: **v0.22.0 shipped May 20, 2026** (baseline 0.20.3). Adds MoGe depth estimation, Stable Audio 3 support, plus LTXVideo + Hunyuan3D enhancements and assorted bug fixes (audio nodes, mask indexing, clip saving). v0.21.x dropped first (May 11/13), then v0.22.0. Bump warranted.
- **krita-ai-diffusion**: v1.50.0 still current (released May 3). 33 commits to main since — no v1.51 tag yet.
- **LM Studio**: **v0.4.15 Build 2 (May 29, 2026)** — added CUDA tensor parallelism for multi-GPU, Physical Batch Size load option, HTTP/2 fix. v0.4.14 (May 29) shipped stable MTP Speculative Decoding. Operator runs LM Studio locally — flag for next pull.
- **Models**:
  - **Flux 2 Klein**: Family stable; community ecosystem is hot — mlx-community 3/4/5/6-bit packs and SDNQ-4bit quantizations of klein-9b landed within the last 24–48 h. The KV-cache 9B-KV variant remains the recommended path for multi-reference editing.
  - **WAN**: No 2.3 or 2.4 — series jumped 2.5 → 2.6 (current shipping, Dec 2025) with **2.7 announced for late March 2026 release window** (9-grid I2V, instruction-based editing). Confirm 2.7 release status next run; if shipped, replace WAN 2.2 references in the calibrations file.
  - **LTX-Video**: **2.3 released March 5, 2026** (22B, native 4K@50fps with synchronized audio, rebuilt VAE). No 2.4/3.0 yet.
  - **Z-Image**: Lineup expanded — **Z-Image-Base** dropped Jan 28, 2026 (non-distilled, 30–50-step quality ceiling); **Z-Image-Omni-Base** announced (unified gen+edit), no release date yet. Worth wiring an arch_registry entry.
  - **Qwen-Image / Edit**: **Qwen-Image-2.0** (Feb 10, 2026, 7B unified gen+edit, 2K native, Pro typography). **Qwen-Image-Edit-2511** revision adds character consistency + integrated LoRAs.

## HF trending shift

_First run — no prior digest to diff against. Baseline top-15 trending (text-to-image, sorted by trendingScore) captured today as the anchor for the next digest:_

1. microsoft/Lens (95) — NEW May 15
2. microsoft/Lens-Turbo (67) — NEW May 15
3. prism-ml/bonsai-image-ternary-4B-gemlite-2bit (65) — NEW May 21
4. black-forest-labs/FLUX.1-dev (53) — legacy anchor
5. prism-ml/bonsai-image-ternary-4B-mlx-2bit (44)
6. Tongyi-MAI/Z-Image-Turbo (35) — already in baseline
7. black-forest-labs/FLUX.1-schnell (29) — legacy
8. Phr00t/Qwen-Image-Edit-Rapid-AIO (29)
9. Qwen/Qwen-Image-Bench (28) — judge model
10. prism-ml/bonsai-image-binary-4B-gemlite-1bit (23) — 1-bit variant
11. prism-ml/bonsai-image-ternary-4B-unpacked (23)
12. prism-ml/bonsai-image-binary-4B-mlx-1bit (19)
13. ponpoke/flux2-klein-9b-uncensored-text-encoder (17)
14. Tongyi-MAI/Z-Image (16)
15. prism-ml/bonsai-image-binary-4B-unpacked (16)

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | Live and active. Working FLUX.2-Klein 4B/9B + Dev support via `iFayens/ComfyUI-PuLID-Flux2`; weights at `Fayens/Pulid-Flux2` on HF. Training scripts temporarily pulled (stability) per repo notes. |
| **MagCache** | Stable. `Zehong-Ma/ComfyUI-MagCache` integrated. Reported 2× on Flux-Kontext, 1.7× on Wan2.1-VACE-1.3B, 1.75× on Qwen-Image, 1.7× on HunyuanVideo-1.5. NeurIPS 2025 paper. Already shipped in the 2026-05-08 upgrade pass per memory. |
| **HyperSwap** | Released and active via FaceFusion 3.3.0+; 1A (speed) / 1B / 1C (quality) variants at 256 res. Existing FR `ComfyUI-ReActor` issue #143 still open requesting 1C — not yet integrated upstream. 256 variant already adopted per memory. |
| **Differential Diffusion inpaint** | Mature; still the recommended FLUX inpaint path per Feb 2026 community write-ups. No new variant. Already adopted per memory. |

## Backend results

- **local_index**: **0 baseline candidates** — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` has `loras: {}` (schema-only). Operator should populate or note that this is the intended empty-state.
- **huggingface (via MCP)**: 15 trending + 10 flux2-klein recent-modified + cluster query — see top-5 table.
- **civitai**: SKIPPED (no `upgrade_research.py` backend present; web search of civitai surfaced only the PuLID Flux Klein 4B face workflow, which is already noted in the PuLID cluster row).
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI bundle 0.20.3 → 0.22.0** (high priority). Diff is two minor releases (0.21.x, 0.22.0) — MoGe + Stable Audio 3 add new node surfaces; LTX/Hunyuan3D bug fixes affect existing workflows. Verify spellcaster's vendored node compat before merging.
2. **Populate `lora_calibrations_sfw.json` or document the empty state.** The local_index backend has nothing to anchor against — every subsequent digest will under-report local-side candidates. If the empty state is intentional (e.g. canonical lookup moved elsewhere), add a one-line `notes:` comment so future runs don't re-flag.
3. **Probe `microsoft/Lens` + `Lens-Turbo`.** Top of HF trending, MIT-licensed, diffusers-native, brand new. Worth a 30-minute fit-test for the T2I base slot before community PRs land asking about it.

## Run metadata

- runtime: ~6m (wall-clock from section 1 start to digest write)
- WebSearch calls: 12; WebFetch calls: 3; HF MCP calls: 4 (under 25 combined budget)
- `tools/upgrade_research.py` **not present** in this checkout — entire section 2 (ranked JSON+MD artefact) was skipped. Operator should confirm the tool exists on the local machine or port it to the cloud routine.
- LoRA calibration file is schema-only (0 loras): `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json`.
- Baseline `build_*` count: **74** (vs 73 reference, +1 — likely from the most recent multi-stage builder backfill PRs #40/#41/#44).
- Last 5 commits visible: `534e64b` NSFW source-of-truth annotations · `3ab3a8f` portrait consistency proxy · `1ddc285` Klein/Flux model_family align · `3da2d68` builders-manifest target_class backfill · `04ebc5d` target_class SSoT.
- next scheduled run: 2026-06-01 16:00 UTC
