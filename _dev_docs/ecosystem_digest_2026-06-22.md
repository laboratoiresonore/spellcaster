# Ecosystem digest — 2026-06-22

_Generated by the every-48h cloud routine. Runtime: ~12m. Backends reached: HF MCP + WebSearch (the `tools/upgrade_research.py` script is not present in this checkout — see Run metadata). LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. First run — no prior digest to diff against._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Likes | Why |
|---|---|---|---|---|---|
| 1 | Bundled runtime (ComfyUI 0.20.3) | **ComfyUI v0.23.0** (2026-06-01) | GitHub | n/a | Three minor versions behind baseline; brings multi-threaded model loading + native MoGe + Stable Audio 3 + MediaPipe face detection. Pure perf win, no workflow churn expected. |
| 2 | Qwen-Image-Edit pipeline | **Qwen-Image 2.0** (2026-02-10) | HF | n/a | 7B params, unified gen+edit, currently #1 on AI Arena for both T2I and editing. Fits the 16 GB VRAM ceiling comfortably. Likely replaces both Qwen-Image-Edit-2511 and the older text-rendering paths. |
| 3 | LTX-Video build_* (text/image-to-video) | **LTX-2.3** (2026-03-05) | HF / Lightricks | n/a | 22B DiT with native 4K @ 50fps and synchronized audio in a single pass. Heavy (exclusive-load, will pin the GPU) — flag as heavy. New VAE + LoRA fine-tuning support. |
| 4 | Krita plugin baseline (v1.50) | **krita-ai-diffusion v1.51.1** (Acly) | GitHub | n/a | Adds Anima control modes (Scribble, Line Art, Unblur), ROCm managed-install option, plus macOS/Linux upgrade fixes. Low-risk bump. |
| 5 | Flux2-Klein face/identity workflow | **dx8152/Flux2-Klein-9B-Migration** (2026-06-10) | HF | 0 dl / 28 likes | LoRA published 12 days ago; pairs with the Klein-9B Consistency LoRA already in upgrade-pass batch. Newer, complementary identity-retention adapter — worth A/B against current PuLID-Flux2 path. |

## Ecosystem releases since last digest

(No prior digest exists — these are all "new vs. operator's stated baselines".)

- **ComfyUI**: v0.21.1 (2026-05-13) → v0.22.0 (2026-05-20) → **v0.23.0 (2026-06-01)**. Baseline 0.20.3 is 3 minor versions behind. [Releases](https://github.com/Comfy-Org/ComfyUI/releases)
- **krita-ai-diffusion**: **v1.51.1** (latest patch on Acly fork). [Releases](https://github.com/Acly/krita-ai-diffusion/releases)
- **LM Studio**: **v0.4.16** (2026-06-08). Adds LM Link general availability + iOS/iPad "Locally" mobile app. [Changelog](https://lmstudio.ai/changelog)
- **Models**:
  - **Flux 2 Klein**: shipped 2026-01-15 (already in upgrade-pass batch). Notable fresh LoRAs: SmartCharacterSwap (Jun 1), schematic depth/normal/pose LoRA (Jun 1), Migration (Jun 10).
  - **WAN**: 2.6 (Dec 2025) is current open release. 2.7 announced for March 2026 (9-grid I2V, instruction-based editing) but no public weights yet — keep on watchlist.
  - **LTX**: **LTX-2.3** (Mar 5, 2026) — 22B, 4K, native audio.
  - **Z-Image**: **Z-Image-Base** (Jan 28, 2026) — non-distilled checkpoint, 30–50 steps, higher artistic ceiling than Turbo. Z-Image-Edit + Z-Image-Omni in early-access.
  - **Qwen-Image-Edit**: superseded by **Qwen-Image 2.0** (Feb 10, 2026) — unified model.

## HF trending shift

_Skip — first run, no prior digest._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | No core update; ecosystem stable. Companion adapters (SmartCharacterSwap, Migration LoRA) emerging on Klein-9B. |
| MagCache | No new release since 2025-11-23 (Qwen-Image support, 1.75×). Stable. |
| HyperSwap | Stable; HyperSwap 1a/1b/1c 256 already shipped via ReActor. No new variants surfaced. |
| Differential Diffusion inpaint | No upstream change; **LanPaint v1.5.0** (Mar 2026) + Anima support (May 2026) is the newcomer worth evaluating as an alternative inpaint path. |

## Backend results

- local_index: 0 baseline candidates from `lora_calibrations_sfw.json` (the shipped file is an empty `{}` — operator-side calibrations not seeded yet).
- huggingface: 4 actionable candidates (Klein Migration LoRA, SmartCharacterSwap, schematic-LoRA, Qwen-Image 2.0).
- civitai: SKIPPED (`upgrade_research.py` not in repo — see Run metadata).
- comfy_manager: SKIPPED (LAN-only).
- local_llm: SKIPPED (LAN-only).

## Action items for operator

1. **Bump bundled ComfyUI from 0.20.3 → 0.23.0.** Three releases of cumulative perf + new-model support; revert risk is low because the breaking changes from 0.21.x were already reverted in 0.21.1. Worth a smoke-run on the 77 `build_*` workflows.
2. **Evaluate Qwen-Image 2.0** as a drop-in for the current Qwen-Image-Edit-2511 path — 7B fits the 16 GB ceiling, and the unified gen+edit story collapses two workflows into one.
3. **Add LTX-2.3 as a heavy/exclusive-load build_*** if there's appetite for native-audio video; flag VRAM-heavy (22B DiT, exclusive load on the RTX 5060 Ti).

## Run metadata

- runtime: ~12 minutes
- WebSearch / WebFetch call count: 9 WebSearch, 0 WebFetch (well under the 25 budget)
- HF MCP calls: 4 (`hub_repo_search` ×3, `paper_search` ×1)
- **Anomaly**: `tools/upgrade_research.py` is not present in this checkout — the routine spec assumed it. Digest produced from WebSearch + HF MCP only; civitai backend skipped. Operator may want to land that script before the next 48h cycle, or update this routine's spec.
- baseline drift: 77 `build_*` functions in `spellcaster_core.workflows` (operator baseline noted 73 at 2026-05-13 → +4 since). Minor, no action.
- next scheduled run: 2026-06-24 ~16:00 UTC
