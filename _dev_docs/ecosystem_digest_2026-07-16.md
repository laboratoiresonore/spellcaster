# Ecosystem digest — 2026-07-16

_Generated by the every-48h cloud routine. Runtime: ~4m. Backends reached: HF MCP + WebSearch/WebFetch + local_index (empty).  LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. `tools/upgrade_research.py` was NOT present in this checkout — see `## Run metadata` for the failure note._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | Base T2I (FLUX.1-dev / SDXL) | **krea/Krea-2-Turbo** | hf | 191.5K dl / 624 likes / trending 48 | Brand-new (Jun 18 2026) foundation model dominating HF trending; ecosystem of LoRAs (M87 trending 77, ostris style ref, Krea2-realism-V2) is already exploding — biggest single ecosystem shift since the last shipped upgrade batch. |
| 2 | FLUX.2 family | **black-forest-labs/FLUX.2-klein-4B** | hf | ~13 GB VRAM, Apache 2.0 | Fits inside the 16 GB ceiling (heavy — flag: exclusive-load); unified generation + editing in one model; strong quantised community forks (WaveCut OrbitQuant W4A4, wikeeyang True-V3 GGUF). |
| 3 | Qwen editor path | **Qwen-Image-Edit-2511** + **Qwen-Image-2.0** | web + hf | Qwen-Image-2.0 = 7B (down from 20B) | Qwen-Image-Edit-2511 improves character identity + integrates popular LoRAs into base; Qwen-Image-2.0 (Feb 10 2026, 7B) matches or exceeds 20B predecessor at native 2K — direct replacement for existing Qwen editors. |
| 4 | Turbo/distilled T2I | **fal/ideogram-v4-instant** + **-v4-fast** | hf | trending 24 / 20, released Jul 13 2026 | 8-step CFG-distilled (instant) or 20-step FP4 QAD (fast); very fresh — evaluate whether the tiny step budget is worth the licence tradeoff before adding a build_* method. |
| 5 | Face identity (PuLID→Flux2) | **iFayens/ComfyUI-PuLID-Flux2 v0.6.2** | web | First working PuLID for Flux 2 | Direct successor path for the existing PuLID-Flux2 build already shipped in the 2026-05-08 batch — same node concept, now targets Klein 4B/9B/Dev 32B. Watch for a stable 0.7.x before rewiring. |

_Selection criteria: strongest HF trending score cross-referenced with concrete public release dates in the last ~30 days, filtered to what fits a 16 GB VRAM SFW pipeline. Krea-2 baseline downloads dwarf everything else in the top-15 — genuine ecosystem inflection._

## Ecosystem releases since last digest

- **ComfyUI**: 0.20.3 → **0.21.0 → 0.22.0 (2026-05-20) → 0.23.0 (2026-06-01)**. 0.21 landed block prefetch + LoRA async loading (LTX adoption reported for speed). Three minor bumps stacked, so the bundle in the operator repo is now ~3 minors behind — worth a bump pass.
- **krita-ai-diffusion**: v1.50 → **v1.51.0 → v1.52.0 (2026-06-28) → v1.52.1 (2026-06-30)**. v1.51 added Anima control layers (Scribble / Line Art / Unblur). v1.52 adds Krea 2 support. v1.52.1 fixes managed-server connect. Auto-update was broken in v1.51.x → v1.52 required a manual reinstall for many users — flag for the operator.
- **LM Studio**: v0.4.14 (2026-05-29, stable MTP speculative decoding) → **v0.4.16 (Locally mobile app + LM Link Tailscale mesh)** → v0.4.18 (2026-07-07, UI scaling / reasoning fixes) → **v0.4.19 (2026-07-08)** — latest.
- **Models** (only substantively new):
  - **Krea-2-Turbo / Krea-2-Raw** (2026-06-18): new open-weight foundation, ecosystem forming fast on HF. **This is the story of the last 30 days.**
  - **FLUX.2 klein 4B / 9B / 9B-KV**: 4B fits 16 GB VRAM (~13 GB, "heavy" — exclusive-load); 9B needs ≥24 GB (out of scope for the RTX 5060 Ti). Apache 2.0. KV variant caches reference images for accelerated multi-reference editing.
  - **WAN**: baseline is 2.2 (Jul 2025). **WAN 2.6** shipped Dec 2025 (up to 15-sec generations, multi-shot). **WAN 2.7** promised March 2026 — could not confirm public weights as of this run; treat as future-quarter watch.
  - **LTX-2 (2026-01-06)** and **LTX-2.3 (2026-03-05)**: 4K@50fps with synchronised audio in a single pass. LTX-Video 2.4 / 3.0 not found — nothing there yet.
  - **Z-Image / Z-Image-Turbo**: Z-Image-Turbo (Nov 2025) still trending (1.0M dl, 4992 likes, trending 31). Z-Image (Jan 27 2026) is the full base. No "Turbo 2" yet.
  - **Qwen-Image-2.0 (2026-02-10)**: 7B params (from 20B), native 2K, currently #1 on AI Arena for both T2I and image editing.
  - **Ideogram-v4-instant / -v4-fast** (2026-07-13, fal): brand-new distilled variants of Ideogram 4 — 3 days old at digest time.

## HF trending shift

- **New in top 15 trending (last ~30d)**: mgwr/M87 (Krea-2 LoRA, trending 77), krea/Krea-2-Turbo (trending 48), ostris/krea2_turbo_style_reference, wikeeyang/Krea2-Turbo-HD-V1 (GGUF), wikeeyang/Flux2-Klein-9B-True-V3, Phr00t/Qwen-Image-Edit-Rapid-AIO (ComfyUI packaging), John2386/fullgreed (Z-Image finetune), fal/ideogram-v4-instant, fal/ideogram-v4-fast, RudySen/Krea2-realism-V2, krea/Krea-2-Raw.
- **Dropped from top 15**: first-run baseline — no prior digest to diff against. Notable is that among "old guard" only FLUX.1-dev (trending 44), FLUX.1-schnell (18) and SDXL-base-1.0 (18) still make the cut; Z-Image-Turbo holds firm.
- **Signal**: the top 15 is dominated by 6+ Krea-2 derivatives, telling us the community is converging around it as a new base — heavier weight than any single release note.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **iFayens/ComfyUI-PuLID-Flux2 v0.6.2** live, supports Klein 4B/9B + Dev 32B. Direct upgrade path from the shipped PuLID-Flux2 in the 2026-05-08 batch — but at 0.6.x it's still pre-stable; wait for 0.7.x. |
| MagCache | NeurIPS 2025 paper; **Wan2.1 + Wan2.2 MagCache inference added 2026-03-25**, **Wan2.2 SenCache added 2026-03-31 (up to 1.4x speedup)**. If the Spellcaster MagCache node targets pre-2.2, an upgrade to the SenCache variant is a real perf win. |
| HyperSwap | Live via FaceFusion 3.3.0 with three variants (1A speed / 1B balance / 1C max realism). Licence switched from MIT to **OpenRAIL-AS** — **licence delta needs review** before pushing further HyperSwap variants into a public repo. |
| Differential Diffusion inpaint | ComfyUI V3 migration folded differential diffusion into core nodes. No standalone release change of note; the shipped Spellcaster method still works. |

## Backend results

- local_index: **0** baseline candidates from `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` (schema v2, `loras: []` — empty in this checkout).
- huggingface: **7** flux2-klein community forks + 15 T2I trending candidates surfaced via `hub_repo_search`.
- civitai: SKIPPED — `tools/upgrade_research.py` not present in this checkout (was the only civitai-hitting entry point).
- comfy_manager: SKIPPED (LAN-only).
- local_llm: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI bundle**: 0.20.3 → 0.23.0 (three minors landed since baseline). Block prefetch + LoRA async loading are worth harvesting.
2. **Evaluate Krea-2-Turbo as a first-class Spellcaster arch**: it is the single most significant change in this window; 191K downloads in <1 month + ecosystem of LoRAs. Worth an experimental `build_krea2_*` prototype.
3. **Verify local `tools/upgrade_research.py` script is present in your working tree** — the cloud clone at `laboratoiresonore/spellcaster` HEAD does not have it. Either it was never pushed, or it lives in a branch. If it stays local-only, this digest routine will keep skipping the civitai + local_index sweeps.
4. **krita-ai-diffusion**: safe to bump to v1.52.1 for Krea 2 support, but do the download manually — v1.51.x auto-update is broken.

## Run metadata

- runtime: ~4m
- WebSearch/Fetch call count: 9 web searches, 0 web fetches (well under 25 budget)
- HF MCP calls: 5
- next scheduled run: 2026-07-18 (48h)
- **failures**:
  - `tools/upgrade_research.py` — file missing from checkout `laboratoiresonore/spellcaster@ccef78a`. Digest fell back to WebSearch + HF MCP.
  - Gmail MCP — server present but **requires OAuth** which cannot be completed in this non-interactive sandbox. Digest not emailed; operator will find it in git + Drive.
  - `comfy_manager` + `local_llm` — LAN-only, expected skip.
- baseline drift check: `build_*` count = **77** (baseline was 73 as of 2026-05-13) — repo has grown by 4 builders; not a regression.
