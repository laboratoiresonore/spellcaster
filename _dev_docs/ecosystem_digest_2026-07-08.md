# Ecosystem digest — 2026-07-08

_Generated by the every-48h cloud routine. First run — no prior digest to diff._
_Backends reached: HuggingFace MCP + WebSearch/WebFetch. `tools/upgrade_research.py` is **absent from the repo** (see Run metadata); `local_index` fell back to a manual pass over `lora_calibrations_sfw.json` (schema stub, 0 entries). LAN-only backends (comfy_manager, local_llm) deferred to the operator's local cron._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads / Likes | Why |
|---|---|---|---|---|---|
| 1 | new arch (build_krea2_*) | `krea/Krea-2-Turbo` + `krea/Krea-2` (RAW) | HF | open-weights, released May 12 2026 | First **truly new** open-weights foundation image model since Flux 2 Klein — 12.9B DiT built from scratch, aesthetics/style-first. Turbo is 8-step distilled → fits the fast-preview slot Z-Image-Turbo occupies today. Already integrated by krita-ai-diffusion v1.52. Heavy (12.9B) but Turbo checkpoint should fit 16GB. |
| 2 | qwen_image_edit method (build_qwen_edit_*) | `Qwen/Qwen-Image-Edit-2511` | HF | 173.9K DL / 1109 likes | Direct successor to `Qwen-Image-Edit-2509`. Big multi-person consistency gains, unified 2-image fusion. Community `unsloth/…-GGUF` quantisation live (228K DL). Heavy (20B MMDiT) → exclusive-load. |
| 3 | qwen_image method (build_qwen_image_*) | `Qwen/Qwen-Image-2512` | HF | 59.4K DL / 902 likes | Successor to Qwen-Image-2.0 (7B, unified gen+edit). Small (7B) → comfortable on 16GB. Newer than the 2.0 the operator's memory anchors on. |
| 4 | flux2klein / distillation | `epfl-vita/flux2-klein-1step-rdm` | HF | 105 DL / 8 likes (new: 1 Jul 2026) | 1-step distilled Klein — potential replacement for MagCache-accelerated Klein flow. Fresh (7-day-old), signal-worth-watching rather than adopt-now. |
| 5 | face-swap enhancement | FaceFusion Labs HyperSwap **1C** | GitHub / Segmind | live in FaceFusion 3.3.0+ | 1C variant prioritises maximum realism (vs. 1A/1B speed variants operator likely baselined). Same repo, no new arch needed. |

_Filter method: cross-referenced HF trendingScore with WebSearch release-date signal. Flux 2 Klein finetunes were suppressed since Klein is already in the arch registry (`flux2klein`) — the operator has Klein._

## Ecosystem releases since operator baseline

- **ComfyUI**: baseline **0.20.3** → current **0.23.0** (Jun 1 2026). Three minor releases jumped:
  - 0.21.0: Gemma4 text gen, block-prefetch + LoRA async loading (LTX speedup)
  - 0.22.0 (May 20 2026): MoGe, Stable Audio 3
  - 0.23.0 (Jun 1 2026): **multi-threaded model loading** (big for cold-start), MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens
- **krita-ai-diffusion**: baseline **v1.50** → current **v1.52.1** (Jun 30 2026).
  - 1.51.0: ROCm managed install (AMD GPUs), improved inpaint control
  - 1.52.0/.1: **Krea 2 support**, extended regional prompting to Anima, auto-updater bug fix (recommend manual update if operator is on 1.51.x)
- **LM Studio**: baseline unclear, current **0.4.14** (May 29 2026) — MTP Speculative Decoding is stable, big local-LLM throughput win for models with built-in MTP heads.
- **Models** (substantively new):
  - **Krea 2**: **NEW open-weights foundation model** (May 12 2026), 12.9B DiT, RAW + Turbo, community-license open weights.
  - **Wan 2.6**: current live version (2.7 planned March 2026 per WaveSpeed — that date has passed with no OSS drop yet; keep watching). 2.6 landed Dec 2025. Baseline `wan` arch in spellcaster tracks 2.2 → **two majors behind**.
  - **LTX 2.3**: released Mar 5 2026 (successor to LTX-2 from Jan 6 2026). 22B DiT, native 4K@50fps + synchronised audio. Spellcaster `ltx` arch tracks earlier — likely two majors behind.
  - **Qwen-Image-2512**: 7B unified gen+edit (successor to 2.0, Feb 10 2026).
  - **Qwen-Image-Edit-2511**: multi-person consistency upgrade over 2509.
  - **Z-Image-Base** (Jan 28 2026): non-distilled counterpart to Z-Image-Turbo. Also **Z-Image Omni Base** coming (unified gen+edit).

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | ✅ shipped — `Fayens/Pulid-Flux2` native weights + `iFayens/ComfyUI-PuLID-Flux2` node (v0.6.2, last activity May 22 2026). Supports Klein 4B/9B + Dev 32B. |
| **MagCache** | ✅ published, NeurIPS 2025. Official code at `Zehong-Ma/MagCache`. 2.68× speedup on Wan 2.1, community port to Wan 2.2 tracked in `Wan-Video/Wan2.1#454`. Confirmed operator already shipped in the 2026-05-08 upgrade batch. |
| **HyperSwap** | ✅ live in FaceFusion 3.3.0+, three variants (1A speed / 1B balanced / 1C realism). Note license moved MIT → OpenRAIL-AS mid-2025 — flag for compliance review. |
| **Differential Diffusion inpaint** | ✅ stable, well-documented ComfyUI recipes for both SDXL and Flux paths. No new drop this cycle — steady-state. |

## Backend results

- **local_index**: 0 candidates — `lora_calibrations_sfw.json` is a schema stub with an empty `loras` array. Consider populating from real calibration store on next local sync.
- **huggingface (MCP)**: 4 targeted queries executed; strongest signals surfaced above.
- **civitai**: not queried this run (no `tools/upgrade_research.py`; manual browse skipped to preserve WebSearch budget).
- **comfy_manager**: SKIPPED (LAN-only).
- **local_llm**: SKIPPED (LAN-only).

## Action items for operator

1. **Bump the ComfyUI bundle from 0.20.3 → 0.23.0** — three minor releases behind; multi-threaded model loading in 0.23 alone is worth it for the spellcaster cold-start path. Check for node compatibility breakage first (0.22 changed a lot of partner-node OpenAPI definitions).
2. **Add `build_krea2_turbo` / `build_krea2_raw`** — Krea 2 is the standout new arch this cycle. Turbo (8-step) is the natural addition; RAW is heavier and best left for a follow-up. krita-ai-diffusion already ships integration, so operator's Krita path unblocks itself.
3. **Restore `tools/upgrade_research.py`** — the routine was authored assuming this exists but it's not in the repo. Digest quality across future runs will be materially worse until this is checked in. If it lives in a private branch, cherry-pick to `main`; if it was never written, this is the first concrete gap to close before the next cycle.
4. **Bump `wan` arch from 2.2 → 2.6** (or defer until 2.7 lands). Two majors behind, but 2.7 is imminent — if operator wants to skip a hop, wait ~4 weeks and land 2.6-or-2.7 in one pass.
5. **Populate `lora_calibrations_sfw.json`** — empty `loras[]` array blocks the `local_index` backend entirely.

## Run metadata

- **runtime**: ~10 min (interactive-driven, not automated)
- **WebSearch calls**: 11 · **WebFetch calls**: 0 (used HF MCP for structured data instead) · **HF MCP calls**: 5
- **known gaps**:
  - `tools/upgrade_research.py` **missing from repo** — real-backend research skipped. Fallback pass via WebSearch + HF MCP only.
  - **Gmail MCP**: authentication required in interactive session; email delivery unavailable this run.
  - **`_dev_docs/upgrade_research/` empty** — no prior run history to diff against; this is the first digest.
- **next scheduled run**: 2026-07-10 ~16:00 UTC (48h)

_All findings above filter for SFW canon. No LAN IPs, usernames, or tokens are embedded. Cross-references use candidate names, not PR/issue numbers._
