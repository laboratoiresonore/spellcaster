# Ecosystem digest — 2026-08-04

_Generated by the every-48h cloud routine. First run — no prior digest to diff against. Runtime: ~4m. Backends reached: HF MCP + WebSearch (Civitai indirect, via HF `Muapi/*` mirrors). LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron. Also: `tools/upgrade_research.py` was not present in the cloud checkout — fell back to direct HF MCP + WebSearch. Also: Gmail MCP was not authorized in this sandbox — this digest is delivered via git PR + Google Drive backup only; the operator's local session can mirror to inbox when it next runs, or re-authorize Gmail for future runs._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Likes | Why |
|---|---|---|---|---|---|
| 1 | Flux family (`build_faceswap*`, portrait workflows) | **FLUX.2 Klein 9B** + GGUF | HF `black-forest-labs/FLUX.2-klein-9B` | 339K DL / 1.16K likes | Jan 2026 BFL successor. Face-identity holds under high-strength swap; 9B fits 16GB via `unsloth/FLUX.2-klein-9B-GGUF` (83.7K DL). Community `Pulid-Flux2` adapter already lands (Mar 2026). |
| 2 | WAN i2v/t2v (`build_wan_*`, 6 methods) | **WAN 2.6 / 2.7 suite** | Alibaba release (Apr 2026) | Apache 2.0 | Baseline pinned at WAN 2.2 — five point-releases missed. 2.7 ships T2V + I2V + R2V + instruction-based video edit under one license. Direct drop-in for existing WAN handlers. |
| 3 | `build_zimage_*` (0 today) or new build for photoreal fast-gen | **Z-Image Turbo** + Z-Image Base + Z-Image Edit | HF `Tongyi-MAI/Z-Image-Turbo` | 1.1M DL / 5.08K likes (Turbo alone); Comfy-Org mirror at 5.3M DL | 6B params, 2.3 s per 1024² on RTX 4090. Fits 16GB comfortably. `Base` model (Jan 2026) adds CFG + negative prompts; `Edit` (Feb 2026) is the missing image-editing sibling. Highest-signal HF trend in the run. |
| 4 | Qwen-Image-Edit (`build_qwen_*`) | **Qwen-Image-Edit-2511** | HF `Qwen/Qwen-Image-Edit-2511` | 225.9K DL / 1.24K likes | Dec 2025 refresh of the Qwen edit pipeline. `fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA` (61.7K DL / 1.5K likes) unlocks camera-control image edits — new workflow surface. Comfy-Org single-file mirror already ships (866K DL). |
| 5 | `build_ltx_video` (LTX-Video) | **LTX-Video 2.3** | HF `Lightricks/LTX-2.3` (adapters at `Muapi/ltx-2.3-*`) | Base + growing adapter ecosystem (May 2026) | Direct successor to the arch our single LTX builder targets. New: audio-video inpainting, IC-LoRA control, image-to-audio. Larger jump than a numbering suggests. |

## Ecosystem releases since last digest

_(No prior digest — this is the delta from the operator's local baseline, not from a previous cloud run.)_

- **ComfyUI**: baseline 0.20.3 → **v0.23.0 (2026-06-01)**. Also passed v0.21.1 (May 13) with Partner Nodes + LLM nodes and v0.22.0 (May 20) with MoGe + Stable Audio 3 support. v0.23.0 adds multi-threaded model loading, MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens, cheap-latent-preview for Flux 2, improved LoRA training. **3 minor releases behind.**
- **krita-ai-diffusion (Acly)**: baseline v1.50 → **v1.52.1**. v1.51 required inpaint control model for full-strength Fill/Expand (bigger quality jump), added ROCm managed install for supported AMD GPUs. v1.52 added basic Krea 2 support + fixed a broken auto-updater. v1.52.1 fixed managed-server connect. Recommends Krita 5.3.2.1.
- **LM Studio**: **v0.4.20 (2026-07-22)** — Bionic agent runtime, LM Link (remote instance access over Tailscale), MCP OAuth, Qwen 3.6 support, enterprise internal-network endpoints. Note: LM Studio also acquired Locally AI (iOS/macOS runner) in April 2026.
- **Models — new since baseline:**
  - **FLUX.2 Klein**: 4B + 9B released 2026-01-14 (see #1 above).
  - **WAN 2.6** (Dec 2025) → **WAN 2.7** (2026-04-01→06). Full suite Apache 2.0.
  - **LTX-Video 2.3**: base + adapters (see #5).
  - **Z-Image Turbo / Base / Edit**: full family, Nov 2025 → Feb 2026.
  - **Qwen-Image-Edit-2511** (Dec 2025) + multi-angle LoRA (Jan 2026).

## HF trending shift

_Skipped — first run, no prior digest to diff against. Next run will populate this._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | Landed. `Fayens/Pulid-Flux2` (2026-03-21, 17 likes, ComfyUI-compat) is the reference community adapter; a second fork exists (`Shivammesh26/Pulid-Flux2`, May 2026). No official BFL PuLID-2 yet — community carries this. |
| MagCache | Shipped in the 2026-05 upgrade batch per baseline — nothing new to add. Underlying paper is NeurIPS 2025; the 2.68× WAN 2.1 speedup number is now the reference. |
| HyperSwap | No new HyperSwap model line in 2026. Existing `hyperswap_1a/1b/1c_256` models are still what ReActor and FaceFusion ship. Baseline `HyperSwap 256` note remains accurate. |
| Differential Diffusion inpaint | No new algorithmic variant surfaced. Only ecosystem-adjacent news: LTX-2.3 added audio-video inpainting (different surface). |

## Backend results

- local_index: **0** baseline candidates — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` is currently an empty stub (`{"schema_version": 1, "notes": "...", "loras": {}}`). Not a cloud problem; the community-baseline LoRAs haven't been written yet. Operator: seed this and the local backend becomes useful.
- huggingface (via MCP): ~50 candidate rows surfaced across 7 queries; digest headline drawn from the 5 highest-trending arch-matches to Spellcaster's `build_*` surface.
- civitai: not queried directly (Civitai's public search wasn't reachable in this run); LoRA adapter signal came in via HF's `Muapi/*` Civitai mirror (LTX-2.3 loras).
- comfy_manager: SKIPPED (LAN-only).
- local_llm: SKIPPED (LAN-only).

Also: `build_*` function count is **77** (baseline says 73 as of 2026-05-13 — +4 functions since baseline). Not a break; note as drift.

## Action items for operator

1. **Bump ComfyUI to v0.23.0** — cheap-latent-preview for Flux 2 is the enabling piece for candidate #1; multi-threaded loading is a straight throughput win. Three minor releases behind is where the flag turns amber.
2. **Land FLUX.2 Klein 9B (GGUF)** — pair with `Fayens/Pulid-Flux2`. New `build_flux2_klein_*` methods can supersede or augment `build_faceswap*` for identity-critical portrait work. Fits 16 GB via unsloth GGUF quants — not exclusive-load.
3. **Retire WAN 2.2 references and rebuild WAN handlers on WAN 2.7** — the biggest gap in the current stack. Fine to stage via `build_wan27_*` first and deprecate the 2.2 builders in the next pass.

## Run metadata

- runtime: ~5m (cloud-clone → digest → deliver)
- WebSearch/Fetch call count: 9 WebSearch (no WebFetch this run — HF MCP + WebSearch summaries had enough)
- HF MCP call count: 11 (4 initial retries on `sort` param + 7 targeted arch queries)
- next scheduled run: 2026-08-06 (every-48h cadence)
