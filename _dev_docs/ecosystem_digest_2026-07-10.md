# Ecosystem digest — 2026-07-10

_Generated by the every-48h cloud routine. First run — no prior digest to diff. Backends reached: HF Hub (via MCP) + Web (WebSearch/WebFetch). LAN-only backends (`comfy_manager`, `local_llm`) deferred to operator's local cron. **`tools/upgrade_research.py` is not present in the repo** — see Run metadata._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Signal | Why |
|---|---|---|---|---|---|
| 1 | bundle root (all) | **ComfyUI v0.23.0** | GitHub (Comfy-Org) | released 2026-06-01 | Baseline is v0.20.3 → three minor versions behind. v0.23 adds multi-threaded model loading, PixelDiT, MediaPipe face detect, and Microsoft Lens. v0.22 (2026-05-20) added Stable Audio 3 + MoGe. |
| 2 | `flux2klein` arch | **FLUX.2 Klein 4B GGUF quantizations** (adithya-balaji/…, unsloth/…) | HuggingFace | uploaded 2026-07-10 (fresh today) | 4B base fits ~13 GB — GGUF Q4/Q6 brings it comfortably under the 16 GB ceiling for the RTX 5060 Ti. Klein 9B remains **heavy** (~29 GB, exclusive-load only). |
| 3 | `qwen` arch (image-edit method) | **Qwen-Image-Edit-2511** | Qwen / HF | released Feb 2026 | Character-identity consistency improved; multi-person group-photo fusion; popular LoRAs baked into base. Direct win for `img2img` + `photobooth` under the qwen arch. |
| 4 | `wan` arch (video_gen, video_img2video, video_animate) | **WAN 2.6** (Dec 2025) → **WAN 2.7** (March 2026 target) | Alibaba / WaveSpeed | released / upcoming | Spellcaster ships WAN 2.2 references. WAN 2.6 hits 15-second clips, 24 fps, up to 150 reference frames for identity lock. WAN 2.7 adds 9-grid I2V + instruction-based editing. Four minor releases behind. |
| 5 | face-identity (`photobooth`, `faceswap`) on Klein | **ComfyUI-PuLID-Flux2 v0.6.2** (iFayens) | GitHub / HF (Fayens/Pulid-Flux2) | last updated 2026-05-22 | Cluster item from the upgrade-pass baseline (2026-05-08 batch shipped PuLID-Flux2). This is the first working PuLID for FLUX.2 Klein 4B / 9B / Dev — safetensors, InsightFace + EVA-CLIP encoding. Verify current PuLID-Flux2 wiring is at v0.6.2. |

## Ecosystem releases since last digest

_First run — no diff possible. All below are versus the embedded baseline (ComfyUI 0.20.3, krita-ai-diffusion v1.50, upgrade-pass 2026-05-08)._

- **ComfyUI**: v0.21.1 (2026-05-13), **v0.22.0** (2026-05-20, MoGe + Stable Audio 3), **v0.23.0** (2026-06-01, multi-threaded loader + PixelDiT + MediaPipe + Microsoft Lens). See <https://github.com/Comfy-Org/ComfyUI/releases>.
- **krita-ai-diffusion**: **v1.51.0** (Fill/Expand + ROCm managed install), **v1.52.0** (2026-06-28, Krea 2 support + Anima regional prompting + broken auto-update), **v1.52.1** (2026-06-30, managed-server-connect fix; recommended for Krita 5.3.2.1). See <https://github.com/Acly/krita-ai-diffusion/releases>.
- **LM Studio**: **0.4.15** (2026-06-04, tensor parallelism for multi-GPU + physical batch size for llama.cpp), **0.4.16** (2026-06-25, "Locally" iOS/iPad app via LM Link). Earlier in 2026: 0.4.13 mlx-engine v1.8.1 (Qwen 3.5/3.6, Gemma 4), 0.4.10 MCP OAuth, 0.4.0 headless daemon `llmster`. See <https://lmstudio.ai/changelog>.
- **Models**:
  - **FLUX.2 Klein**: 4B and 9B (4-step distilled + 50-step base) family shipped; today saw fresh 4B GGUF (`adithya-balaji/FLUX.2-klein-4B-GGUF`), LiteRT INT8 (`litert-community/FLUX.2-klein-4B-LiteRT`), and OrbitQuant W2A3/W2A4/W3A3/W4A4 (`WaveCut/FLUX.2-klein-4B-OrbitQuant-*`). `wikeeyang/Flux2-Klein-9B-True-V2` is the ecosystem's heaviest-hit finetune (227K downloads, 165 likes).
  - **WAN**: 2.6 (Dec 2025), 2.7 targeted March 2026. Now four minor versions ahead of spellcaster's WAN 2.2 references.
  - **LTX**: **LTX-2** (Jan 2026, first open 4K + audio, 22B DiT) → **LTX-2.3** (2026-03-05, quality + prompt-adherence + audio improvements). No 2.4 or 3.0 yet.
  - **Z-Image**: Full **Z-Image base** (Alibaba Tongyi, 2026-01-27) — successor to Z-Image-Turbo. **Requires 24 GB VRAM** → mark **heavy**, not viable on 16 GB ceiling. Z-Image-Turbo remains the operator's choice; **Z-Image-Omni-Base** (unified gen+edit) is worth watching.
  - **Qwen-Image**: **Qwen-Image-2.0** (2026-02-10) — 7B params, native 2K, unified gen+edit, tops AI Arena. **Qwen-Image-Edit-2511** — identity consistency + multi-person group fusion + baked LoRAs.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| **PuLID-Flux2** | Active — `iFayens/ComfyUI-PuLID-Flux2` last updated 2026-05-22 at v0.6.2; native Klein weights on `Fayens/Pulid-Flux2`. Cross-check spellcaster's PuLID-Flux2 pin (shipped 2026-05-08). |
| **MagCache** | Active — `Zehong-Ma/ComfyUI-MagCache` (194 stars). Confirmed: WAN 2.2 1.5-2× speedup, Qwen-Image 1.75×, HunyuanVideo-1.5 1.7×, Flux-Kontext 2×. Was in the 2026-05-08 upgrade batch — verify Qwen-Image + HunyuanVideo-1.5 ratios are picked up. |
| **HyperSwap** | Active — HyperSwap 256 models (`1a/1b/1c`) landed in ComfyUI-ReActor. Cluster item from 2026-05-08 batch (HyperSwap 256 shipped). No new 512-variant yet. |
| **Differential Diffusion inpaint** | Steady state — no 2026-specific movement found in a targeted search. Cluster shipped 2026-05-08; likely no upgrade needed this cycle. |

## HF Hub trending / freshness signal

- **Flux2-Klein-9B-True-V2** (wikeeyang) — **227K downloads / 165 likes** — the finetune the community rallied around. True-V3 released 2026-06-25 (32K downloads already).
- **FLUX.2-klein-4B GGUF variants** — multiple fresh uploads today (2026-07-10), including a first LiteRT INT8 build for on-device inference.
- **Wan2.2-I2V-A14B-Diffusers-fni8** (jajmangold, 2026-07-08) — INT8/W8A8/dp4a quant of WAN 2.2 I2V, brings it to older SM 7.0 (Volta) cards. Not directly relevant on RTX 5060 Ti but confirms WAN 2.2 is still receiving quant work.
- **wan2.2-i2v-a14b-candle** / MLX ports — Apple Silicon builds landing; irrelevant for CUDA target.
- Trending sort in the MCP returned only zero-score placeholders — actual top-15 requires a follow-up via a different query (see Run metadata).

## Backend results

- `local_index`: **0 baseline candidates** — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` is empty (`{"schema_version": 1, "loras": {}}`). No LoRAs cataloged yet; the calibration store hasn't been written to.
- `huggingface`: **~55 candidates surfaced** across the 3 targeted queries + trending; top signals detailed above.
- `civitai`: SKIPPED — `tools/upgrade_research.py` is not present in the repo, so the civitai backend didn't run. WebSearch surfaced one civitai result (`Face-Identity Creator - Flux Klein 4B`) as a workflow, not a candidate.
- `comfy_manager`: SKIPPED (LAN-only).
- `local_llm`: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI bundle to v0.23.0** (or at least v0.22.0 to gate on MoGe / Stable Audio 3). Baseline 0.20.3 is three minor versions behind and the multi-threaded model loader in 0.23 is a meaningful cold-start win. Verify PixelDiT / MediaPipe changes don't break the 77 `build_*` functions on load.
2. **Add or upgrade the `wan` arch to WAN 2.6** (safe today) with a placeholder for 2.7. WAN 2.2 is now four minor versions behind and 2.6 already ships identity-lock reference frames (150) — a natural fit for `video_animate` under the `WAN_EXTRA_METHODS` tuple.
3. **Ship `tools/upgrade_research.py`** — the routine's spec assumes it exists and it's missing on `main`. Without it every future cloud run is degraded to WebSearch + HF MCP only, and the `local_index` / `civitai` backends never light up. Recommend restoring it (or scaffolding it) before the next 48-hour cycle.

## Run metadata

- Runtime: ~4m (target cap 60m)
- WebSearch calls: 12 / ~15
- WebFetch calls: 0
- HF MCP calls: 4 (`hub_repo_search` ×3, `paper_search` ×1)
- `tools/upgrade_research.py`: **NOT PRESENT** on `main` (commit `ccef78a`). Producer of the ISO-week json/md never ran. Digest assembled entirely from WebSearch + HF MCP per the section-10 failure path.
- Gmail MCP: **UNAUTHENTICATED** in this cloud sandbox — email delivery not attempted. Operator needs to complete OAuth in their claude.ai connector settings for the next run.
- Google-Drive MCP: available, backup attempted below.
- Repo state: 77 `build_*` functions (baseline was ~73 as of 2026-05-13; +4 is within normal drift). `lora_calibrations_sfw.json` is empty (`0` cataloged LoRAs).
- Next scheduled run: 2026-07-12 (48h)
