# Ecosystem digest — 2026-06-02

_Generated by the every-48h cloud routine. Runtime: ~12m. Backends reached: HF MCP + WebSearch/Fetch. `tools/upgrade_research.py` is **absent** from this cloud checkout (see Run metadata), so the local_index/huggingface/civitai backends could not be invoked; HF MCP queries were used as a substitute. LAN-only backends (comfy_manager, local_llm) deferred to operator's local cron._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Rating | Why |
|---|---|---|---|---|---|
| 1 | (tooling) ComfyUI bundle | **ComfyUI 0.23.0** (Jun 1, 2026) | GitHub | n/a | 3 minor versions ahead of baseline 0.20.3. Adds MediaPipe face detection, multi-threaded model loading (significant load-time wins), OAuth 2.1. Affects every workflow. |
| 2 | `build_klein_*` family | **FLUX.2-klein-9B-Blitz-ComfyUI** (`kkangnom/FLUX.2-klein-9B-Blitz-ComfyUI`) | hf | 31 dl / 0 likes (just landed Jun 1) | ComfyUI-native finetune exposing `Flux2KleinPipeline` — drop-in for current Klein build_* workers; lower friction than wiring up the BFL base + diffusers shim. |
| 3 | `build_klein_*` pose/character flows | **Flux.2-Klein-9B-MatchingPose LoRA** (`nhathoangfoto/Flux.2-Klein-9B-MatchingPose`) | hf | 5.3K dl / 20 likes | The most-pulled Klein LoRA on the Hub right now. Pose-transfer + character-consistency, addresses the exact gap CivitAI 2642426 multi-angle (just shipped, commit `13c5675`) targets. |
| 4 | WAN image-to-video flows (currently WAN 2.2) | **WAN 2.6 i2v** (Alibaba, Dec 2025) | wan.video | n/a | Baseline references WAN 2.2; that's 4 minor versions stale. WAN 2.6 adds temporal-consistency + native audio reactivity. Heavy (likely ~14 GB) — flag as exclusive-load. |
| 5 | `build_klein_*` 16GB-VRAM ceiling | **FLUX.2-klein-9b-SDNQ-4bit** (`codeShare/FLUX.2-klein-9b-SDNQ-4bit`) | hf | 1.1K dl / 1 like | Explicitly tagged "16GB" + "T4" — slots under the RTX 5060 Ti ceiling without exclusive-load gymnastics. Active maintenance (last modified today, Jun 2). |

## Ecosystem releases since last digest

> First run — no prior digest to diff against. Treat the entries below as "since baseline 2026-05-13".

- **ComfyUI**: **0.23.0 released Jun 1, 2026** (baseline 0.20.3). Also: 0.22.0 (May 20 — MoGe, LTXV audio VAE), 0.21.0 (May 11 — auto-regressive video, VOID model). Three minor versions in three weeks. [releases](https://github.com/Comfy-Org/ComfyUI/releases)
- **krita-ai-diffusion**: **v1.51.0 released May 31, 2026** (baseline v1.50). Anima v1.0 control layers (Scribble/Line Art/Unblur), managed ROCm install for AMD, inpaint/outpaint with optional prompt. [release notes](https://github.com/Acly/krita-ai-diffusion/releases/tag/v1.50.0)
- **LM Studio**: **0.4.15 build 2 released May 29, 2026** — tensor parallelism for multi-GPU, Physical Batch Size load option. Earlier this cycle: 0.4.14 stable MTP speculative decoding, 0.4.13 mlx-engine v1.8.1. [changelog](https://lmstudio.ai/changelog)
- **Models** (substantively new since baseline):
  - **FLUX.2 Klein** family: stable; weekly cadence of community derivatives. New this week: Blitz-ComfyUI tune, SmartCharacterSwap LoRA (`nhathoangfoto`), GGUF (`AAGhigjo`), SDNQ-4bit quant (`codeShare`). All Jun 1–2, 2026.
  - **WAN**: 2.6 is current (Dec 2025); **WAN 2.7 was scheduled March 2026** — search did not surface a confirmed launch, worth a manual check at wan.video. Baseline 2.2 is stale.
  - **LTX**: **LTX-2.3 released Mar 5, 2026** — 22B DiT, native 4K @ 50 fps with synchronized audio. Heavy (>13 GB) — exclusive-load only. [news](https://www.cliprise.app/news/ltx-2-3-lightricks-open-source-4k-video-native-audio)
  - **Z-Image**: full base **Z-Image** (not just Turbo) **released Jan 27, 2026**; **Z-Image-Edit** variant also available. Promotes the family from preview to full-stack.
  - **Qwen-Image-Edit**: superseded by **Qwen-Image-2.0** (Feb 10, 2026) — unified 7B model replacing both Qwen-Image (20B) and Qwen-Image-Edit. #1 AI Arena in both gen + edit categories at launch. Lightning V2.0 4-step/8-step distillations also available.

## HF trending shift

> Skipped — no prior digest for diff. The `trendingScore` sort on `text-to-image` returned mostly stale 2022–2023 template repos this run, so trending data was not load-bearing for the candidate ranking; the Klein-family hub_repo_search produced the actionable signals instead.

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | Already shipped per 2026-05-08 upgrade pass. Upstream `iFayens/ComfyUI-PuLID-Flux2` still active; training scripts temporarily removed pending stability fixes. No action. |
| MagCache | Already shipped. New: HunyuanVideo-1.5 support landed Nov 2025 (1.7× speedup) — relevant only if operator adds HunyuanVideo builders. |
| HyperSwap | Already shipped (HyperSwap 256). **Heads up**: FaceFusion 3.3.0 mid-2025 switched license from MIT → OpenRAIL-AS — worth confirming compatibility with project's licensing posture. |
| Differential Diffusion inpaint | Already shipped. ComfyUI migrated nodes to V3 schema — verify the spellcaster builders still resolve them post-0.23 bump. |

## Backend results

- `tools/upgrade_research.py`: **SCRIPT MISSING** from this checkout (see Run metadata). Substituted with HF MCP queries.
- local_index: would have yielded **0** candidates — `comfyui-spellcaster/spellcaster_core/lora_calibrations_sfw.json` contains an empty `loras: []` list (schema present, no entries).
- huggingface (via MCP): **~40** Klein-family + WAN 2.2 repos surveyed; **5** surfaced into top-5 / mentions.
- civitai: not reachable this run (no script).
- comfy_manager: SKIPPED (LAN-only).
- local_llm: SKIPPED (LAN-only).

## Action items for operator

1. **Bump ComfyUI 0.20.3 → 0.23.0** in the bundled portable. Three minor versions with material wins (multi-threaded loading, MediaPipe FD, OAuth 2.1). Lowest-risk highest-impact upgrade this cycle.
2. **Bump krita-ai-diffusion v1.50 → v1.51.0**. Anima v1.0 control-layer rework + optional-prompt inpaint flow matches the in-tree Krita commit (`46d903f`) intent.
3. **Audit `build_*` WAN references**. Baseline targets WAN 2.2 (4 minor versions stale). WAN 2.6 is GA, WAN 2.7 was scheduled March 2026 — verify whether to skip-ahead. Heavy model, will need exclusive-load wiring on 16 GB.
4. **Restore `tools/upgrade_research.py`** to the cloud checkout (or commit it if it lives only on the local box). Without it, this digest's "local_index/civitai" sections degrade to "skipped".

## Run metadata

- runtime: ~12m
- WebSearch calls: 9 · WebFetch calls: 1 · HF MCP calls: 5 (total external: 15, within 25 budget)
- WAN 2.7 launch status: unconfirmed — flagged for next run
- build_* function count this checkout: **75** (baseline 73 @ 2026-05-13; matches the two recent klein/krita feat commits)
- Anomaly: `tools/upgrade_research.py` referenced by the routine spec is not present in `laboratoiresonore/spellcaster@main` as of `46d903f`. Either the operator has it locally-only or the path drifted — confirm before next 48h cycle.
- next scheduled run: 2026-06-04 16:09 UTC
