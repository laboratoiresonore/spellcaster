# Ecosystem digest — 2026-08-08

_Generated by the every-48h cloud routine. First run. Runtime approx. 15m. Backends reached: WebSearch + WebFetch + Hugging-Face MCP. LAN-only backends (comfy_manager, local_llm) deferred to the operator's local cron. `tools/upgrade_research.py` was not present in this cloud clone, so `local_index`, `huggingface`, and `civitai` backends were replaced by direct WebSearch + HF MCP queries — noted in `## Run metadata`._

## Headline — top 5 upgrade candidates

| # | Spellcaster method/arch | Candidate | Source | Downloads/Likes | Why |
|---|---|---|---|---|---|
| 1 | Flux family text-to-image / image-editing | **FLUX.2 [klein] 4B** (`black-forest-labs/FLUX.2-klein-4B`, Apache 2.0) | hf | 377.3K / 842 | Unifies gen + edit in one compact ~13 GB checkpoint — fits the 16 GB VRAM ceiling with headroom. Sub-second inference. Native ComfyUI + PuLID-Flux2 support already available. This is the single highest-leverage swap in this run. |
| 2 | ComfyUI runtime bundle | **ComfyUI v0.23.0** (June 1, 2026) | web | — | Baseline is 0.20.3; three minor versions behind. 0.23.0 adds multi-threaded model loading (meaningful startup + swap-in win for the 16 GB ceiling) plus MediaPipe face detection, NVIDIA PixelDiT, Microsoft Lens. |
| 3 | Image-to-video / video generation | **WAN 2.7** suite (Alibaba, Apache 2.0, released 1–6 April 2026) | web | — | Weights-open successor to WAN 2.2 (baseline). Ships T2V, I2V, reference-to-video with voice cloning, and instruction-based editing under one license. WAN 2.5 / 2.6 are API-only; 2.7 is the actual upgrade path. |
| 4 | Fast t2i / distilled | **Z-Image Base 6B** (Tongyi-MAI, open-sourced January 2026) | hf/web | — | Full undistilled sibling of Z-Image-Turbo; supports CFG, negatives, better prompt adherence at the cost of slower gen. Fits comfortably below 16 GB. Natural pairing with the existing Turbo path when quality > speed. |
| 5 | Unified image gen + edit | **Qwen-Image-2.0** (10 Feb 2026) | web | — | Integrates Qwen-Image and Qwen-Image-Edit into one 7B model with native 2K resolution, professional typography, and unified generation-editing. Also of note: **Qwen-Image-Edit-2511** (Nov 2025) with drift mitigation, multi-person consistency, and built-in LoRA support — worth cross-checking against the current Qwen-Image-Edit `build_*` node. |

## Ecosystem releases since last digest

- **ComfyUI**: **0.21.0, 0.21.1 (May 13), 0.22.0 (May 20), 0.23.0 (June 1, 2026)** — baseline 0.20.3 is 3 minor versions stale. Notable: 0.22 adds MoGe and Stable Audio 3; 0.23 adds multi-threaded model loading. → https://github.com/Comfy-Org/ComfyUI/releases
- **krita-ai-diffusion**: **v1.51.0 → v1.51.1 → v1.52.0 → v1.52.1** (latest) since baseline v1.50. v1.51 added ROCm managed install; v1.52 adds Krea 2 support and requires manual reinstall (previous auto-update was broken). v1.52.1 fixes the "plugin not connecting to managed server after restart" bug. → https://github.com/Acly/krita-ai-diffusion/releases
- **LM Studio**: Stable at **0.4.16** (June 8, 2026) — no new release in July or August. Recent notable: 0.4.14 stable MTP speculative decoding, 0.4.15 tensor parallelism, 0.4.16 default context 8k + "Locally" iOS/iPad companion app. → https://lmstudio.ai/changelog
- **Models** (substantively new):
  - **Flux 2 Klein**: Released 14 Jan 2026 (4B + 9B, base + instruct, BF16 + fp8, Apache-2.0 on the 4B). Unsloth-GGUF quants available. `unsloth/FLUX.2-klein-base-9B` diffusers repo landed **5 Aug 2026** — the freshest artifact this run.
  - **WAN**: 2.6 (Dec 2025, API-only), **2.7 (Apr 2026, open weights)**. Multiple mature quant/distill artifacts on HF (Wan2.2 GGUF at 332K DLs, LIGHTX2V 4-step distill).
  - **LTX-Video**: **LTX-2.3** released March 2026 — 22B, synchronized video+audio in one pass, both `ltx-2-3-fast` and `ltx-2-3-pro` at 720p/1080p, open weights on HF.
  - **Z-Image**: **Z-Image Base 6B** (Jan 2026), Z-Image Edit, Z-Image Omni (Base testing).
  - **Qwen-Image**: **Qwen-Image-2.0** (10 Feb 2026, unified gen+edit, 7B, native 2K), **Qwen-Image-Edit-2511** (Nov 2025 refresh).

## HF trending shift

_Skipped — no prior digest to diff against on this first run._

## Upgrade-cluster watchlist

| Cluster | Status this run |
|---|---|
| PuLID-Flux2 | **Shipped and stable.** `iFayens/ComfyUI-PuLID-Flux2` — supports Klein 4B / 9B / Dev with native PuLID weights (Fayens/Pulid-Flux2 v1 & v2). Directly complements candidate #1. |
| MagCache | **Officially supports WAN 2.2** with 1.5–2× speedup. **SenCache** for WAN 2.2 T2V/I2V adds up to 1.4× more. Reported ~2.8–3.0× on WAN 2.1. Baseline lists MagCache as already-shipped — verify local wiring targets 2.2, not just 2.1. |
| HyperSwap | Stable. `hyperswap_1a_256` is FaceFusion's current default swapper; 1a/1b/1c variants trade speed vs realism. Already in baseline shipped-upgrades set (2026-05-08). Nothing new. |
| Differential Diffusion inpaint | Native in ComfyUI (Gaussian Blur Mask + Differential Diffusion + Inpaint Model Conditioning). Already shipped in baseline. Guides in 2026 recommend combining with mask feathering + multi-pass — worth an audit pass rather than a re-recommend. |

## Backend results

- local_index: **SKIPPED** — `tools/upgrade_research.py` not present in this cloud clone; `lora_calibrations_sfw.json` present but empty (schema-only, `loras: {}`) so it would not have produced candidates.
- huggingface (via HF MCP fallback): **20+** proposals surveyed across `flux-2 klein`, `wan 2.2 i2v` targeted queries.
- civitai: **SKIPPED** — no `tools/upgrade_research.py`; skipped for this run.
- comfy_manager: SKIPPED (LAN-only)
- local_llm: SKIPPED (LAN-only)

## Action items for operator

1. **Ship the tool.** `tools/upgrade_research.py` is missing from the cloud checkout — either the file isn't committed, or the routine's baseline expectation is wrong. Fix once and every subsequent 48h run gets the real ranked top-50 instead of a hand-curated top-5. (Corollary: `_dev_docs/upgrade_research/*` will start accumulating properly on the next run.)
2. **Bump the ComfyUI bundle to 0.23.0** — three minor versions of stale runtime is the largest quiet regression here; multi-threaded model loading is a direct win under the 16 GB ceiling.
3. **Prototype a Flux.2-klein-4B build path** — 13 GB fits 5060 Ti cleanly, unifies gen + edit, and the ecosystem has already shipped native PuLID + LoRA infrastructure. If Flux[dev] is currently the primary t2i path, klein-4B is the drop-in successor.
4. **Verify MagCache targets WAN 2.2**, not just 2.1 — official support is now 1.5–2× on 2.2 and the operator's baseline shipped MagCache before that support landed.

## Run metadata

- runtime: ~15m (from 16:11 UTC start)
- WebSearch calls: 8; WebFetch calls: 0; HF MCP calls: 3 (well inside the ~25-call budget)
- Repo state: 77 `build_*` functions (baseline was 73 on 2026-05-13 → +4 net). HEAD at `ccef78a` on `main`.
- `tools/upgrade_research.py`: not present in this clone; substituted with web + HF MCP research per section 10 failure protocol.
- Gmail MCP: authentication required (server surfaced an auth-needed notice). Email delivery deferred — operator can authorize via claude.ai connector settings and the next run will deliver via email.
- next scheduled run: **2026-08-10 ~16:11 UTC**
